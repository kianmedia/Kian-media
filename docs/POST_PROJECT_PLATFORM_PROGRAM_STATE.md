# برنامج ما بعد منصّة المشاريع — حالة التنفيذ

> **الحالة المستهدفة:** كل موديول `CODE READY — SQL/PRODUCTION ACCEPTANCE REQUIRED`.
> لا يُقال LIVE قبل Push + SQL + قبول الإنتاج بواسطة المالك.

---

## Batch 0 — نقطة الأمان والتجميد · ✅

### حالة Git عند البداية

| البند | القيمة |
|---|---|
| `START_HEAD` | `1f0faff11d1cd0c910191fda75f927565a014d11` |
| `origin/main` | `1f0faff` — **مطابق** |
| Commits غير مرفوعة | **0** |
| Working Tree | **نظيف** — لا حاجة إلى Checkpoint |

> **ملاحظة:** دفعتَ العمل السابق قبل مغادرتك، فالقاعدة نظيفة تمامًا وكل ما
> يظهر غير مرفوع بعد الآن هو **من عمل هذه الليلة وحده**.

### التجميد — مفروض آليًّا لا بالوعد

`tests/project_platform_freeze.test.js` + `tests/fixtures/project_platform_freeze.json`

**31 مسارًا مجمَّدًا** يُقارَن كل منها بـ`START_HEAD`. الاختبار يفشل باسم الملفّ
عند أيّ لمسة. أُثبِتت **عدم تفريغه**: تعديل `lib/portal/large-projects.ts` يُسقطه،
واستعادته تُعيده أخضر.

ثلاثة حرّاس:
1. صفر تغيير في المسارات المجمَّدة منذ `START_HEAD`.
2. قائمة المسارات نفسها تُفحص — لو قُلّصت لصار الاختبار يمرّ دائمًا.
3. الملفّات الحرجة ما زالت موجودة — لا حذف صامت.

**رفع التجميد قرار صريح** يتطلّب تحديث ملفّ النقطة بـcommit مستقلّ يشرح السبب.

### ما يُسمح للموديولات الجديدة

قراءة `project_id` وعرض اسم المشروع فقط. **ممنوع**: تعديل `projects` أو أعمدته أو
سياساته، أو تغيير واجهة المنصّة أو دورة حياتها. العلاقة اختيارية وإضافية وبأقلّ
دائرة انفجار.

---

## حالة المراحل

| المرحلة | الحالة |
|---|---|
| Batch 0 — التجميد ونقطة الأمان | ✅ منجز |
| Phase 1 — مركز الاتصالات | ⏳ |
| Phase 2 — مركز التشغيل | ⏳ |
| Phase 3 — CRM | ⏳ |
| Phase 4 — المالية | ⏳ |
| Phase 5 — التقارير التنفيذية | ⏳ |

---

## معوّقات خارجية معروفة سلفًا

- **Google Apps Script غير منشور** — البريد لا يصل فعليًّا. يُجهَّز العقد والتوقيع
  والحمولة، **ولا يُنشر**.
- **Zoho Books** — لا مزامنة حيّة، ولا اعتماد، ولا ادّعاء «متّصل».
- **WhatsApp** — Placeholder فقط، بلا API حيّ.
- لا إرسال بريد أو واتساب حقيقيّ أثناء التطوير أو الاختبار.
