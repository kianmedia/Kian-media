# مؤجَّل — ما بعد التشغيل

> **لم يبدأ أيّ بند هنا، ولن يبدأ دون قرار صريح منك.** هذا سجلّ نيّة لا خطة تنفيذ.
> كل بند مذكور بسببه وبما يمنعه اليوم، حتى لا يُعاد فتح النقاش من الصفر لاحقًا.

---

## مؤجَّل بأمرك — ممنوع البدء

| البند | ما يمنعه اليوم |
|---|---|
| **PWA** | تجربة الجوال الحالية تكفي التشغيل؛ التحوّل يستهلك دورة كاملة |
| **تطبيق أصليّ** | قرار منتَج لا هندسة — يسبقه استقرار التشغيل |
| **مساعد ذكاء اصطناعيّ** | يحتاج بيانات نظيفة ومستقرّة أوّلًا |
| **توليد محتوى آليّ** | نفس السبب + قرار تحريريّ |
| **تنبؤات AI** | لا تاريخ تشغيليّ كافٍ بعد |
| **WhatsApp API الحيّ** | يحتاج حسابًا واعتمادًا تجاريًّا وقوالب معتمَدة |
| **Zoho Live Sync** | يحتاج بيانات اعتماد وقرار مطابقة حسابيّة |
| **SMS** | مُطفأ في Supabase، ولم يُطلَب تشغيليًّا |
| **تغييرات MFA** | مجمَّد بأمرك |
| **S4a / S4b** | مكتوب ولم يُطبَّق — قرارك |
| **`org_admin`** | مكتوب ولم يُطبَّق — قرارك |
| **أيّ تطوير جديد لمنصّة المشاريع** | 🧊 مجمَّدة، والتجميد مفروض باختبار |

---

## بنود معلّقة من عمل سابق — ليست جديدة

- **`M-012`** — اختبار النموذج العامّ بعد Fix C (`/quote-request` و`/opportunities`
  في نافذة خاصّة بلا تسجيل دخول). دقيقتان.
- **Fix D** — تصعيد `PATCH /profiles`. **لم يُثبَت حيًّا**؛ استعلام التحقّق في
  `MORNING_SECURITY_EXECUTION_CHECKLIST.md`. صفر صفوف ⇒ لا وجود للثغرة.
- **السلوك الحيّ لصلاحيات المونتير** — `project_editor_permissions_BEHAVIOR_DIAGNOSTIC.sql`
  بجلستَي مونتير ومالك.
- **حزمة Transition Approval** — مكتوبة ولم تُطبَّق.
- **شاشة الاستيراد بلا مُنتقي مشروع** — يجب إنشاء المشروع أوّلًا. قيد سير عمل لا خلل.

---

## قاعدة

لا يُنقل بند من هنا إلى التنفيذ إلا بقرارك، وبعد أن يستقرّ ما قبله على الإنتاج.
