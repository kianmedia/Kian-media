# 🔴 حادثة — ملفّات مجهولة في شجرة العمل الرئيسية

> **التاريخ:** ٥ أغسطس ٢٠٢٦ · **الحالة: `UNRESOLVED — AWAITING KHALED`**
> ⛔ **لم أحذف ولم أُزِح ولم أعمل `reset` ولا `stash` لأيّ ملفّ منها.**

---

## ١. ماذا وجدت

في شجرة العمل `/Users/khaledm4/Documents/GitHub/Kian-media` (على
`integration/v2-1-overnight`) توجد **٨٦ ملفًّا غير متعقَّب** ضمن ١٩ مدخلًا:

| التصنيف | العدد |
|---|---|
| نسخة مطابقة تمامًا موجودة على فرع ما | **٦٢** |
| 🔴 **لا نسخة مطابقة على أيّ فرع — فريدة على القرص** | **٢٤** |
| إجمالي حجم الفريدة | **٩٬٣٧١ بايت** |

**الجرد الكامل ببصماته:** `docs/incidents/UNTRACKED_WORKING_TREE_MANIFEST.json`

**الملفّات الفريدة** كلّها من تخطيط ما قبل Wave 1 (قبل مجموعات المسارات
`(ar)`/`(en)`): `app/en/*` · `app/quote-request/layout.tsx` ·
`app/terms/layout.tsx` · `app/live-status/layout.tsx` … إلخ.

**أوقات تعديلها:** `2026-08-02T22:31` → `2026-08-03T04:14`.

---

## ٢. 🔴 لماذا هذا حاجب

`npm run build` في الشجرة الرئيسية **يفشل**:

```
You cannot have two parallel pages that resolve to the same path.
Please check /(ar)/live-status/page and /live-status/page.
```

⇒ لا بناء، ولا Playwright (حزمته تبني)، ولا بوّابة كاملة، ولا دمج آمن.
⚠️ **وأخطر من ذلك:** أيّ `git add -A` كان سيبتلع هذه الملفّات في commit.

---

## ٣. ما لا أعرفه — وأقوله صراحةً

- ⛔ **لا أعرف من أنشأها.** لا `stash`، ولا commits مجهولة في `reflog`،
  ولا قاعدة في `.gitignore` أو `.git/info/exclude` تفسّرها.
- ⚠️ أوقات التعديل تقع **داخل نافذة أوّل تشغيل ليليّ لهذا البرنامج**، لكنّي
  **لا أستطيع إثبات** أنّها من عمليّة قمتُ بها.
- ⛔ **ولا أدّعي أنّها بلا قيمة.** ٢٤ منها لا نسخة لها في أيّ مكان.

---

## ٤. ما فعلتُه بدلًا من لمسها

أنشأت **شجرة عمل معزولة** (`git worktree`) للفرع، وأكملت العمل فيها.
⇒ الشجرة الرئيسية **لم تُمسّ إطلاقًا**، والملفّات الـ٨٦ ما تزال في مكانها كما هي.

---

## ٥. خيارات خالد

| # | الخيار | الأثر |
|---|---|---|
| **أ** | **افحصها ثمّ احذفها** إن تبيّن أنّها بقايا | يُصلح البناء فورًا |
| **ب** | **انقلها خارج المستودع** للحفظ | يُصلح البناء ويحفظها |
| **ج** | **اعتمدها** على فرع مستقلّ إن كانت عملًا مقصودًا | تحتاج توفيقًا مع مجموعات المسارات |

**فحصها بأمان (قراءة فقط):**

```bash
cd ~/Documents/GitHub/Kian-media && git status --porcelain && git diff --no-index /dev/null app/en/page.tsx | head -40
```

⛔ **ولا تُشغّل `git clean` قبل أن تقرّر** — يمحو الـ٢٤ الفريدة بلا رجعة.
