# خطة إدخال «مسبار 10» إلى الإنتاج

> **لا يُنفَّذ منها شيء في جلسة التطوير.** هذه خطة تنفّذها أنت بعد العودة.
> كل خطوة لها **شرط نجاح صريح**. إن سقط شرط، **قف** ولا تُكمل — الخطوة التالية
> تفترض نجاح ما قبلها.

---

## ⚠️ اقرأ هذا أوّلًا — شرطان يسبقان كل شيء

**1. ✅ ملفّ Excel جاهز ومُعالَج.**
`docs/input/MISBAR10_PLAN.xlsx` — 11 كتلة، 79 مخرجًا، توزيع مطابق لتحقّقك.
حزمة الاستيراد الحقيقية جاهزة: `docs/misbar10_import_ready.csv` و
`docs/misbar10_import_payload.json`. المعاينة الحقيقية عبر المحرّك أعطت:
**مشروع رئيسيّ 1 · مراحل 11 · مخرجات 79 · مقبول 79 · غير صالح 0 · مكرّر 0**.

**2. الجدول `deliverables` لا يتحمّل مسبار اليوم.**
عمود `type` مُقيَّد بـ`check (type in ('video','photo','other'))`. الطباعة والبثّ المباشر
والفعاليات والتنفيذ الميدانيّ والهدايا والتقارير **ترفضها قاعدة البيانات الآن**.
⇒ **تشغيل `project_platform_large_projects_RUNME.sql` شرط لا مفرّ منه قبل أيّ استيراد.**

---

## المرحلة أ — الدفع والنشر

| # | الخطوة | شرط النجاح |
|---|---|---|
| 1 | `git push origin main` من GitHub Desktop | لا commits متبقّية |
| 2 | انتظار Vercel | Deployment = **Ready** (لا Building ولا Error) |
| 3 | فتح البوابة والدخول بحساب المالك | الصفحة تفتح، **لا شاشة بيضاء** |

> **لماذا الخطوة 3 قبل أيّ SQL:** الكود كلّه مبنيّ على Feature Detection، فالمفترض أن
> الموقع يعمل **قبل** الترحيل مع ظهور «الترحيل معلّق» في الأماكن الجديدة.
> **إن انكسرت صفحة هنا فهذا خلل في Feature Detection — أبلغني ولا تُكمل.**

---

## المرحلة ب — تشغيل SQL بالترتيب

**الترتيب إلزاميّ.** الاستيراد يكتب في أعمدة يُنشئها ملفّ المشاريع الكبيرة.

| # | الملفّ | النوع |
|---|---|---|
| 1 | `project_platform_large_projects_PREFLIGHT.sql` | قراءة — ماذا سيتغيّر |
| 2 | `project_platform_large_projects_RUNME.sql` | **تغيير** |
| 3 | `project_platform_large_projects_POSTCHECK.sql` | قراءة — تحقّق |
| 4 | `project_bulk_import_PREFLIGHT.sql` | قراءة |
| 5 | `project_bulk_import_RUNME.sql` | **تغيير** |
| 6 | `project_bulk_import_POSTCHECK.sql` | قراءة |

**قواعد:**
- شغّل PREFLIGHT واقرأ نتيجته **قبل** RUNME. لا تتخطَّه.
- كل RUNME داخل `begin … commit`: الفشل = تراجع ذاتيّ كامل، لا حالة نصفيّة.
- ظهرت `raise exception`؟ **لا تُعِد التشغيل.** الرسالة تقول ما لم يتحقّق. أرسلها لي حرفيًّا.
- **لا تشغّل ROLLBACK** إلا عند عطل مُثبَت.

> 🔒 **ما لا يُشغَّل في هذه الجلسة إطلاقًا:** Fix D · S4a · S4b · org_admin ·
> أيّ Diagnostic أو Cleanup أو Rollback أمنيّ. مجمَّدة بأمرك.

---

## المرحلة ج — اختبار المنصة قبل لمس مسبار

**لا تُدخل مسبار مباشرة.** جرّب على مشروع تجريبيّ يمكن حذفه:

| # | الاختبار | شرط النجاح |
|---|---|---|
| 1 | أنشئ مشروعًا تجريبيًّا | يُحفظ **بلا تاريخ** بنجاح |
| 2 | أنشئ مرحلتين تحته | تظهران كمشروعين فرعيّين مرتّبين |
| 3 | أضف مخرجًا نوعه **طباعة** | يُقبل — هذا يُثبت أن القيد الضيّق زال |
| 4 | أضف مخرجًا بلا تاريخ | `awaiting_schedule`، **لا يظهر بالأحمر ولا يُحسب متأخّرًا** |
| 5 | افتح Dashboard | العدّادات تُطابق ما أدخلتَه |
| 6 | جرّب فلترًا وإجراءً جماعيًّا | العدد المتأثّر معروض، والنتيجة صادقة |
| 7 | **احذف المشروع التجريبيّ** | لا يبقى أثر |

**الخطوة 3 هي الاختبار الحاسم.** إن رُفض «طباعة» فالترحيل لم يُطبَّق فعليًّا — قف.

---

## المرحلة د — إدخال مسبار 10

| # | الخطوة | ملاحظة |
|---|---|---|
| 1 | الملفّ **موجود مسبقًا** في `docs/input/MISBAR10_PLAN.xlsx` | لا شيء تفعله هنا |
| 2 | اختر العميل أو أنشئه | لا تُنشئ عميلًا مكرّرًا |
| 3 | أنشئ المشروع الرئيسيّ «مسبار 10» | `schedule_status = awaiting_schedule` |
| 4 | ارفع الملفّ في شاشة الاستيراد واضغط **Preview** | ⛔ **لا تضغط Execute بعد** |
| 5 | **راجع أرقام Preview** | ⬇ القسم التالي |
| 6 | **Dry Run** | يتحقّق دون كتابة |
| 7 | **Confirm Import** | بعد رضاك عن الأرقام فقط |
| 8 | تحقّق ما بعد الاستيراد | العدد الفعلي = عدد Preview |
| 9 | **أعِد رفع الملفّ نفسه واضغط Preview ثانية** | ⬇ الاختبار الحاسم |
| 10 | عيّن مدير المشروع والفريق | |
| 11 | اضبط `client_visible` لكل مخرج | **الافتراضيّ مخفيّ** — الإظهار قرار واعٍ |
| 12 | أبقِ الجدولة `awaiting_schedule` | التواريخ لاحقًا بـBulk Scheduling |
| 13 | ارفع ملفات المشروع الأساسية | |

### الخطوة 5 — ماذا تقرأ في Preview

قارن بتوقّعك (**11 مرحلة · 79 مخرجًا**):

- **المتوقّع الآن مُثبَت مسبقًا:** 1 مشروع · 11 مرحلة · 79 مخرجًا · 0 غير صالح.
  **تحذير واحد متوقَّع:** تجاهُل الورقة الجانبية «ورقة1» — هذا صحيح ومقصود.
- **إن اختلف العدد على الإنتاج** ⇒ لا تُكمل. يعني أن الملفّ المرفوع ليس النسخة
  التي عُولجت هنا. **لا تحذف صفوفًا ولا تدمجها للوصول إلى 79.** أرسل لي الفرق.
- `duplicate > 0` في **أوّل** استيراد ⇒ الملفّ فيه صفوف مكرّرة فعلًا — راجعه قبل Execute.

### الخطوة 9 — الاختبار الحاسم لمنع التكرار

بعد استيراد ناجح، ارفع **نفس الملفّ** واضغط Preview:

- ✅ **المتوقّع:** `deliverablesToCreate = 0` و`duplicate = العدد الكامل`.
- ❌ **إن أظهر أنه سينشئ مخرجات جديدة: قف فورًا ولا تضغط Execute** — هذا يعني أن
  `external_key` غير مستقرّ، وستحصل على نسخة ثانية من المشروع كلّه. أبلغني.

---

## المرحلة هـ — القبول بثلاثة حسابات

**لا تعتبر المشروع شغّالًا قبل هذه الثلاثة.**

**المالك:** يرى كل المراحل والمخرجات · يعدّل ويعتمد · يرى الملاحظات الداخلية · Bulk Actions تعمل.

**الموظف:** يرى ما تسمح به صلاحيته فقط · يعمل على المخرجات المسندة إليه ·
**لا يستطيع تعديل صلاحيات أحد** (هذا يُثبت أن Fix B حيّ على الإنتاج).

**العميل — الأهمّ:** يرى المشروع والمراحل المصرّح بها والمخرجات `client_visible` فقط.
**ويجب ألّا يرى:** الملاحظات الداخلية · التكاليف · الربحية · أجور الفريق ·
بيانات الاستيراد (`external_key`, `import_batch_id`, `source_row_number`) ·
السجلّ الداخليّ · الملفات الداخلية · النسخ غير المرسلة للمراجعة · المخرجات المخفيّة.

> **افتح حساب العميل في نافذة خاصّة وانظر بعينك.** إخفاء الواجهة ليس أمنًا؛
> لو ظهر أيّ بند من القائمة أعلاه فهي **ثغرة تسريب** — أبلغني فورًا.

---

## المرحلة و — بند معلّق منفصل عن مسبار

`M-012` — **اختبار النموذج العامّ بعد Fix C**: افتح `/quote-request` و`/opportunities`
في نافذة خاصّة **دون تسجيل دخول** وأرسل طلبًا تجريبيًّا. Fix C عدّل ستّة مُسنِدات
ثلاثة منها مكشوفة لـ`anon`؛ الملفّ صُمّم ألّا يكسر المسار العامّ لكن **هذا لم يُختبر حيًّا**.
دقيقتان. إن انكسر، الحلّ في §5 من `SECURITY_EMERGENCY_ROLLBACK_PLAN.md`.

---

## إن ساء شيء

| العَرَض | التصرّف |
|---|---|
| SQL رفع `raise exception` | لا تُعِده. أرسل النصّ حرفيًّا. القاعدة تراجعت ذاتيًّا |
| صفحة بيضاء بعد Push وقبل SQL | خلل Feature Detection — أبلغني، الترحيل ليس الحلّ |
| «طباعة» مرفوضة بعد الترحيل | الترحيل لم يُطبَّق — أعِد قراءة POSTCHECK |
| Preview يعيد إنشاء ما استُورد | ⛔ لا تضغط Execute. `external_key` غير مستقرّ |
| العميل يرى بيانات داخلية | ⛔ أخفِ المشروع عنه فورًا وأبلغني |
| مطالبة MFA غير متوقّعة | `update public.mfa_settings set enforcement_mode='off' where id=1;` |

**قاعدة عامّة:** أضيق تدخّل أوّلًا. لا تتراجع عن طبقة تحت طبقة مُطبَّقة فوقها.
