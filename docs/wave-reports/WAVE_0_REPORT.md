# WAVE 0 — تقرير الموجة · السلامة والتحصين والبيئة

> **الفرع:** `feat/wave-0-safety-regression-environment` (مفرَّع من `docs/v2_1-audit`)
> **المرجع:** [`MASTER_BRIEF_v2.1.md`](../../MASTER_BRIEF_v2.1.md) §4 WAVE 0
> **التاريخ:** 2026-08-02
>
> ❌ لا Push · ❌ لا Merge · ❌ لا Deploy · ❌ لا تعديل على `main` · ❌ **لم يُشغَّل أي SQL
> على أي قاعدة** · ❌ لم تُمَسّ Zoho أو WhatsApp أو المساعد الذكي أو مهام cron الثلاث ·
> ❌ لا cron جديد · ❌ لا سرّ ولا Token ولا DSN في أي ملف.

---

## ١. ملخّص ما تمّ

Wave 0 هي الموجة الوحيدة التي يقابلها فراغ حقيقي في المستودع (**٧١٪ بناء جديد**).
نُفِّذت ثلاث كتل: **(أ)** موافقة خصوصية صريحة ومُسجَّلة ومُصدَّرة على النماذج الأربعة،
خلف راية مطفأة؛ **(ب)** سدّ فجوتين أمنيتين حقيقيتين — HSTS، ومسار عام بلا حدّ معدّل
يصل إلى القاعدة بمفتاح الخدمة؛ **(ج)** البنية الغائبة كليًا: فصل البيئات، النسخ
الاحتياطي، وصلة الرصد، DNS البريد، وسجلّ الأعلام الموحّد.

**المبدأ الحاكم طوال الموجة:** ثلاثة بنود طلبها v2.0 كـ«بناء» تبيّن أنها **موجودة**،
فتحوّلت إلى تدقيق تغطية بدل نظام موازٍ (G13).

---

## ٢. نتائج البناء والفحص

| الفحص | الأساس (قبل) | بعد Wave 0 | الحكم |
|---|---|---|---|
| `npm test` | 3706/3706 | **3725/3725** | ✅ **+19 اختبارًا، صفر فشل** |
| `npm run typecheck` | exit 0 | **exit 0** | ✅ |
| `npm run lint` | exit 0 · ٤ تحذيرات | **exit 0 · ٤ تحذيرات** | ✅ **بلا تحذير جديد** |
| `npm run build` | ناجح | **ناجح · ٥٦ صفحة** | ✅ |

**التحذيرات الأربعة سابقة لهذه الموجة** وفي ملفات لم تُمَسّ:
`ProjectCoreDashboard.tsx:72` · `RenterRentalView.tsx:320,335` · `PwaProvider.tsx:118`.

### ⚠️ اختبار قائم كسرته الموجة ثم أُصلح بالطريقة الصحيحة
توسيع `IntakeResult` كسر `tests/public_forms_honest_success_phase1.test.js`
(«captureIntake no longer returns void») لأنه يثبّت شكل السطر الأول حرفيًا.
**لم أُضعِّف الاختبار** — أعدتُ تشكيل التعديل ليُبقي السطر المثبَّت كما هو ويضيف الحقل
تحته، مع تعليق يمنع تكرار الخطأ. الاختبار الحارس بقي بكامل قوته.

### تحقّق حيّ في متصفح على بناء إنتاجي محلي
| ما أُثبت | النتيجة |
|---|---|
| HSTS يُخدَم فعلًا | `max-age=63072000; includeSubDomains` · **بلا `preload`** ✅ |
| الترويسات القائمة لم تُفقد | `X-Frame-Options` · `X-Content-Type-Options` · `Referrer-Policy` · `Permissions-Policy` (**`geolocation=(self)` سليمة**) · CSP بنصفيها ✅ |
| 🔒 **الراية OFF = لا تغيير** | **صفر** حقل موافقة في DOM ✅ |
| الراية ON | حقل واحد · `required` · **غير مُحدَّد افتراضيًا** · رابط `/privacy-policy` بـ`target=_blank` · النصّ العربي مطابق ✅ |
| نقر الرابط لا يقلب الموافقة | نقر الرابط: `false → false` · نقر التسمية: `false → true` ✅ |

> 🔒 كل التحقّق على **بناء محلي**. المسارات المفحوصة (`/robots.txt`, `/upload-files`)
> **لا تلمس Supabase**. ❌ لم يُجرَ أي اختبار متصل بـProduction.

---

## ٣. ما نُفِّذ — بنداً بنداً

### V2-0.1 · موافقة الخصوصية ✅
| المتطلب | الحالة |
|---|---|
| V2-0.1-A…D · checkbox على النماذج الأربعة | ✅ عبر مكوّن مشترك واحد |
| V2-0.1-E · `/quick-access` | ❌ **مشطوب** — الصفحة روابط بلا نموذج (مقدمة خاطئة في v2.0) |
| V2-0.1-F · حفظ الموافقة + الوقت | ✅ كود جاهز · **SQL ملف فقط** |
| V2-0.1-G · النصّ والرابط | ✅ النصّ الحرفي من v2.1 |

**ثلاثة قرارات تصميم تستحق الذكر:**

1. **الموافقة مُصدَّرة لا boolean.** `CONSENT_VERSION` يُحفظ مع كل صفّ. بدونها، أول
   تغيير في الصياغة يجعل **كل الصفوف التاريخية تدّعي موافقة على نصّ لم يره أصحابها**.
   اختبار يجزّئ النصّ ويفشل إن تغيّر بلا رفع النسخة في نفس الـcommit.
2. **الرفض لا يُسجَّل.** «لم تُسجَّل موافقة» و«رُفضت الموافقة» ادّعاءان مختلفان؛
   الثاني غير صحيح لطلب أُرسل والراية مطفأة. لذلك `consent_given` يبقى `NULL`
   للصفوف التاريخية، **بلا `DEFAULT false`**.
3. **🔴 الحفظ عبر دالّة ثانية لا معاملات جديدة.** PostgREST يحلّ الدوال **بأسماء
   المعاملات**؛ إضافة `p_consent_*` إلى `capture_public_intake` كانت ستجعل **كل إرسال
   عام يفشل بـPGRST202** بين نشر الكود وتشغيل الترحيلة — تحويل تحسينِ خصوصيةٍ إلى
   انقطاع كامل في استقبال الطلبات. الشقّ إضافي بعد التزام الصفّ: غياب الدالّة يكلّف
   سطر سجلّ (`PUBLIC_INTAKE_CONSENT_NOT_RECORDED`) **والطلب لا يُفقد**. اختبار يمنع
   إعادة إدخال المعامل.

### V2-0.2 · تدقيق الأسرار ✅
فحص الشجرة **وكامل سجلّ Git (٦٬٠٣٤ كائنًا)** → **نظيف**. لم يُلتزَم ملفّ `.env` قط،
و`.env.local` مُستثنى. ⇒ **لا حاجة لإعادة كتابة التاريخ ولا لتدوير مفاتيح بسبب المستودع.**
**فجوة واحدة:** `SHEETS_ENDPOINT` ثابت في الكود — ليس سرًّا (يُشحن للمتصفح بحكم بنيته)
لكنه **لا يمكن أن يختلف بين البيئتين**، فكل إرسال تجريبي يصل إلى ورقة الإنتاج.
مُبلَّغ لـWave 1 · V2-1.6، **لم يُغيَّر** (خارج النطاق ويمسّ مسار إرسال حيّ).

### V2-0.3 · فصل البيئات ✅ (كود) · ⏳ (البيئة نفسها)
`.env.example` **مُوسَّع لا مُعاد كتابته** — حافظتُ على فخّ `PROJECT_EMAIL_ALERTS_ENABLED`
الموثَّق. أُضيفت وسوم النطاق و**١٤ متغيّرًا يقرأها الكود وكانت غائبة**.
`docs/ENVIRONMENTS.md` يوضّح ما يغفله v2.0: **عزل القاعدة وحده ليس عزلًا** — مفاتيح
Zoho/WhatsApp/n8n المشتركة تعني أن نشرة تجريبية قد **تُصدر فاتورة حقيقية**.
`scripts/seed-preview.ts` بثلاثة حواجز، **الحاسم منها اختلاف عنوان الهدف عن الإنتاج**.

### V2-0.4 · النسخ الاحتياطي ✅ (الآلية) · ⏳ (الأسرار)
**لم يكن هناك نسخ احتياطي من أي نوع.** workflow بتشغيل يدوي فقط، و`schedule` معلَّقة
عمدًا: مهمّة ليلية تفشل في بريد يعتاد الجميع تجاهله **أسوأ من لا مهمّة**. تفشل بصوت
عالٍ عند نسخة صغيرة أو فهرس أرشيف شبه فارغ.
🔴 `RESTORE_RUNBOOK` يذكر صراحةً **ما لا تغطّيه النسخة**: ملفات Storage (صور العهدة،
أدلة التأجير، المخرجات) **فجوة مفتوحة**.

### V2-0.5 · المراقبة ⚠️ جزئي — انحراف G9 مُعلَن
`@sentry/nextjs` **لم يُثبَّت**. السبب: G12 يوجب تبرير كل حزمة، و**شحن `import` غير
قابل للحلّ يكسر البناء للجميع** ويخالف Definition of Done.
المُنجَز: `lib/observability.ts` — **وصلة** بلا اعتماديات، no-op حقيقي بلا `SENTRY_DSN`،
وكل شيء يمرّ عبر `pgRedact()`. وصل الناقل لاحقًا يمسّ **ملفًا واحدًا**.
`docs/OBSERVABILITY.md` يحمل أمر التثبيت وخطوات الضبط. ⏳ مراقب التوفّر خطوة يدوية.

### V2-0.6 · أمان المسطح العام ✅ + اكتشاف
**HSTS** أُضيف (بلا `preload` — قرار لا رجعة فيه يحتاج اعتمادك وحصر النطاقات الفرعية).
**تدقيق التغطية** على ٣٤ مسار POST وجد فجوتين:

| # | المسار | الحالة |
|---|---|---|
| ١ | `/api/public/secure-document` | ✅ **أُصلح** — كان المسار العام الوحيد بلا حدّ، وكل نداء يصل إلى Postgres **بمفتاح الخدمة**. الردّ عند التجاوز **بشكل «رمز غير صالح»** لا `429`، كي لا يؤكّد للمُعدِّد أنه يطرق بابًا حقيقيًا |
| ٢ | `/api/integrations/whatsapp/quote-request` | ⏳ **مُبلَّغ ولم يُمَسّ** — عام، بلا مصادقة، يستدعي RPC بصلاحية service_role. 🔒 **G7: WhatsApp مجمَّد** ⇒ يحتاج قرارك |

### V2-0.7 · DNS البريد ✅ (مستند)
مرتَّب كي لا ينقلب: **حصر المرسِلين أولًا** (SPF ناقص مرسِلًا يزيد الحجب)، والبدء بـ
`p=none` لا `p=reject`. ويذكر صراحةً أن **DNS شرط ضروري غير كافٍ** — السبب الجذري
الموثَّق لعدم وصول بريد البوابة هو غياب فرع `portal_notify` في Apps Script.

### V2-0.8 · سجلّ الأعلام ✅
❌ لم أُنشئ `lib/flags.ts` — كان سيصبح **مصدرًا ثالثًا** فوق ١٨ جدول إعدادات و~٢٠
متغيّر بيئة (G13-5). السجلّ يوثّق القائمين، ويكشف أن **ثلاثة «أعلام» ليست أعلامًا**
بل مفاتيح opt-out تُطفئ عند ضبطها.

### V2-0.A · الاختبارات الأساسية ✅
الحدّ الأدنى في §O **موجود أصلًا** ضمن 3706 اختبارًا — Wave 0 **شغّلته وأثبتت خضرته**
ولم تكتبه. أُضيف **١٩ اختبارًا** تثبّت قرارات Wave 0 نفسها.

### V2-0.9 · إثبات حالة Production ⏳
أكّدتَ تطبيق البنود الأمنية المعلّقة، فلم أعد أطلب إثباتها.
`docs/GATE_A_VERIFICATION_PACK.md` يبقى مرجعًا للتحقّق الدوري.

---

## ٤. ما لم يُنفَّذ — والسبب

| البند | السبب |
|---|---|
| تثبيت `@sentry/nextjs` | انحراف G9 مُعلَن — G12 + خطر كسر البناء. الوصلة جاهزة والخطوات موثَّقة |
| مراقب التوفّر | خدمة خارجية — تحتاج حسابك |
| مشروع Supabase للـPreview | يحتاج لوحتي Supabase وVercel — لا صلاحية لي |
| تشغيل أي SQL | 🔒 G2 — ملفات فقط. `consent_capture_EXTENSION_RUNME.sql` بانتظارك |
| SPF/DKIM/DMARC فعليًا | تُنفَّذ في لوحة DNS |
| أسرار النسخ الاحتياطي | GitHub Secrets — تحتاج حسابك |
| حدّ معدّل لمسار WhatsApp | 🔒 G7 مجمَّد — يحتاج قرارك |
| نقل `SHEETS_ENDPOINT` لمتغيّر بيئة | Wave 1 · V2-1.6 — يمسّ مسار إرسال حيّ |
| نسخ احتياطي لملفات Storage | فجوة موثَّقة — تحتاج تصميمًا مستقلًا |

---

## ٥. Migrations بانتظار التطبيق اليدوي

| الملف | الحالة |
|---|---|
| `docs/consent_capture_EXTENSION_RUNME.sql` | ⏳ **لم يُشغَّل** |
| `docs/consent_capture_EXTENSION_POSTCHECK.sql` | ⏳ قراءة فقط |
| `docs/consent_capture_EXTENSION_ROLLBACK.sql` | ⏳ عند الحاجة |

**ما يفعله:** ٣ أعمدة امتداد على `public_intake` + دالّة إلحاق واحدة. ❌ لا جدول جديد ·
❌ لا `DROP` · ❌ لا مساس بـ`capture_public_intake` · معاملة واحدة · idempotent ·
**فحص ذاتي يرفض الالتزام** إن اختلّ أي شرط (بما فيه تغيّر توقيع دالّة الالتقاط).
**RLS:** لا سياسة جديدة — سياسة الصفّ القائمة تغطّي الأعمدة الجديدة تلقائيًا، والملف
**يفشل** إن كانت RLS معطّلة أو إن ملك `anon` صلاحية SELECT.

**أمر التشغيل:** Supabase → SQL Editor → الصق `RUNME` → ثم `POSTCHECK` وتأكّد من C-1…C-6.
**التراجع:** إطفاء الراية يكفي (بلا SQL). حذف الأعمدة **يُتلف موافقات حقيقية** ومحجوب
خلف اعتمادك الصريح.

---

## ٦. الأعلام

| العلم | الحالة | التفعيل |
|---|---|---|
| `NEXT_PUBLIC_CONSENT_CHECKBOX_ENABLED` | 🔴 **OFF** | **بعد** تشغيل الـRUNME |
| `SENTRY_DSN` | Missing | بعد تثبيت الحزمة |

السجلّ الكامل: [`docs/FEATURE_FLAG_REGISTRY.md`](../FEATURE_FLAG_REGISTRY.md).
🔒 **أعلام Zoho وWhatsApp والمساعد لم تُمَسّ** (G7) — اختبار يثبّت ذلك.

---

## ٧. انحرافات G9

| # | الانحراف | السبب |
|---|---|---|
| ١ | Sentry غير مثبَّت | G12 + خطر كسر البناء. الوصلة جاهزة |
| ٢ | `schedule` معلَّقة في workflow النسخ | نصّ v2.1 «workflow_dispatch حتى تُضبط الأسرار» + G8 |
| ٣ | `.env.example` وُسِّع لا أُعيد كتابته | الملف يحمل معرفة حرجة كان إتلافها خسارة |
| ٤ | V2-0.1-E مشطوب | `/quick-access` بلا نموذج |
| ٥ | V2-0.6-A تحوّل إلى تدقيق | النظام مبني ومطبَّق — بناء ثانٍ يخالف G13-6 |
| ٦ | `lib/flags.ts` لم يُنشأ | كان سيصبح مصدرًا ثالثًا (G13-5) |
| ٧ | إصلاح `secure-document` خارج نصّ البند | فجوة أمنية حقيقية وجدها التدقيق، والموجة موجة سلامة |

---

## ٨. ازدواجات مُنعت (G13)

| كان يمكن إنشاؤه | القائم |
|---|---|
| middleware حدّ معدّل | `lib/server/rateLimit.ts` + `public_rate_limits` + `rl_consume` |
| `lib/flags.ts` | ١٨ جدول إعدادات + ~٢٠ متغيّر بيئة |
| ترويسات أمان جديدة | `next.config.js` (HSTS فقط أُضيف) |
| `consent` كجدول مستقل | ٣ أعمدة امتداد على `public_intake` |
| معامل موافقة في `capture_public_intake` | دالّة إلحاق منفصلة |
| ناقل رصد ثانٍ | `pgRedact()` القائم يُعاد استخدامه |

---

## ٩. خطواتك اليدوية

| # | الإجراء | يفتح |
|---|---|---|
| ١ | 🔴 **أنشئ مشروع Supabase للـPreview واربطه بـVercel** ([`ENVIRONMENTS.md`](../ENVIRONMENTS.md) §٤) | رفع تجميد G3 + اختبار آمن |
| ٢ | شغّل `consent_capture_EXTENSION_RUNME.sql` ثم الـPOSTCHECK | تفعيل الموافقة |
| ٣ | فعّل `NEXT_PUBLIC_CONSENT_CHECKBOX_ENABLED=true` **بعد (٢)** | V2-0.1 حيّ |
| ٤ | أضف `SUPABASE_DB_URL` في GitHub Secrets ثم شغّل النسخ يدويًا | نسخ احتياطي فعلي |
| ٥ | **نفّذ تمرين استعادة واحدًا على Preview وسجّل الزمن** | إثبات RTO ≤ ٦٠ د |
| ٦ | `npm i @sentry/nextjs` + الضبط + `SENTRY_DSN` | رصد فعلي |
| ٧ | احصر مرسِلي البريد ثم SPF → DKIM → DMARC `p=none` | وصولية البريد |
| ٨ | مراقب توفّر مجاني | كشف الانقطاع |
| ٩ | **قرار:** حدّ معدّل لمسار WhatsApp العام (G7 مجمَّد) | سدّ الفجوة الثانية |

---

## ١٠. المقترح التالي

**⛔ Wave 1 لم تبدأ.** وقبل اعتمادها، بندان من Wave 0 يستحقان الإغلاق:

1. 🔴 **بيئة Preview** (الخطوة ١). بدونها تبقى «الاختبارات على Preview حصرًا» غير
   قابلة للتحقيق، ويبقى تجميد الدفع قائمًا.
2. **تمرين الاستعادة** (الخطوة ٥). **نسخة لم تُستعَد مرة واحدة ليست نسخة احتياطية.**

وWave 1 نفسها تحمل قرارات تنتظرك: رقم سنوات الخبرة · أسماء «إعلان قصير» الثلاثة ·
عدّادات الفئات (٥٦ مقابل ٤٦ بالتصميم) · `LEADS_WEBHOOK_URL`.
