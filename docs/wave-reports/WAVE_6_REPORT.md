# WAVE 6 — تقرير · **DEVELOPMENT COMPLETE / RELEASE VERIFICATION PENDING**

> ✅ **البنود الستّة عشر كلّها مصنَّفة، ولا بند مجهول الحالة.** الوسم
> `overnight-wave-6-complete`، و`overnight-wave-6-partial` **باقٍ** يؤرّخ الحالة
> الوسطى.
>
> ⛔ **RELEASE VERIFICATION PENDING:** ثلاث حزم SQL لم تُشغَّل، وثلاثة أعلام
> مطفأة، ولا شيء نُشر.

الفرع `feat/wave-6-assets-archive-compliance-knowledge` · ⛔ لا Push · لا SQL
شُغِّل · لا Deploy · لا اتصال بـProduction · لا رفع ملفّات · لا حذف.

---

## ١. البنود الستّة عشر — كلّها مصنَّفة

| ID | المتطلب | القرار | الحالة |
|---|---|---|---|
| **V2-6.1-A** | QR → صفحة حالة | `/e/<token>` **تشترط تسجيل الدخول** وفق العقد (§٢) | ✅ **PASS** |
| V2-6.1-B | دفتر استخدام | مُشغِّلات قائمة | ✅ **CANCELLED — ALREADY BUILT** |
| V2-6.1-C | جدول صيانة + تنبيهات | `custody_inventory_maintenance` + cron | ✅ **CANCELLED — ALREADY BUILT** |
| **V2-6.1-D** | حقول سجلّ الأصول | الفجوة الوحيدة: `asset_insurance_coverage` | ✅ **PASS** |
| **V2-6.2-A/B** | أرشيف الوسائط الفيزيائية | `archive_media` + `archive_project_links` + احتفاظ W6-1 + إدخال | ✅ **PASS** |
| **V2-6.3-A** | تراخيص الموسيقى | `music_licenses` + ربط بالمشروع + إدخال | ✅ **PASS** |
| **V2-6.3-B** | ملخّص حقوق قابل للطباعة | `project_rights_summary` + شاشة طباعة — ⛔ بلا أسماء | ✅ **PASS** |
| **V2-6.4-A** | سجلّ HSE | **عرض مشتقّ** يوحّد ثلاثة سجلّات — ❌ لا رابع | ✅ **PASS** |
| **V2-6.5-A** | إقرارات الظهور | `model_releases` بحدّ PDPL الأدنى + سحب الإذن + إدخال | ✅ **PASS** |
| **V2-6.6-A** | وثيقة الإجراء | ❌ **لا جدول `sops`** — قاعدة المعرفة قائمة (§٣) | ✅ **PASS** |
| **V2-6.6-B** | خطوات + بذور | `sop_items` + ثلاث بذور **مسوّدة** محروسة | ✅ **PASS** |
| **V2-6.6-C** | إرفاق بقائمة إلزامية | `sop_attach_to_task` — يُلحق ولا يمحو، ويرفض غير المعتمَد | ✅ **PASS** |
| V2-6.7-A | Postmortems | `project_post_reviews` · `project_lessons_learned` | ✅ **CANCELLED — ALREADY BUILT** |
| **V2-6.8-A** | مولّد دراسات الحالة (بقالب، بلا AI) | `cs_generate_draft` **داخل `cs_*`** — مسوّدة فقط (§٤) | ✅ **PASS** |
| **V2-6.8-B** | طابور المسوّدات | **حالة داخل `cs_*`** — ⛔ لا جدول `portfolio_drafts` | ✅ **PASS** |
| **V2-6.8-C** | 🔴 قيد النشر الصلب | سكربت تطوير/CI يرفض بيئة النشر صراحةً (§٤) | ✅ **PASS** |

**لا بند «NOT IMPLEMENTED».** وما يبقى معلَّقًا قرارات لا كود (§٧).

---

## ٢. V2-6.1-A — الصفحة **ليست عامّة**، وهذا هو العقد

`QR_SECURITY_CONTRACT.md` §٥ صريح:

> «لا بحث مجهول الهوية في V1. الاختيار متعمّد: بحث عامّ ولو بحمولة فقيرة يعطي
> مجهولًا القدرة على **تأكيد أنّ رمزًا ما حقيقيّ**.»

والـBrief يقول «تخضع لـ`QR_SECURITY_CONTRACT.md`» — فالعقد حاكم.

| | السلوك |
|---|---|
| زائر بلا جلسة | **رسالة محايدة واحدة** لا تنفي ولا تؤكّد وجود الرمز |
| موظّف بلا صلاحية أصول | «لا تملك صلاحية» |
| موظّف | حمولة متدرّجة يقرّرها **الخادم** |

والصفحة تمرّ عبر `custody_inv_qr_scan` **وحدها** — `..._public_payload` مسحوبة
من الجميع، فتجاوزها يعني تحديد معدّل وتدقيقًا قابلَين للتجاوز. والحقول **قائمة
بيضاء صريحة**، فتغيّر لاحق في الخادم لا يُسرّب سعرًا أو موظّفًا أو مسار تخزين
إلى ورقة معلّقة على معدّة.

---

## ٣. 🔴 انحراف موثَّق: V2-6.6-A ليس جدولًا جديدًا

الـBrief يطلب `sops` 🆕. والفحص أثبت أنّ قاعدة المعرفة **قائمة بالكامل**:

| ما يلزم إجراءً | في `ai_knowledge_sources` |
|---|---|
| نوع «إجراء تشغيليّ» | ✅ `source_type` يقبل `'operations_procedure'` **أصلًا** |
| مسوّدة/مراجعة/اعتماد/رفض/انتهاء/أرشفة | ✅ `status` بالقيم الستّ |
| صلاحيات | ✅ `allowed_roles text[]` + `sensitivity` |
| إصدارات | ✅ `ai_source_revisions` |
| مسار اعتماد | ✅ `submitted_by`/`approved_by`/`rejected_reason` |
| مرفقات | ✅ `storage_bucket`/`storage_path` |

⇒ `sops` كان سيصير **قاعدة معرفة ثانية**. الجديد الوحيد خطواته المرتَّبة
(`sop_items`)، وهي لا توجد في أيّ جدول.

---

## ٤. ما يرفضه هذا البناء

| القاعدة | كيف فُرضت |
|---|---|
| لا سجلّ حوادث رابع | HSE **عرض** يقرأ الثلاثة ولا يكتب (N-2 + فحص طفريّ) |
| لا نظام أصول/عهدة/صيانة ثانٍ | (N-1) يمنع ١٤ اسمًا، ويمنع `alter`/`delete` على ستّة أنظمة قائمة |
| الأرشفة ليست حذفًا | `link_status` يشمل **`missing`** — الفقد يظهر ولا يُخفى |
| صحّة الوسيط تُدخَل لا تُستنتَج | `health_status` افتراضه `unknown` |
| PDPL — أقلّ بيانات | لا هويّة ولا عنوان ولا ميلاد (فحص طفريّ يمنع إضافتها) |
| لا رابط تخزين مخزَّن | `bucket`+`path` + قيد يرفض `^https?://` في حقل المسار |
| حقّ السحب | `withdrawn_at` + قيد يوجب وقتًا |
| ملخّص الحقوق لا يطبع اسمًا | أعداد وحالات فقط (فحص طفريّ يمنع طباعة الأسماء) |
| مسوّدة لا تُفرَض على طاقم | `sop_attach_to_task` يشترط `approved` |
| لا ادّعاء امتثال | (G-3) يمنع ISO/certified/SLA/ضمان |

---

## ٥. الفحوصات

| الفحص | قبل | بعد |
|---|---|---|
| `npm test` | 3947/3947 | **3964/3964** ✅ (+١٧) |
| `npm run typecheck` | exit 0 | **exit 0** ✅ |
| `npm run build` | exit 0 | **exit 0** ✅ |
| `npm run lint` | 42 / 0 | **42 / 0** ✅ مطابق لخطّ الأساس |

**تجميد منصّة المشاريع باقٍ:** لم يُمَسّ ملفّ منها؛ الواجهة في مسار مستقلّ
`/client-portal/registers` كما في Wave 5 (W5-4 لم يُرفَع).

---

## ٤. V2-6.8 — داخل المنصّة القائمة، وبقيد النشر الصلب

`cs_case_studies` تحمل بالفعل دورة عشر حالات (`draft` → `internal_review` →
`legal_review` → إذن العميل → `approved` → `scheduled` → `published` →
`unpublished` → `archived`) مع `cs_submit`/`cs_review_decide`/`cs_legal_decide`/
`cs_approve`/`cs_guard_publish` ونسخًا غير قابلة للتعديل وسجلّ تدقيق وصلاحيات.

⇒ **لا محرّك حالات ثانٍ ولا نظام موافقات ثانٍ.** الجديد ثلاثة أشياء:

| الجديد | لماذا |
|---|---|
| `source_provenance` + `generation_method` | من أين جاءت المسوّدة — **مفاتيح لا قيم**، فلا يتسرّب اسم ولا رقم |
| `cs_generate_draft` | يُنشئ **مسوّدة** فقط. كلّ حقل عامّ يبقى **فارغًا** — القالب هيكل لا محتوى، ولا يُنسخ نصّ تسويقيّ ولا رقم ولا اسم عميل |
| `exported_at`/`exported_by` | أثر التصدير **حالة على الدراسة** — ⛔ لا جدول `portfolio_drafts` |

### 🔴 القيد الصلب V2-6.8-C

نظام ملفّات Vercel وقت التشغيل **للقراءة**، وما يُكتب فيه **يُفقد عند أوّل نشر**
— أي أنّ كتابة ملفّ محتوى من مسار خادميّ تبدو ناجحة ثمّ تختفي بصمت، وهو أسوأ من
الفشل الصريح.

| الحارس | أين |
|---|---|
| `assertNotDeployedRuntime` يرفض `VERCEL`/`VERCEL_ENV`/`NEXT_RUNTIME` | السكربت، ومُختبَر بثلاث بيئات |
| لا `writeFile`/`fs` في أيّ مسار تحت `app/api` | (C-6) يمسح كلّ الملفّات |
| لا `child_process` ولا `git push` ولا `gh pr` | (C-7) |
| اسم الملفّ مُعقَّم · `PATH_TRAVERSAL_BLOCKED` · لا استبدال صامت | (C-8) يُنفَّذ **فعليًّا في مجلّد مؤقّت** |
| التقنيع بقائمة بيضاء · التجهيل يحذف الاسم فعلًا | (C-9) |
| ⛔ لا تصدير قبل الاعتماد · و«معتمَدة» ≠ «منشورة» | (C-5) + فحص طفريّ |

---

## ٦. ما لم يُنجَز — بالاسم

| البند | الحالة |
|---|---|
| واجهة **تحرير** وثائق الإجراءات نفسها | 🟡 **خارج نطاق Wave 6.** القراءة والإرفاق مبنيّان، والتحرير يقع في قاعدة المعرفة القائمة (`ai_knowledge_sources`) التي لا واجهة لها في المستودع أصلًا — وبناء واجهة لها توسيعٌ لنطاق موجة أخرى |
| ربط وسيط أرشيف بمشروع من الواجهة | 🟡 الجدول والدوالّ والقراءة مبنيّة؛ الربط يقع اليوم بإدخال مباشر — بند تالٍ صغير |

⛔ **ولا بند من الـBrief بلا حالة.** ما سبق تحسينات لا متطلّبات.

---

## ٧. قرارات تنتظر خالد

| # | القرار | التصنيف |
|---|---|---|
| **W6-1** | مدد الاحتفاظ بالأرشيف الفيزيائيّ وسياسة الإتلاف | **PENDING KHALED / LEGAL REVIEW** — ⛔ لم يُثبَّت أيّ رقم بالتخمين |
| **W6-2** | اعتماد بذور الإجراءات الثلاث (اليوم `draft`، فلا تُرفَق بمهمّة) | PENDING CONTENT REVIEW |
| W6-3 | دلو خاصّ لإقرارات الظهور وإثباتات التراخيص قبل أيّ رفع | MANUAL PRODUCTION VERIFICATION |

⚠️ **لم تُنفَّذ تحقّقات متصفّح ولا E2E ولا axe** — ولا يُدّعى ذلك.
