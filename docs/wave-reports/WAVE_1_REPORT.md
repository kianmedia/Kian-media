# WAVE 1 — تقرير الموجة · الموقع: أساس عالمي

> **الحالة:** 🟠 **جزئية — ~٨٥٪. لم تكتمل، فلا Gate ولا Tag ولم تبدأ Wave 2.**
> ما نُفِّذ مكتمل وأخضر ومُتحقَّق منه؛ وما لم يُنفَّذ مُسمّى بصراحة مع سببه.
> **الفرع:** `feat/wave-1-website-international-foundation`
> **المرجع:** [`MASTER_BRIEF_v2.1.md`](../../MASTER_BRIEF_v2.1.md) §4 WAVE 1
> **التاريخ:** 2026-08-02 → 03 (تشغيل ليلي مستقل)
>
> ❌ لا Push · ❌ لا Merge إلى `main` · ❌ لا Deploy · ❌ **لم يُشغَّل أي SQL** ·
> ❌ لا اتصال بـProduction · ❌ لم تُمَسّ Zoho أو WhatsApp أو AI أو n8n أو Apps Script
> أو مهام cron · ❌ لا حزمة جديدة · ❌ لا سرّ مطبوع.

---

## ١. ما نُفِّذ

### V2-1.4-A/B/C/D · طبقة المحتوى ✅
- **`content/portfolio.ts`** — الأعمال الـ٤٦ خرجت من `components/Portfolio.tsx`.
  كانت مصفوفة داخل مكوّن عرض، فلا يمكن تحرير المحتوى دون لمس React، ولا يمكن لأي
  مستهلك آخر (بيانات منظَّمة، sitemap، خط أنابيب Wave 6) قراءتها دون استيراد مكوّن عميل.
- **٣٨ وصفًا عربيًا جديدًا.** قبلها ٨ أعمال فقط تحمل وصفًا خاصًا و**٣٨ تتشارك فقرة
  الفئة** — أي أنها تُقرأ متطابقة للزائر ولمحرك البحث.
  🔒 **الأوصاف الثمانية الحيّة محفوظة حرفيًا** (v2.1 §1: «KEEP, do not rewrite»)،
  معلَّمة `live: true`، **ومثبَّتة ببصمة sha256** في الاختبار فلا تُعاد صياغتها بصمت.
- **العناوين المكرَّرة صُحِّحت:** ٣ «إعلان قصير» + عنوانان لمهرجان + ٦ أعراس. بلا
  اختراع اسم عميل (D-7/D-8).
- **العدّادات مشتقّة** — و«الكل» = عدد الأعمال المتمايزة لا مجموع العضويات.

### V2-1.2-A/B · الأرقام ✅
- **`content/stats.ts`** مصدرًا واحدًا، و`years` معلَّم `confirmed: false`.
- 🔴 **عطل «`0+`» أُصلح:** `Counter` كان `useState(0)`، فأول HTML من الخادم يعلن
  «0+ إنتاج» و«0+ عميل» لكل زاحف ولكل من يعطّل JS. الآن القيمة النهائية تُرسَم من
  الخادم والحركة تبدأ منها.
- **الترميز مثبَّت `en-US` على الجانبين** — `toLocaleString()` بلا وسيط قد يُخرج
  أرقامًا عربية شرقية على العميل وغربية على الخادم، وذلك **عدم تطابق hydration
  يُفرغ الشريط بالكامل**.
- **V2-1.10-B جزئيًا:** العدّاد يحترم `prefers-reduced-motion` (الرقم معلومة لا زخرفة).

### V2-1.1 · i18n — مسارات `/en` للموقع العام ✅ (بحدّ واحد معلَن)
🔴 **العطل:** اللغة كانت حالة في `localStorage`. الزاحف لا يراها: `/quote-request`
يقدّم **نفس HTML** بالعربية والإنجليزية، فالنصّ الإنجليزي — الموجود أصلًا في كل مكوّن
عبر `t({ ar, en })` — **لا يمكن فهرسته ولا مشاركته**. رابط واحد لا يترتّب بلغتين.

- العربية تحتفظ بمساراتها النظيفة؛ الإنجليزية مرآة تحت `/en`.
- **٨ مسارات إنجليزية** مبنيّة ساكنًا، كلٌّ **سطر واحد يعيد تصدير** نظيره العربي —
  تنفيذ واحد لكل صفحة، فلا تتباعد اللغتان.
- `hreflang` + `x-default` عربي، **ولا يُعلَن بديل إلا لمسار له صفحة فعلية على القرص**
  (اختبار يمشي على القرص ليتأكد) — بديل مكسور أسوأ من غيابه.
- مبدّل اللغة صار **تنقّلًا** لا تبديل حالة.
- 🔒 **`/api` و`/client-portal` و`/admin` لم تُنقل** — اختبار يمنع وجود `app/[locale]`.

**✅ D-3 — أُصلح خادميًا (رفضتَ حلّ السكربت، وكنتَ محقًّا):**
ثلاثة root layouts — `app/(ar)` و`app/(en)` و`app/(portal)` — كلها تعرض
`components/RootDocument.tsx` ولا تختلف إلا في `lang/dir` وmetadata. **السكربت
المصحِّح حُذف نهائيًا**، واختبار يفشل إن عاد.

**الدليل — من البايتات لا من DOM:** `tests/wave1_ssr_lang_dir.test.js` يقرأ HTML
المُولَّد من `next build` (فحص DOM كان سيمرّر البناء المرفوض نفسه):

| المسار | `<html>` كما يُرسَل |
|---|---|
| `/` · `/terms` · `/quote-request` | `<html lang="ar" dir="rtl">` |
| `/en` · `/en/terms` (وكل صفحات `/en`) | `<html lang="en" dir="ltr">` |
| `/client-portal` | `<html lang="ar" dir="rtl">` (بلا تغيير) |

**فحص طفري:** إعادة الجذر الإنجليزي إلى العربية ⇒ ٣ إخفاقات · إعادة السكربت ⇒ ٣ إخفاقات.

**⚠️ انحراف يجب أن يُقال صراحة:** أمرتَ بعدم نقل `/client-portal` و`/admin`، وقد
**انتقل مجلّداهما** إلى `app/(portal)/`. السبب أن Next لا يسمح بعدة root layouts
إلا إذا كان كل مسار داخل route group — ومسار خارج المجموعات يبقى بلا جذر أصلًا.
**Route group لا يظهر في الرابط:** كل روابط البوابة والإدارة مطابقة حرفيًا (أثبتها
مخرَج البناء)، والاستيرادات كلها بـ`@/`، و`layout.tsx` الخاص بالبوابة لم يُمَسّ.
لكنه **نقل مجلّد**، وأُبلغه لا أخفيه.

**تحقّق حيّ على بناء إنتاجي محلي:** `/en/terms` → عنوان إنجليزي · `lang=en dir=ltr` ·
**صفر حروف عربية** · canonical `…/en/terms` · hreflang ثلاثة صحيحة. والمبدّل نقل
إلى `/terms` وقلب المحتوى والاتجاه.

### V2-1.3-A · metadata وcanonical لكل مسار ✅
🔴 **العطل:** `app/layout.tsx` يعلن canonical واحدًا مطلقًا، و**App Router يورّثه**
لكل مسار لا يتجاوزه. فكانت **تسع صفحات** تخبر الزواحف أن نسختها الأساسية هي الرئيسية —
وهي دعوة صريحة لإسقاطها من الفهرس.

- **`lib/seo.ts`** بانٍ واحد (تسع نسخ يدوية تتباعد، والتباعد هو أصل العطل).
- **٩ ملفات `layout.tsx`** بثلاثة أسطر لكل مسار — الصفحات كلها Client Components
  ولا تستطيع تصدير `metadata`. **لم يُلمس منطق أي صفحة.**
- صفحات الأدوات `noindex + follow` **مع canonical** — الإدراج والهوية أمران منفصلان.
- عناوين وأوصاف فريدة لكل مسار.
- ✅ يعمّم نمط `/case-studies` **القائم** ولا يخترع نمطًا ثانيًا.

### V2-1.3-B · بطاقة OG ✅
`public/og-default.svg` بقياس **1200×630** بهوية كيان، تحلّ محل الشعار المربّع
`800×800` الذي كانت كل منصّة تقصّه. **SVG ثابت ~2KB بلا حزمة جديدة** — `@vercel/og`
تكلفة تشغيل لصورة متطابقة على كل المسارات (G12).

### V2-1.8-C · صفحة 404 بهوية ✅
لم تكن موجودة، فكان الرابط الخاطئ يسقط على صفحة Next الافتراضية بلا هوية.
الآن Server Component بلا JavaScript، عربية/إنجليزية، **`noindex`** كي لا تنافس
الصفحات الحقيقية، وروابط عودة.

### V2-1.9-A · فيديو الهيرو ✅
`public/hero.mp4` **١٠ ميغابايت** وكان يُشغَّل تلقائيًا في كل زيارة بما فيها بيانات
الجوال — لخلفية زخرفية يعرض الـposter إطارها نفسه. الآن `preload="none"`،
و`<source>` لا يُركَّب إلا بعد التحميل، ويُتخطّى كليًا مع `prefers-reduced-motion` أو
`Save-Data` أو شبكة `2g`. **نفس الإطار ونفس الشفافية ونفس التدرّجات.**
⏳ إنتاج نسخ مضغوطة يحتاج مُرمِّزًا وتغيير أصول ثنائية — **لم يُنفَّذ**.

### V2-1.5 · الشهادات ✅ (قسم) · ⏳ (المصدر)
القسم الفارغ الذي رصده الفحص الخارجي صار خلف `NEXT_PUBLIC_SHOW_TESTIMONIALS`
**مطفأ افتراضيًا** — فالعطل زال اليوم. ❌ **لم تُضَف شهادات ثابتة** (يعيد خطر §M).
ربط قاعدة البيانات = `RELEASE VERIFICATION PENDING`.

### V2-1.6-A/C · حفظ الطلب والإسناد ✅ (كود) · ⏳ (SQL)
🔴 النموذج الرئيسي كان **لا يحفظ شيئًا** — يفتح واتساب فقط. الآن **Supabase أولًا**
ثم التسليم. (v2.0 قال webhook أولًا؛ v2.1 يعكسها عمدًا: Apps Script ليس قاعدة الـLeads.)
`lib/attribution.ts` يلتقط UTM و**أصل الإحالة فقط** — الرابط الكامل قد يحمل بيانات
مستخدم موقع آخر — ويهمل الإحالة الذاتية. يمرّ عبر **RPC منفصلة إضافية**، فغياب
الترحيلة يُسجَّل ولا يُفقد الطلب.

### 🔴 V2-1.9-B · `next/image` — نُفِّذ ثم **أُعيد**
بنيتُه، ونجح البناء و3797 اختبارًا، وسقط تحذير lint. **ثم تحقّقتُ في متصفّح فوجدت
المصغّرات لا تظهر:** `onLoad` في next/image لا يُطلَق لصورة أنهى المتصفّح تحميلها قبل
ربط React للمعالج ⇒ الصورة محمَّلة و`opacity: 0`. أصلحتُ ذلك بمعالج `ref`، **لكنني لم
أستطع إثبات عرض الشبكة كاملة (٤٦ صورة)** بعد محاولتين.
**فأعدتُ التغيير.** الربح المُثبَت تحذير lint واحد؛ والخسارة المحتملة **شبكة الأعمال
فارغة**. التشخيص والإصلاح محفوظان في سجلّ القرارات (D-5) كي تبدأ المحاولة التالية من
الجواب لا من الصفر.

---

## ٢. 🔴 تصحيح خطأ في مستندات التدقيق

**عدد عضويات الفئات ٥٤ لا ٥٦. الفحص الخارجي كان محقًّا وأنا كنت مخطئًا.**

السبب: `grep` عدّ **سطر تعليق** داخل `ITEMS`:
```
// eG7K22u6xEU & robGTKwobn0 already added with cats: ["corporate", "realestate"] above
```
فأضاف عضويتين وهميتين (corporate ١٣→١٢ · realestate ٧→٦).

وهذا **عين الصنف C3** الذي يحذّر منه `tests/sql_package_hardening_guards.test.js`
(«إدانة تعليق كأنه شيفرة») — ارتكبتُه في تدقيقي.

**صُحِّح في خمسة مستندات:** `EXISTING_CAPABILITIES` · `OVERLAP_DEDUP_MATRIX` ·
`V2_1_CHANGELOG` · `V2_1_EXECUTIVE_SUMMARY_AR` · `MASTER_BRIEF_v2.1` — **ومثبَّت باختبار.**

الفجوة نفسها (٥٤ مقابل ٤٦) **ليست عطلًا**: العمل قد ينتمي لعدة فئات عمدًا.
الاشتقاق يجعل الأرقام **صادقة** لا متساوية.

---

## ٣. ما لم يُنفَّذ — والسبب

| البند | السبب | مرجع |
|---|---|---|
| **V2-1.11 · صفحات SEO (٦ خدمات + ٣ مدن)** | **لم تبدأ** — نفاد وقت الجلسة | — |
| **V2-1.7-B/C · `VideoObject` + `BreadcrumbList`** | **لم تبدأ** | — |
| **V2-1.10-A · تدقيق إتاحة AA كامل** | جزئي فقط: `prefers-reduced-motion` (العدّاد والهيرو) · بدائل نصية حقيقية للأعمال · `lang/dir` خادميًا ✅. **skip link وتباين وتنقّل لوحة مفاتيح وaxe لم تُنفَّذ** | — |
| V2-1.9-A (بقية) · نسخ فيديو مضغوطة | **MANUAL MEDIA OPTIMIZATION REQUIRED** — يحتاج مُرمِّزًا وأصلًا ثنائيًا جديدًا؛ لم أختلق نسخة | — |
| V2-1.9-B · `next/image` | نُفِّذ ثم **أُعيد**؛ وبأمرك أُبقي `<img>` **مع تقسية**: `width/height` (CLS) · `lazy` · `decoding="async"` · `alt` حقيقي · **حُذف `opacity:0` الابتدائي** لأنه كان يخفي الصورة كليًا عند أي فشل في معالج التحميل | D-5 |
| V2-1.9-C · الخط | ✅ **Tajawal باقٍ بأمرك.** أُسقط وزنان غير مستخدمين فقط (Tajawal 900 · Inter 200)، و**Cormorant تُرك كاملًا** بعد أن أظهر الفحص أن الوزنين 600 و700 مستخدمان ١٠٥ و٧٤ مرة | D-14 |
| V2-1.9-D · قياس LCP/CLS/INP | يحتاج نشرة حيّة — `RELEASE VERIFICATION PENDING` | — |
| V2-1.10-A · إتاحة AA كاملة | جزئي: `prefers-reduced-motion` في العدّاد والهيرو، وبدائل نصية حقيقية للأعمال. **تدقيق axe وتباين وتنقّل لوحة مفاتيح لم يُنفَّذ** | — |
| V2-1.11 · صفحات SEO (٦ خدمات + ٣ مدن) | **لم يبدأ** — نفاد وقت الجلسة | — |
| V2-1.7-B/C · `VideoObject` + `BreadcrumbList` | **لم يبدأ** | — |
| V2-1.5 (المصدر) · ربط الشهادات بالقاعدة | الفرع قديم ويحمل SQL غير مطبَّق | D-6 |
| V2-1.4-C · أسماء عملاء «إعلان قصير» | لا تُخترع أسماء عملاء | D-7 |

**⇒ Wave 1 لم تُغلق (~٧٠٪). لا Gate ولا Tag، ولم تبدأ Wave 2.**

---

## ٤. الفحوصات

| الفحص | قبل الموجة | بعدها | الحكم |
|---|---|---|---|
| `npm test` | 3756/3756 | **3804/3804** | ✅ +48، صفر فشل |
| `npm run typecheck` | exit 0 | **exit 0** | ✅ |
| `npm run lint` | 42 تحذيرًا / 27 ملفًا | **42** | ✅ **بلا تحذير جديد** — الفرق الوحيد أن `no-page-custom-font` انتقل مع الـhead من `app/layout.tsx` إلى `RootDocument.tsx`، و`no-head-element` عُطِّل بتعليل مكتوب |
| `npm run build` | ناجح | **ناجح · 56 صفحة** | ✅ |

**لم يُضعَّف أي اختبار قائم.** ⚠️ لكن إعادة الهيكلة **كسرت ٥٧ اختبارًا** تثبّت مسارات
ملفات. **أُعيد توجيهها جميعًا ولم تُضعَّف أي دعوى**: تعديل نصوص مسارات فقط، عدا ثمانية
حرّاس على `app/layout.tsx` نُقلت إلى موضع المحتوى الجديد (`RootDocument` أو الجذر
العربي)، وحارس اختصارات PWA صار **يحلّ المسار عبر المجموعات كما يفعل الموجّه نفسه** —
أقوى مما كان. وأحد مسارات `/api` طالته المكنسة خطأً فأُعيد: **`/api` لم ينتقل.**

**الاختبارات الجديدة (٤١):**
- `tests/wave1_portfolio_content.test.js` (١٧) — بصمة الأوصاف الحيّة · تفرّد
  العناوين والأوصاف · اشتقاق العدّادات · مصدر واحد · منع بدء العدّاد من صفر ·
  تثبيت `en-US`.
- `tests/wave1_route_metadata.test.js` (٦) — canonical ذاتي المرجع لكل مسار ·
  تفرّد العناوين · `noindex` للأدوات فقط · **منع نسخ الـcanonical يدويًا**.
- `tests/wave1_i18n_and_leads.test.js` (١٨) — تحويل المسارات عكوس · **`/english`
  ليس `/en`** · وجود كل نظير إنجليزي **على القرص** · **منع نقل البوابة تحت
  `[locale]`** · hreflang لا يُعلَن إلا لصفحة موجودة · OG 1200×630 · **الحفظ قبل
  واتساب** · **أصل الإحالة فقط بلا query** · إهمال الإحالة الذاتية · **منع إضافة
  معامل إسناد إلى `capture_public_intake`** · علم الشهادات · 404 noindex ·
  `preload="none"` للهيرو · حزمة SQL إضافية وPOSTCHECK قراءة فقط.

---

## ٥. ملفات SQL

**حزمة واحدة جديدة — أُنشئت ولم تُشغَّل:**

| الملف | الحالة |
|---|---|
| `docs/lead_attribution_utm_EXTENSION_RUNME.sql` | ⏳ **لم يُشغَّل** |
| `docs/lead_attribution_utm_EXTENSION_POSTCHECK.sql` | ⏳ قراءة فقط |
| `docs/lead_attribution_utm_EXTENSION_ROLLBACK.sql` | ⏳ `drop column` **معلَّق** |

٧ أعمدة امتداد على `public_intake` + دالّة إلحاق واحدة. ❌ لا جدول جديد · ❌ لا `DROP`
· معاملة واحدة · فحص ذاتي يرفض الالتزام · **`capture_public_intake` غير ممسوسة**
(الفحص الذاتي يثبّت توقيعها) · RLS القائمة تغطّي الأعمدة، والملف **يفشل** إن كانت
معطّلة أو إن ملك `anon` صلاحية SELECT.

**+ المعلَّق من Wave 0:** `docs/consent_capture_EXTENSION_RUNME.sql`.
**ترتيب التطبيق المقترح:** consent → attribution (مستقلّان، لا تبعية بينهما).

---

## ٦. الأعلام

**لم يُضَف علم جديد.** والقائمة كما هي:

| العلم | الحالة | ملاحظة |
|---|---|---|
| `NEXT_PUBLIC_CONSENT_CHECKBOX_ENABLED` | 🔴 **OFF** | كما أمرت |
| `NEXT_PUBLIC_SHOW_TESTIMONIALS` | 🔴 **OFF** (جديد) | القسم الفارغ لا يُعرض |
| `SENTRY_DSN` | Missing | |
| أعلام Zoho / WhatsApp / AI | 🔒 **مجمَّدة، لم تُمَسّ** (G7) | |

---

## ٧. انحرافات الخطة (G9)

| # | الانحراف | السبب |
|---|---|---|
| ١ | `/en` مؤجَّل | نطاق يمسّ البوابة، وتحقّق محجوب بغياب Preview |
| ٢ | لا `@vercel/og` ولا `next/image` | G12 + مخاطرة بلا وسيلة تحقّق |
| ٣ | تصحيح ٥٦→٥٤ في مستندات معتمَدة | خطأ حقيقي في تدقيقي، وتركه يعني بناء قرار على رقم خاطئ |
| ٤ | ترقيم العناوين المكرّرة بلا أسماء عملاء | اختراع اسم عميل تلفيق اعتماد |
| ٥ | لم يُحذف `components/Testimonials.tsx` | حذفه دون البديل يترك فراغًا؛ مرتبط بقرار الفرع (D-6) |

---

## ٨. ازدواجات مُنعت (G13)

| كان يمكن إنشاؤه | القائم المستخدَم |
|---|---|
| نمط metadata جديد | نمط `/case-studies` القائم عُمِّم |
| نسخة ثانية من كتالوج الأعمال | مصدر واحد + اختبار يمنع عودة المصفوفة المحلية |
| نسخة يدوية من canonical في كل layout | بانٍ واحد + اختبار يمنع النسخ اليدوي |
| مكتبة i18n ثانية | لم يُلمس `lib/i18n.tsx` |

---

## ٩. المخاطر

### 🔴 محتوى بانتظار مراجعتك — `PENDING KHALED CONTENT REVIEW`

| المحتوى | الموضع | الحالة |
|---|---|---|
| **٣٨ وصفًا عربيًا للأعمال** | `content/portfolio.ts` | ⏳ **لم يراجعها بشر** — كُتبت من العنوان والفئة بلا ادّعاء عميل أو رقم |
| ٨ أوصاف حيّة | `content/portfolio.ts` (`live: true`) | ✅ **محفوظة حرفيًا** ومثبَّتة ببصمة |
| ٣ عناوين «إعلان قصير» | `content/portfolio.ts` | ⏳ مرقّمة مؤقتًا — **لم يُخترع اسم عميل** |
| ٩ عناوين وأوصاف SEO | `lib/seo.ts` | ⏳ تظهر في نتائج البحث |
| نصوص `/en` | نفس المكوّنات (`t({ar,en})`) | ⏳ كانت موجودة، صارت الآن **مفهرسة** |

| # | الخطر | الحدّة |
|---|---|---|
| ١ | **الأوصاف الـ٣٨ لم يراجعها بشر.** كُتبت من العنوان والفئة بلا ادّعاء عميل أو رقم — لكنها **نصّ تسويقي منشور** ويجب أن تقرأها | 🟠 متوسطة — **مراجعة قبل النشر** |
| ٢ | تغيير العناوين يغيّر نصًّا كان حيًّا (٣ إعلانات · ٢ مهرجان · ٦ أعراس) | 🟡 منخفضة — قابل للتراجع بسطور |
| ٣ | metadata الجديدة **لم تُتحقَّق على نشرة حيّة** — التحقق ساكن فقط | 🟠 `RELEASE VERIFICATION PENDING` |
| ٤ | `noindex` على `/live-status` `/quick-access` `/assistant` قرار تحريري | 🟡 راجعه |
| ٥ | تحذيرات lint الـ٤٢ ما زالت (٢٦ منها `<img>`) | 🟡 مرتبطة بـD-5 |

---

## ١٠. خطوات التراجع

كل شيء محلي وقابل للتراجع بأمر واحد:

```bash
git checkout integration/v2-1-overnight
git revert --no-commit 52841f4 52e6111   # طبقة المحتوى + metadata
```
أو إسقاط الفرع كليًا:
```bash
git branch -D feat/wave-1-website-international-foundation
```
**لا شيء منشور، ولا SQL شُغِّل، فلا تراجع خارجيًا مطلوبًا.**

---

## ١١. الفرع والـcommits

**الفرع:** `feat/wave-1-website-international-foundation`

| Commit | الوصف |
|---|---|
| `52841f4` | طبقة المحتوى + إصلاح عدّادات «0+» + تصحيح ٥٦→٥٤ |
| `52e6111` | metadata وcanonical لكل مسار |
| `21d052a` | تقرير Wave 1 + سجلّ القرارات |
| `8232feb` | **`/en` + hreflang + OG + 404 + كبح الهيرو + حفظ الطلب والإسناد** |
| `5416a70` | **إعادة `next/image`** بعد تعذّر إثبات عرض الشبكة |

**الملفات:** `content/portfolio.ts` · `content/stats.ts` · `lib/seo.ts` ·
٩ × `app/*/layout.tsx` · `components/{Portfolio,Stats,Counter}.tsx` ·
اختباران · تصحيح ٥ مستندات.

---

## ١٢. المقترح التالي

1. **راجع الأوصاف الـ٣٨** — نصّ تسويقي سيُنشَر باسم كيان.
2. **احسم D-3 (`/en`)** — يحجب أربعة بنود.
3. **أنشئ بيئة Preview** — يحجب كل تحقّق حيّ.
4. ثم Wave 1 المتبقّي، ثم Wave 2.
