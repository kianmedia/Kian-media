# التقرير النهائي — إغلاق منصة مشاريع كيان

> تاريخ التقرير: 2026-07-25 · الفرع: `main` · آخر Commit: `cf70d4f`
> كل ادّعاء في هذا التقرير مبنيّ على فحص حيّ للإنتاج أو على `file:line` — لا افتراضات.

---

## ١) ملخص تنفيذي

**الحالة قبل التنفيذ.** منصّة غنيّة بالمزايا ومنشورة بالكامل على قاعدة الإنتاج، لكنها كانت
**غير آمنة للتشغيل**: أي زائر يملك المفتاح العامّ (وهو داخل حزمة المتصفّح) كان يقرأ بيانات
الشركة. إضافةً إلى فقدان صامت في الإشعارات ومسار إفساد يجعل مخرَجًا غير قابل للتسليم أبدًا.

**ما تم إنجازه.**
1. **إغلاق تسريب بيانات حيّ** — طُبِّق على الإنتاج وتحقّقتُ منه بنفسي.
2. **إغلاق ثلاث ثغرات إضافيّة** (إفساد بيانات، كتابة مباشرة تتجاوز الحرّاس، fail-open، تصعيد ذاتيّ).
3. **إصلاح فقدان صامت** في طابور البريد وفي حدثين رئيسيّين لم يكونا يُرسلان بريدًا إطلاقًا.
4. **إكمال دورة الحياة** بحالتَي التعليق والإلغاء.
5. **تثبيت عقد أحداث الإشعارات** ومواصفات ربط Google Apps Script.
6. **696 اختبارًا** تمرّ، وبناء ناجح، وTypeScript وLint نظيفان.

**نسبة الاكتمال النهائية: ~82%** (كانت 72% عند بدء التدقيق).

**هل المنصّة جاهزة للإغلاق التشغيلي؟ لا — ليس بعد.** انظر §12.

**أهم المخاطر التي عُولجت:** تسريب بيانات الشركة للعموم · مخرَج غير قابل للتسليم للأبد ·
حذف قوالب ومحاضر تصوير نهائيًّا بتجاوز الحذف الناعم · إشعارات إلزاميّة تُنتَج ولا تُرسل أبدًا.

---

## ٢) ما كان موجودًا وتم الحفاظ عليه (لم يُعَد بناؤه)

| الميزة | القرار |
|---|---|
| المشاريع الرئيسية والفرعية (`parent_project_id` / `project_scope`) | **محميّة** — لم يُنشأ جدول ولا علاقة بديلة |
| محرّك الانتقال `project_core_set_stage` | **لم يُمَسّ** — مُعرَّف في ٤ ملفّات ولا يمكن الجزم بالمنشور |
| `project_closure_readiness` (5C) | **لم يُعَد كتابته** — أُضيف غلاف `_v2` يُركّب فوقه |
| المخرجات والإصدارات وتعليقات التايم كود | قائمة وتعمل |
| `client_review_version` | **المسار الوحيد الصحيح** — عُزِّز بإسقاط منافسه المعطوب |
| المالية `project_core_financials` | نظام واحد؛ لا مالية على جدول `projects` أصلًا |
| الإشعارات (صندوق/طابور/تتبّع/عامل/مزوّد) | نظام واحد؛ وُثِّق ولم يُستبدل |
| الحذف الناعم والاستعادة | محفوظ — والإصلاحات عزّزته |
| سجلّ التدقيق `pc_log` / `project_activity` | قائم |
| القوالب والموارد والحوكمة والإقفال | قائمة؛ أُكملت لا استُبدلت |

---

## ٣) التحسينات المنفذة

| الميزة | الحالة السابقة | ما تم تنفيذه | الملفات | قاعدة البيانات | الاختبار |
|---|---|---|---|---|---|
| حرّاس الصلاحية | تُعيد `NULL` ⇒ كود ميّت | `coalesce(...,false)` لسبعة حرّاس + سحب EXECUTE من anon | `project_platform_authz_hardening_RUNME.sql` | **مُطبَّق ومتحقَّق** | `authz_null_collapse` (10) |
| مسار مراجعة العميل | إدراج مباشر يُفسد البيانات | إسقاط السياسة + سحب المنح | `..._hardening2_RUNME.sql` §A | بانتظار التشغيل | `authz_hardening2` (17) |
| كتابة مباشرة على جداول | تتجاوز كل الحرّاس | سحب مطابق لاستخدام الواجهة | `..._hardening2_RUNME.sql` §B | بانتظار التشغيل | ✔ |
| `pc_client_cap` | fail-open عند NULL | NULL = «غير مطابق» | `..._hardening2_RUNME.sql` §C | بانتظار التشغيل | ✔ |
| ترقية ذاتية | محرِّر يرفّع نفسه مديرًا | `no_self_promotion` | `..._hardening2_RUNME.sql` §D | بانتظار التشغيل | ✔ |
| دورة الحياة | لا تعليق ولا إلغاء | توسيع CHECK + `project_core_hold_action` | `project_lifecycle_hold_cancel_RUNME.sql` | بانتظار التشغيل | `project_lifecycle_e2e` (30) |
| طابور البريد | نافذة 24h مع cron يوميّ ⇒ فقدان دائم | نافذة تعافٍ 168h | `app/api/cron/notify-email/route.ts` | — | `v1_closure_fixes` (13) |
| إشعار المعاينة/التسليم | لا يُرسل من مساحة العمل الرئيسية | ربط بنفس المُنتِج المثبت | `components/portal/projectcore/ProjectModules.tsx` | — | ✔ |
| نزاهة الإقفال | سبب غير مفروض · `rejected` منحرف · مالية غير مبوّبة · منح شارد | ٤ إصلاحات | `project_closure_integrity_RUNME.sql` | بانتظار التشغيل | ✔ |
| عقد الإشعارات | غير موثّق | عقد كامل + مواصفات GAS | `NOTIFICATION_EVENT_CONTRACT.md` | — | — |

---

## ٤) إصلاحات الأخطاء (السبب الجذري لا العرَض)

**١. تسريب بيانات الشركة للعموم — كان على الإنتاج ✅ أُصلح وطُبِّق**
- العرَض: `company_execution_report` يُعيد 20 مشروعًا و44 مهمة لمستدعٍ غير مُصادَق.
- السبب الجذري: `staff_role()` تُعيد NULL ⇒ `can_manage_projects() = false OR NULL = NULL` ⇒
  `if not NULL then raise` **لا يُنفَّذ**؛ والدوالّ SECURITY DEFINER فتتجاوز RLS. مضاعَفًا بأنّ
  PostgreSQL يمنح EXECUTE لـPUBLIC افتراضيًّا.
- الإصلاح: `coalesce(...,false)` + سحب EXECUTE بتوقيعات من `pg_proc`.
- الاختبار: نموذج منطق ثلاثيّ يُثبت أنّ `if not NULL` لا يعمل + تحقّق حيّ بعد التطبيق.

**٢. مخرَج غير قابل للتسليم للأبد — كان على الإنتاج ⏳ بانتظار التشغيل**
- العرَض: مخرَج «معتمد» لا يمكن تعليمه نهائيًّا أبدًا.
- السبب الجذري: سياسة `"reviews insert"` تسمح بإدراج مباشر؛ المحفّز يضبط
  `deliverables.status` **دون** `deliverable_versions.decision`؛ ثمّ `admin_set_final_version`
  تشترط `decision='approved'` ⇒ رفض دائم، ولا عودة لأنّ المخرَج غادر `client_review`.
- الإصلاح: إسقاط السياسة + سحب المنح + إزالة الغلاف الميّت `submitReview`.

**٣. فقدان دائم لرسائل الطابور — كان على الإنتاج ✅ أُصلح (بانتظار النشر)**
- العرَض: رسائل تبقى `pending` إلى الأبد.
- السبب الجذري: نافذة الالتقاط 24h ≤ فاصل الـcron اليوميّ ⇒ ما يفوت دورة يتجاوز النافذة فيُهمَل.
- الإصلاح: نافذة تعافٍ 168h — أوسع من الفاصل قطعًا.

**٤. حدثان لا يُرسلان بريدًا إطلاقًا — كان على الإنتاج ✅ أُصلح (بانتظار النشر)**
- السبب الجذري: `ProjectModules.tsx` لا يستورد المُرسِل أصلًا؛ الإرسال كان حصرًا من الشاشة القديمة.

**٥. إغلاق مشروع بلا سبب مسجَّل — كان على الإنتاج ⏳ بانتظار التشغيل**
- السبب الجذري: `nullif` يحوّل الفارغ إلى NULL بلا اعتراض، والإغلاق النهائيّ يستبدله بعبارة معلَّبة؛
  وعمود `closure_reason` لم يُكتب قطّ.

**٦. مشكلة مرفوضة تمنع الإقفال للأبد — ⏳ بانتظار التشغيل**
- السبب الجذري: انحراف مفردات — `rejected` اعتُمدت «مغلقة» في كل مكان عدا محرّك الجاهزيّة.

---

## ٥) قاعدة البيانات

- **الجداول:** 76 جدولًا لمنصّة المشاريع — **كلّها موجودة على الإنتاج** (فحص حيّ). لم يُنشأ أي جدول جديد.
- **الأعمدة المضافة:** لا شيء. (التوسعة الوحيدة قيد CHECK على `project_core.core_stage`.)
- **الدوالّ الجديدة:** `project_core_hold_action` · `pcl_require_closure_reason` ·
  `project_closure_readiness_v2` — كلّها مرافِقة، لا بديلة.
- **الدوالّ المُعاد تعريفها بنفس التوقيع:** 7 حرّاس + `pc_client_cap` + `pc_member_add`.
- **المحفّزات:** `trg_pcl_require_closure_reason` (جديد).
- **السياسات:** إسقاط `"reviews insert"` فقط — لا سياسة قراءة مُسّت.
- **الصلاحيات:** سحب EXECUTE من anon عن دوالّ البيانات + `is_client_owner`؛ سحب كتابة/حذف مباشر
  مطابق لاستخدام الواجهة.

### ملفات RUNME — الحالة النهائية

| الملف | الحالة |
|---|---|
| `project_platform_authz_hardening_RUNME.sql` | **Already Applied** ✅ متحقَّق |
| `project_platform_authz_hardening2_RUNME.sql` | **Required** — أمنيّ |
| `project_closure_integrity_RUNME.sql` | **Required** |
| `project_lifecycle_hold_cancel_RUNME.sql` | **Required** |
| `event_bound_email_dispatch_batch9g_RUNME.sql` | **Required (إعادة تشغيل)** — النسخة المنشورة تحمل خطأ 42703 |
| `project_review_decouple_batch10_RUNME.sql` | **Required (إعادة تشغيل)** — نفس السبب |
| `global_notifications_core_batch10_RUNME.sql` | Optional |
| `global_notifications_projects/custody_rental_batch10` | Optional (كتالوج + فحص دخانيّ) |
| باقي ملفّات `project_*` / `deliverable_*` / `preproduction_*` (~68) | **Already Applied** — أثبته فحص الجداول والدوالّ |
| `apps_script_email_patch.gs` | **Superseded** بـ`apps_script_portal_notify_HANDLER.gs` |

**استعلامات التحقق** موجودة في نهاية كل ملفّ RUNME.

---

## ٦) الصلاحيات

| الإجراء | Owner | Super Admin | Admin | Project Manager | Employee | Client |
|---|:--:|:--:|:--:|:--:|:--:|:--:|
| عرض المشروع | ✔ | ✔ | ✔ | ✔ | حسب العضوية | مشاريعه فقط |
| إنشاء/تعديل مشروع | ✔ | ✔ | ✔ | ✔ | ✖ | ✖ |
| تغيير المرحلة | ✔ | ✔ | ✔ | ✔ | ✖ | ✖ |
| القفز بين المراحل | ✔ | ✖ | ✖ | ✖ | ✖ | ✖ |
| تعليق/استئناف/إلغاء | ✔ | ✔ | ✔ | ✔ | ✖ | ✖ |
| إضافة عضو | ✔ | ✔ | ✔ | ✔ | محرِّر: نعم — **عدا ترقية نفسه** | ✖ |
| التسليم النهائي | ✔ | ✔ | ✔ | ✔ | ✖ | ✖ |
| اعتماد/رفض المخرَج | ✖¹ | ✖¹ | ✖¹ | ✖¹ | ✖ | ✔ |
| عرض المالية | ✔ | ✔ | ✖ | ✖ | ✖ | ✖ |
| تجاوز التعارض بسبب | ✔ | ✔ | ✔ | ✔ | ✖ | ✖ |
| الإغلاق (بسبب إلزاميّ) | ✔ | ✔ | ✔ | ✔ | ✖ | ✖ |
| منح وصول عميل | ✔ | ✔ | ✔ | ✖ | ✖ | ✖ |
| سجلّ التدقيق | ✔ | ✔ | ✔ | ✖ | ✖ | ✖ |

¹ اعتماد العميل قرار العميل؛ الكوادر يستخدمون `pc_deliverable_review`. المالية: `can_see_financials` = owner أو `staff_role in ('manager','sales')`.

---

## ٧) الاختبارات

```
npx tsc --noEmit                 → نظيف
npx eslint <الملفات المعدّلة>     → نظيف
node --test tests/               → 696/696 ✅ · 0 فشل
npm run build                    → ✓ Compiled successfully
```

- **دورة المشروع الكاملة** (`project_lifecycle_e2e`, 30): رئيسي + فرعيّان · منع الحلقات ·
  الفريق والمهن · بوّابة `ready` · تعارض موارد + تجاوز بسبب + عدم تكرار عند Retry ·
  طلب تغيير + تطبيق idempotent · المخرجات والإصدارات والاعتماد وقفل النسخة المعتمدة ·
  التسليم النهائي · بوّابات الإقفال · إعادة الفتح · تعليق/استئناف/إلغاء · التجميع الموزون.
- **الأمن** (`authz_null_collapse` 10 + `authz_hardening2` 17).
- **الإغلاق** (`v1_closure_fixes` 13).
- **RLS/العميل:** تحقّق حيّ — مسح 372 دالّة: لا دالّة تُعيد بيانات لـanon.
- **الأداء والهاتف:** ❌ لم يُختبرا.

---

## ٨) نظام الإشعارات وGoogle Apps Script

النظام المعتمد والعقد الكامل في **[`docs/NOTIFICATION_EVENT_CONTRACT.md`](NOTIFICATION_EVENT_CONTRACT.md)**:
أسماء الأحداث · مخطّط الحمولة v1 · التحقّق · idempotency (`event:entity:user`) · إعادة المحاولة
والتراجع الأسّي والفشل النهائيّ · حالات الطابور · الروابط العميقة · التفضيلات مقابل الصلاحية ·
فصل العميل عن الداخليّ.

**حالة Apps Script:** مشروع أُنشئ ونُسخ فيه كود أوّليّ فقط — **لم يُنشر ولا Triggers ولا في الإنتاج.**
المنصّة **لا تعتمد عليه**: البنية الداخلية مكتملة، والبريد وحده ينتظره.
المطلوب: `kianHandlePortalNotify_` + `kianJson_` من
[`apps_script_portal_notify_HANDLER.gs`](apps_script_portal_notify_HANDLER.gs) · ٣ أسطر في `doPost`
· **Deploy → New version** · **لا Triggers** · Script Properties: `KIAN_PORTAL_FALLBACK_TO`
(و`KIAN_PORTAL_SHARED_TOKEN` اختياريّ) · متغيّرات Vercel: `PORTAL_NOTIFY_ENDPOINT`،
`PROJECT_EMAIL_ALERTS_ENABLED` (يجب ألّا تساوي `"false"`)، `SUPABASE_SERVICE_ROLE_KEY`، `CRON_SECRET`.

**عدم الازدواجية:** مُرسِل واحد (`sendProjectEmail`) · طابور واحد (`email_deliveries`) ·
مُحلِّل واحد · cron واحد. والردّ الموسوم `handler:"portal_notify"` **إلزاميّ** — أي ردّ مبهم
يُسجَّل `relay_handler_missing` ولا يُعتبر تسليمًا، فلا يتكرّر عطل «النجاح الكاذب».

---

## ٩) Production

| البند | الحالة |
|---|---|
| Commit الحالي على `origin/main` | `8ffefa0` (قبل دفعة الإغلاق) |
| Commits محليّة غير مدفوعة | 5 (`da0fa70` … `cf70d4f`) |
| قاعدة البيانات | 76/76 جدولًا موجودة · الدوالّ موجودة |
| الإصلاح الأمنيّ الأول | ✅ مُطبَّق ومتحقَّق حيًّا |
| Cron | 3 وظائف يوميّة · `notify-email` عند 03:10 |
| طابور الإشعارات | يعمل؛ الإرسال يتوقّف على معالج Apps Script |
| متغيّرات البيئة (أسماء فقط) | `NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` · `SUPABASE_SERVICE_ROLE_KEY` · `PORTAL_NOTIFY_ENDPOINT` · `PROJECT_EMAIL_ALERTS_ENABLED` · `CRON_SECRET` · `PORTAL_PUBLIC_URL` |
| Smoke Test | حرّاس الصلاحية = `false` ✅ · تقارير الشركة = `42501` ✅ · مسح 372 دالّة: لا تسريب ✅ |
| هل الإنتاج يستخدم آخر Commit؟ | **لا** — 5 commits غير مدفوعة |

---

## ١٠) الإجراءات المطلوبة منك (قائمة واحدة مرتّبة)

1. **`git push`** (5 commits) ثم انتظر نشر Vercel.
2. شغّل على Supabase **بالترتيب**:
   1. `docs/project_platform_authz_hardening2_RUNME.sql` ← **أمنيّ، أولًا**
   2. `docs/project_closure_integrity_RUNME.sql`
   3. `docs/project_lifecycle_hold_cancel_RUNME.sql`
   4. `docs/event_bound_email_dispatch_batch9g_RUNME.sql` (إعادة تشغيل)
   5. `docs/project_review_decouple_batch10_RUNME.sql` (إعادة تشغيل)
3. **Apps Script:** الصق `docs/apps_script_portal_notify_HANDLER.gs` + ٣ أسطر في `doPost` +
   **Deploy → New version**. (أو انشره كسكربت جديد وضع رابطه في `PORTAL_NOTIFY_ENDPOINT`.)
4. **Vercel:** تأكّد أنّ `PROJECT_EMAIL_ALERTS_ENABLED` محذوف أو ≠ `"false"`
   (⚠️ `.env.example:128` يشحنه `false`)، وأنّ `CRON_SECRET` و`SUPABASE_SERVICE_ROLE_KEY` مضبوطان.
5. **اختياريّ لكن مُوصى به:** زِد تواتر `notify-email` (كل ساعة) — يتطلّب خطة Vercel تسمح بذلك،
   أو استدعِ `GET /api/cron/notify-email?secret=$CRON_SECRET` من n8n كل 15 دقيقة.
6. **تحقّق:** لوحة مراقبة الإشعارات ← «اختبار إشعار لحسابي» ⇒ يجب أن تقول «بوابة ✓ · بريد ✓».
7. **تحقّق أمنيّ:** `curl` على `rpc/is_client_owner` ⇒ يجب أن يصبح `42501`.

---

## ١١) قائمة فحص نهائية

**المالك:** إنشاء مشروع · رئيسي + فرعيّ · تعيين فريق · تغيير الحالة · **تعليق ثم استئناف**
(يجب أن يعود للمرحلة السابقة) · مراجعة المالية · المخاطر · طلب تغيير · تجاوز تعارض بسبب ·
إغلاق **بسبب إلزاميّ**.

**الموظف:** رؤية المشروع المسموح · المهام · الجلسات · رفع مخرَج · تحديث حالته ·
**عدم** رؤية المالية · **عدم** القدرة على ترقية نفسه مديرًا.

**العميل:** رؤية مشروعه فقط · المخرَج · تعليق تايم كود · طلب تعديل · اعتماد ·
تحميل النسخة النهائية عند السماح · **عدم** رؤية أي بيانات داخلية.

**الإشعارات:** إشعار داخل المنصّة · بريد · Deep Link · مقروء/غير مقروء · إعادة محاولة ·
**عدم تكرار** عند الضغط مرّتين · فشل البريد **لا** يُفشل الإجراء.

---

## ١٢) الحكم النهائي

# `Project Platform V1: NOT READY`

**العوائق الحقيقية فقط:**

**أ) عوائق تحتاج تدخّلك (لا أستطيع تنفيذها من بيئتي):**
1. `project_platform_authz_hardening2_RUNME.sql` **غير مُطبَّق** — مسار إفساد البيانات وثغرات
   الكتابة المباشرة ما زالت مفتوحة على الإنتاج الآن.
2. الكود غير مدفوع وغير منشور — إصلاحا فقدان البريد لا يعملان بعد.
3. `project_closure_integrity` و`project_lifecycle_hold_cancel` غير مُطبَّقين.

**ب) فجوات وظيفية حقيقية لم تُبنَ بعد (تحتاج عملًا إضافيًّا على الواجهة):**
4. **سياسة الإقفال غير قابلة للضبط** — `pcClosureSettingsUpsert` بلا أي مستدعٍ، فشرط
   «لا تُغلق الرئيسي قبل الفروع» **لا يمكن تفعيله أصلًا**. هذا يُبطل متطلّبًا صريحًا في طلبك.
5. **دورة طلب التغيير في الواجهة إنشاء فقط** — لا إرسال ولا اعتماد ولا تطبيق؛ ومسوّدة بعنوان
   فقط تمنع الإقفال.
6. **المشكلات والقرارات تُنشأ ولا تُغلق** من الواجهة.
7. **لا سطح للعميل** لمراجعة المهام ولا **للقبول النهائي** — فتوقيع «قبول العميل» في محضر
   التسليم يحمل مُعرّف موظّف لا العميل.
8. **لا حماية بعد الإغلاق** — مشروع مُغلق ما زال قابلًا للتعديل، فيختلف إعادة طبع المحضر
   الموقَّع عن الأصل بلا أثر.
9. تبويب المهام للقراءة فقط لمن يملك صلاحيات دقيقة · لوحة الموظف تعرض حالة `in_review` المتقاعدة.

**ما لا أستطيع حلّه من بيئتي:** تشغيل SQL على الإنتاج · الدفع والنشر · تعديل Apps Script ·
ضبط متغيّرات Vercel · اختبار الأداء والهاتف بحساب حقيقي.

**ملاحظة أمانة عن التغطية:** التدقيق غطّى 17/17 محورًا في النهاية (4 منها أُعيد تشغيلها بعد
تعطّلها). البنود (ب) أعلاه مُوثَّقة بـ`file:line` ولم تُنفَّذ بعد — لم أدّعِ إنجازها.

**عدم نشر Google Apps Script ليس عائقًا** لهذا الحكم: البنية الداخلية مكتملة وخطوات الربط موثّقة.
العوائق الحقيقية هي (أ) و(ب) أعلاه.
