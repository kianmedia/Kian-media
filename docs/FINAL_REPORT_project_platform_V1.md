# التقرير النهائي — إغلاق منصة مشاريع كيان

> # ✅ `Project Platform V1: READY FOR FINAL ACCEPTANCE`
>
> **النسخة 3 (الإغلاق)** · 2026-07-26 · الفرع `main` · Tag: `project-platform-v1.0.0`
> **اعتماد المستخدم النهائي:** نفّذ مالك المنصّة اختبار القبول على Production وأفاد بنجاحه
> بالكامل ودون أي أخطاء تشغيليّة. الاعتماد **شهادته هو** — لم أشهد التنفيذ بنفسي
> (لا أملك بيانات دخول)، وهو مسجَّل هنا كما أفاد.
>
> **نطاق V1 مُجمَّد.** لا تُضاف مزايا جديدة لمنصّة المشاريع بعد هذه النقطة؛ ما يلي
> يقتصر على إصلاح عيب مُثبَت.

---

## ملخّص الإغلاق

| البند | النتيجة |
|---|---|
| آخر Commit | `9c79830` (`origin/main` = `HEAD`، صفر معلّقة، شجرة نظيفة) |
| Build | ✅ ينجح · 33/33 صفحة |
| TypeScript | ✅ **0 خطأ** |
| ESLint | ✅ **0 خطأ** (41 تحذير `<img>` سابق لهذا العمل) |
| الاختبارات | ✅ **733/733** |
| قاعدة الإنتاج | ✅ الملفات الخمسة مُطبَّقة ومتحقَّق منها حيًّا |
| تسريب البيانات | ✅ مُغلق · مسح 372 دالّة: صفر |
| قبول المستخدم | ✅ PASS (انظر `ACCEPTANCE_TEST_V1.md`) |

**الخطوة التالية المنفصلة تمامًا:** تفعيل Google Apps Script للإشعارات
(`docs/apps_script_portal_notify_HANDLER.gs`) — **وليست تطوير منصّة المشاريع.**

---

> كل ادّعاء في هذا التقرير مبنيّ على فحص حيّ للإنتاج أو على `file:line` — عدا اختبار
> القبول اليدويّ، وهو منسوب صراحةً إلى المالك.

---

## ٠) تحديث النسخة ٢ — ما تغيّر منذ النسخة الأولى

**تحقّقتُ حيًّا من تنفيذك:**

| البند | النتيجة |
|---|---|
| `git push` والنشر | ✅ `origin/main` = `cc223c8` |
| `..._authz_hardening_RUNME.sql` | ✅ الحرّاس تُعيد `false` · تقارير الشركة `42501` |
| `..._authz_hardening2_RUNME.sql` | ✅ `pc_client_cap` و`pc_member_add` مُحدَّثتان وموجودتان |
| `project_lifecycle_hold_cancel_RUNME.sql` | ✅ `project_core_hold_action` موجودة |
| `project_closure_integrity_RUNME.sql` | ✅ `is_client_owner` صارت `42501` · `project_closure_readiness_v2` موجودة |
| مسح شامل لـ372 دالّة | ✅ **لا دالّة تُعيد بيانات لغير المُصادَق** |

**ثمّ نفّذتُ الفجوات التشغيلية الخمس** (commits `f0f0c5d` و`ee3665d`):

1. **سياسة الإقفال صارت قابلة للضبط** — `ClosurePolicyPanel` داخل تبويب الإقفال، يكتب صفّ
   `project_governance_settings` القائم عبر `pc_closure_settings_upsert` (بلا مخزن سياسات
   موازٍ). شرط **«لا يُغلق الرئيسي قبل فروعه»** صار مُفعَّلًا بضغطة، وتظهر رسالة صريحة حين
   يكون مُعطَّلًا بدل الافتراض الصامت. أضفتُ جانب القراءة الناقص `pcClosureSettingsGet`.
2. **دورة طلب التغيير كاملة** — `GovernanceItemDrawer` يقود
   `draft → internal_review → client_pending → approved/rejected → implemented` خطوةً
   شرعيّةً واحدة في كل مرّة؛ الرفض يستوجب سببًا؛ الإرسال للعميل تأكيد صريح؛ و«التطبيق»
   يعرض ردّ الخادم نصًّا فيظهر `already_applied` عند التكرار بدل إعادة تنفيذ صامتة.
3. **المشكلات والقرارات تُحرَّر وتُغلق** — بكل الحقول التي يقبلها الخادم أصلًا، وإغلاق
   المشكلة يستوجب ملخّص حلّ.
4. **سطح القبول النهائي للعميل** — `ClientFinalAcceptance` على مسار العميل القائم (بلا مسار
   ثانٍ). الآن **يُوقّع العميل بنفسه** و`accepted_by = auth.uid()` تحمل هويّته الحقيقيّة.
5. **حماية ما بعد الإغلاق** — محفّز مشترك على 16 جدولًا يمنع الكتابة على مشروع مُغلق
   (على مستوى الجدول، فيغطّي حتى الكتابة المباشرة عبر PostgREST)، مع استثناء للإدارة
   **يُسجَّل دائمًا** باسم `post_closure_write` فيبقى أي فرق عن المحضر الموقَّع مُفسَّرًا.

**مراجعة ذاتيّة كشفت عيبين حقيقيّين في كودي الجديد وأصلحتهما قبل التسليم:** لوحة السياسة
كانت ستكتب فراغات فوق السياسة الحقيقيّة (تهيئة `useState` تحدث مرّة والبيانات تصل لاحقًا)،
والحقل الرقميّ الفارغ كان سيُرسَل `""` فيُفشل التحويل `::int` بخطأ 22P02.

**الاختبارات: 719/719** · tsc/eslint نظيفان · البناء ينجح.

---

## ٠.٣) تصحيح جوهريّ — «الواجهة الفعليّة» (commit `c24f2a2`)

**كنتَ محقًّا.** أعلنتُ اكتمال «تعليق/استئناف/إلغاء» بناءً على وجود RPC وملفّ SQL، بينما
**لم يكن هناك غلاف TypeScript ولا أي واجهة** — أي أنّ `project_core_hold_action` كانت
غير قابلة للوصول من المنتج إطلاقًا. وجود Backend ليس ميزة. هذا ما صار موجودًا الآن فعلًا:

### خريطة المزايا داخل المنصّة (أين يجدها المستخدم)

| الميزة | المسار داخل الموقع |
|---|---|
| تعليق / استئناف / إلغاء / إعادة فتح | `المشاريع → فتح المشروع → دورة حياة المشروع → إجراءات المشروع` |
| قفل شريط المراحل أثناء التعليق/الإلغاء/الإغلاق | `المشاريع → فتح المشروع → دورة حياة المشروع` (الأزرار معطّلة + سبب ظاهر) |
| دورة طلب التغيير كاملة | `المشاريع → فتح المشروع → الحوكمة → طلبات التغيير → فتح الطلب` |
| تطبيق طلب التغيير (بلا تكرار) | نفس المسار → زرّ «تطبيق على المشروع» |
| حلّ وإغلاق المشكلات | `المشاريع → فتح المشروع → الحوكمة → المشكلات → فتح المشكلة` |
| اعتماد القرارات | `المشاريع → فتح المشروع → الحوكمة → القرارات → فتح القرار` |
| المخاطر والمصفوفة | `المشاريع → فتح المشروع → الحوكمة → سجل المخاطر` |
| سياسة منع إغلاق الرئيسي قبل الفروع | `المشاريع → فتح المشروع → إغلاق المشروع → سياسة الإقفال` |
| القبول النهائي للعميل | `بوابة العميل → مشاريعي → فتح المشروع → مطلوب منك: القبول النهائي` |

### خطأ حقيقيّ اكتشفتُه في كودي السابق وأصلحتُه
مفردات الحالات التي كتبتُها في نافذة الحوكمة كانت **مخترَعة** (`internal_review`،
`client_pending`، `in_progress`، قرار `rejected`) ولا وجود لها في قيود CHECK في
`project_governance_batch5a` — أي أنّ **كل حفظ كان سيفشل** على الإنتاج. استبدلتُها
بالمفردات الحقيقيّة (`draft → submitted → impact_analysis → pending_approval → approved
→ implementing → implemented → verified → closed`) مع إبقاء التسميات العربية طبقةَ عرضٍ
فقط. وأضفتُ خرائط تعريب للحالة والخطورة ونوع التغيير، فلم تعد أي قائمة تعرض مفتاحًا
إنجليزيًّا خامًا. والاختبارات الآن **تقارن كل قيمة بقيد CHECK في ملفّ SQL نفسه** — فأي
قيمة مخترَعة تُسقِط السويت بدل أن تصل الإنتاج.

**733/733 اختبارًا · tsc/eslint نظيفان · البناء ينجح.**

---

## ٠.٢) جولة التحقق الأخيرة (بعد الدفع الأخير)

| # | البند | النتيجة |
|---|---|---|
| 1 | `origin/main` يحوي آخر Commit · لا معلّق | ✅ `9b94168` · صفر معلّقة · شجرة العمل نظيفة |
| 2 | نشر Vercel جديد ناجح | ⚠️ **لا أستطيع إثباته من الخارج** — انظر الملاحظة أدناه |
| 3 | التعديلات موجودة على Production | ⚠️ نفس السبب · الكود مؤكَّد على `origin/main` |
| 4 | Build / TypeScript / ESLint | ✅ البناء ينجح (33/33 صفحة) · **0 خطأ TS** · **0 خطأ ESLint** (41 تحذير `<img>` سابق لعملي) · **719/719 اختبارًا** |

> **لماذا لا أستطيع إثبات النشر؟** حاولتُ استخراج commit المنشور من الموقع: لا يوجد
> `buildId` في HTML، ولا ترويسة تحمل الـSHA، وحزم مساحة عمل المشروع **لا تُحمَّل إلا بعد
> تسجيل الدخول** — فالزائر غير المُصادَق لا يرى شيفرة `ClosurePolicyPanel` أو
> `ClientFinalAcceptance` أصلًا. وقد بحثتُ في الحزم المتاحة فوجدتُ تطابقًا لعبارة ظننتُها
> دليلًا، ثمّ تحقّقتُ فتبيّن أنّها **موجودة أصلًا** في `ClosureCenter.tsx` — أي **إيجابيّة
> كاذبة**، فاستبعدتُها ولم أبنِ عليها. الإثبات الوحيد الموثوق هو لوحة Vercel: تحقّق أنّ
> آخر Deployment **Ready** ومربوط بـ`9b94168` (خطوة 0.1 في اختبار القبول).

---

## ٠.١) التحقق النهائي من Production (2026-07-26)

| # | البند | النتيجة |
|---|---|---|
| 1 | آخر Commit منشور | ✅ `origin/main` = `b21ea07` · صفر معلّقة · الـ3 commits موجودة |
| 1b | الكود الجديد فعلًا على origin | ✅ `ProjectModules` يستدعي المُرسِل · `RECOVERY_WINDOW_HOURS` موجود · `ClientFinalAcceptance` و`ClosurePolicyPanel` موجودان |
| 1c | الموقع يخدم | ✅ `/client-portal` → HTTP 200 |
| 2 | حماية ما بعد الإغلاق | ✅ `pc_project_is_closed` موجودة على الإنتاج. المحفّزات مُثبَتة **بحكم أنّ الملفّ معاملة واحدة (transaction) وself-test يرفض الإتمام إن لم تُركَّب على `deliverables` و`project_tasks`** — أي أنّ نجاح التشغيل يعني تركيبها. (لا أستطيع قراءة `pg_trigger` بمفتاح `anon`.) |
| 3 | اختبار المالك/الموظف/العميل | ⏳ **لم تصلني نتائجه بعد** |
| 4 | إشعارات المعاينة والتسليم | ⚠️ الكود منشور ومؤكَّد · البنية قائمة · **التسليم الفعليّ للبريد ما زال يتوقّف على معالج Apps Script المؤجَّل بطلبك** |
| 5 | سجلّات Vercel و Supabase | ❌ **لا أملك وصولًا إليها من بيئتي** |

---

## ١) ملخص تنفيذي

**الحالة قبل التنفيذ.** منصّة غنيّة بالمزايا ومنشورة بالكامل على قاعدة الإنتاج، لكنها كانت
**غير آمنة للتشغيل**: أي زائر يملك المفتاح العامّ (وهو داخل حزمة المتصفّح) كان يقرأ بيانات
الشركة. إضافةً إلى فقدان صامت في الإشعارات ومسار إفساد يجعل مخرَجًا غير قابل للتسليم أبدًا.

**ما تم إنجازه.**
1. **إغلاق تسريب بيانات حيّ** — طُبِّق على الإنتاج وتحقّقتُ منه بنفسي.
2. **إغلاق ثلاث ثغرات إضافيّة** (إفساد بيانات، كتابة مباشرة تتجاوز الحرّاس، fail-open، تصعيد ذاتيّ).
3. **إصلاح فقدان صامت** في طابور البريد وفي حدثين رئيسيّين لم يكونا يُرسلان بريدًا إطلاقًا.
4. **إكمال دورة الحياة** بحالتَي التعليق والإلغاء.
5. **تثبيت عقد أحداث الإشعارات** ومواصفات ربط Google Apps Script.
6. **696 اختبارًا** تمرّ، وبناء ناجح، وTypeScript وLint نظيفان.

**نسبة الاكتمال النهائية: ~82%** (كانت 72% عند بدء التدقيق).

**هل المنصّة جاهزة للإغلاق التشغيلي؟ لا — ليس بعد.** انظر §12.

**أهم المخاطر التي عُولجت:** تسريب بيانات الشركة للعموم · مخرَج غير قابل للتسليم للأبد ·
حذف قوالب ومحاضر تصوير نهائيًّا بتجاوز الحذف الناعم · إشعارات إلزاميّة تُنتَج ولا تُرسل أبدًا.

---

## ٢) ما كان موجودًا وتم الحفاظ عليه (لم يُعَد بناؤه)

| الميزة | القرار |
|---|---|
| المشاريع الرئيسية والفرعية (`parent_project_id` / `project_scope`) | **محميّة** — لم يُنشأ جدول ولا علاقة بديلة |
| محرّك الانتقال `project_core_set_stage` | **لم يُمَسّ** — مُعرَّف في ٤ ملفّات ولا يمكن الجزم بالمنشور |
| `project_closure_readiness` (5C) | **لم يُعَد كتابته** — أُضيف غلاف `_v2` يُركّب فوقه |
| المخرجات والإصدارات وتعليقات التايم كود | قائمة وتعمل |
| `client_review_version` | **المسار الوحيد الصحيح** — عُزِّز بإسقاط منافسه المعطوب |
| المالية `project_core_financials` | نظام واحد؛ لا مالية على جدول `projects` أصلًا |
| الإشعارات (صندوق/طابور/تتبّع/عامل/مزوّد) | نظام واحد؛ وُثِّق ولم يُستبدل |
| الحذف الناعم والاستعادة | محفوظ — والإصلاحات عزّزته |
| سجلّ التدقيق `pc_log` / `project_activity` | قائم |
| القوالب والموارد والحوكمة والإقفال | قائمة؛ أُكملت لا استُبدلت |

---

## ٣) التحسينات المنفذة

| الميزة | الحالة السابقة | ما تم تنفيذه | الملفات | قاعدة البيانات | الاختبار |
|---|---|---|---|---|---|
| حرّاس الصلاحية | تُعيد `NULL` ⇒ كود ميّت | `coalesce(...,false)` لسبعة حرّاس + سحب EXECUTE من anon | `project_platform_authz_hardening_RUNME.sql` | **مُطبَّق ومتحقَّق** | `authz_null_collapse` (10) |
| مسار مراجعة العميل | إدراج مباشر يُفسد البيانات | إسقاط السياسة + سحب المنح | `..._hardening2_RUNME.sql` §A | بانتظار التشغيل | `authz_hardening2` (17) |
| كتابة مباشرة على جداول | تتجاوز كل الحرّاس | سحب مطابق لاستخدام الواجهة | `..._hardening2_RUNME.sql` §B | بانتظار التشغيل | ✔ |
| `pc_client_cap` | fail-open عند NULL | NULL = «غير مطابق» | `..._hardening2_RUNME.sql` §C | بانتظار التشغيل | ✔ |
| ترقية ذاتية | محرِّر يرفّع نفسه مديرًا | `no_self_promotion` | `..._hardening2_RUNME.sql` §D | بانتظار التشغيل | ✔ |
| دورة الحياة | لا تعليق ولا إلغاء | توسيع CHECK + `project_core_hold_action` | `project_lifecycle_hold_cancel_RUNME.sql` | بانتظار التشغيل | `project_lifecycle_e2e` (30) |
| طابور البريد | نافذة 24h مع cron يوميّ ⇒ فقدان دائم | نافذة تعافٍ 168h | `app/api/cron/notify-email/route.ts` | — | `v1_closure_fixes` (13) |
| إشعار المعاينة/التسليم | لا يُرسل من مساحة العمل الرئيسية | ربط بنفس المُنتِج المثبت | `components/portal/projectcore/ProjectModules.tsx` | — | ✔ |
| نزاهة الإقفال | سبب غير مفروض · `rejected` منحرف · مالية غير مبوّبة · منح شارد | ٤ إصلاحات | `project_closure_integrity_RUNME.sql` | بانتظار التشغيل | ✔ |
| عقد الإشعارات | غير موثّق | عقد كامل + مواصفات GAS | `NOTIFICATION_EVENT_CONTRACT.md` | — | — |

---

## ٤) إصلاحات الأخطاء (السبب الجذري لا العرَض)

**١. تسريب بيانات الشركة للعموم — كان على الإنتاج ✅ أُصلح وطُبِّق**
- العرَض: `company_execution_report` يُعيد 20 مشروعًا و44 مهمة لمستدعٍ غير مُصادَق.
- السبب الجذري: `staff_role()` تُعيد NULL ⇒ `can_manage_projects() = false OR NULL = NULL` ⇒
  `if not NULL then raise` **لا يُنفَّذ**؛ والدوالّ SECURITY DEFINER فتتجاوز RLS. مضاعَفًا بأنّ
  PostgreSQL يمنح EXECUTE لـPUBLIC افتراضيًّا.
- الإصلاح: `coalesce(...,false)` + سحب EXECUTE بتوقيعات من `pg_proc`.
- الاختبار: نموذج منطق ثلاثيّ يُثبت أنّ `if not NULL` لا يعمل + تحقّق حيّ بعد التطبيق.

**٢. مخرَج غير قابل للتسليم للأبد — كان على الإنتاج ⏳ بانتظار التشغيل**
- العرَض: مخرَج «معتمد» لا يمكن تعليمه نهائيًّا أبدًا.
- السبب الجذري: سياسة `"reviews insert"` تسمح بإدراج مباشر؛ المحفّز يضبط
  `deliverables.status` **دون** `deliverable_versions.decision`؛ ثمّ `admin_set_final_version`
  تشترط `decision='approved'` ⇒ رفض دائم، ولا عودة لأنّ المخرَج غادر `client_review`.
- الإصلاح: إسقاط السياسة + سحب المنح + إزالة الغلاف الميّت `submitReview`.

**٣. فقدان دائم لرسائل الطابور — كان على الإنتاج ✅ أُصلح (بانتظار النشر)**
- العرَض: رسائل تبقى `pending` إلى الأبد.
- السبب الجذري: نافذة الالتقاط 24h ≤ فاصل الـcron اليوميّ ⇒ ما يفوت دورة يتجاوز النافذة فيُهمَل.
- الإصلاح: نافذة تعافٍ 168h — أوسع من الفاصل قطعًا.

**٤. حدثان لا يُرسلان بريدًا إطلاقًا — كان على الإنتاج ✅ أُصلح (بانتظار النشر)**
- السبب الجذري: `ProjectModules.tsx` لا يستورد المُرسِل أصلًا؛ الإرسال كان حصرًا من الشاشة القديمة.

**٥. إغلاق مشروع بلا سبب مسجَّل — كان على الإنتاج ⏳ بانتظار التشغيل**
- السبب الجذري: `nullif` يحوّل الفارغ إلى NULL بلا اعتراض، والإغلاق النهائيّ يستبدله بعبارة معلَّبة؛
  وعمود `closure_reason` لم يُكتب قطّ.

**٦. مشكلة مرفوضة تمنع الإقفال للأبد — ⏳ بانتظار التشغيل**
- السبب الجذري: انحراف مفردات — `rejected` اعتُمدت «مغلقة» في كل مكان عدا محرّك الجاهزيّة.

---

## ٥) قاعدة البيانات

- **الجداول:** 76 جدولًا لمنصّة المشاريع — **كلّها موجودة على الإنتاج** (فحص حيّ). لم يُنشأ أي جدول جديد.
- **الأعمدة المضافة:** لا شيء. (التوسعة الوحيدة قيد CHECK على `project_core.core_stage`.)
- **الدوالّ الجديدة:** `project_core_hold_action` · `pcl_require_closure_reason` ·
  `project_closure_readiness_v2` — كلّها مرافِقة، لا بديلة.
- **الدوالّ المُعاد تعريفها بنفس التوقيع:** 7 حرّاس + `pc_client_cap` + `pc_member_add`.
- **المحفّزات:** `trg_pcl_require_closure_reason` (جديد).
- **السياسات:** إسقاط `"reviews insert"` فقط — لا سياسة قراءة مُسّت.
- **الصلاحيات:** سحب EXECUTE من anon عن دوالّ البيانات + `is_client_owner`؛ سحب كتابة/حذف مباشر
  مطابق لاستخدام الواجهة.

### ملفات RUNME — الحالة النهائية

| الملف | الحالة |
|---|---|
| `project_platform_authz_hardening_RUNME.sql` | **Already Applied** ✅ متحقَّق |
| `project_platform_authz_hardening2_RUNME.sql` | **Required** — أمنيّ |
| `project_closure_integrity_RUNME.sql` | **Required** |
| `project_lifecycle_hold_cancel_RUNME.sql` | **Required** |
| `event_bound_email_dispatch_batch9g_RUNME.sql` | **Required (إعادة تشغيل)** — النسخة المنشورة تحمل خطأ 42703 |
| `project_review_decouple_batch10_RUNME.sql` | **Required (إعادة تشغيل)** — نفس السبب |
| `global_notifications_core_batch10_RUNME.sql` | Optional |
| `global_notifications_projects/custody_rental_batch10` | Optional (كتالوج + فحص دخانيّ) |
| باقي ملفّات `project_*` / `deliverable_*` / `preproduction_*` (~68) | **Already Applied** — أثبته فحص الجداول والدوالّ |
| `apps_script_email_patch.gs` | **Superseded** بـ`apps_script_portal_notify_HANDLER.gs` |

**استعلامات التحقق** موجودة في نهاية كل ملفّ RUNME.

---

## ٦) الصلاحيات

| الإجراء | Owner | Super Admin | Admin | Project Manager | Employee | Client |
|---|:--:|:--:|:--:|:--:|:--:|:--:|
| عرض المشروع | ✔ | ✔ | ✔ | ✔ | حسب العضوية | مشاريعه فقط |
| إنشاء/تعديل مشروع | ✔ | ✔ | ✔ | ✔ | ✖ | ✖ |
| تغيير المرحلة | ✔ | ✔ | ✔ | ✔ | ✖ | ✖ |
| القفز بين المراحل | ✔ | ✖ | ✖ | ✖ | ✖ | ✖ |
| تعليق/استئناف/إلغاء | ✔ | ✔ | ✔ | ✔ | ✖ | ✖ |
| إضافة عضو | ✔ | ✔ | ✔ | ✔ | محرِّر: نعم — **عدا ترقية نفسه** | ✖ |
| التسليم النهائي | ✔ | ✔ | ✔ | ✔ | ✖ | ✖ |
| اعتماد/رفض المخرَج | ✖¹ | ✖¹ | ✖¹ | ✖¹ | ✖ | ✔ |
| عرض المالية | ✔ | ✔ | ✖ | ✖ | ✖ | ✖ |
| تجاوز التعارض بسبب | ✔ | ✔ | ✔ | ✔ | ✖ | ✖ |
| الإغلاق (بسبب إلزاميّ) | ✔ | ✔ | ✔ | ✔ | ✖ | ✖ |
| منح وصول عميل | ✔ | ✔ | ✔ | ✖ | ✖ | ✖ |
| سجلّ التدقيق | ✔ | ✔ | ✔ | ✖ | ✖ | ✖ |

¹ اعتماد العميل قرار العميل؛ الكوادر يستخدمون `pc_deliverable_review`. المالية: `can_see_financials` = owner أو `staff_role in ('manager','sales')`.

---

## ٧) الاختبارات

```
npx tsc --noEmit                 → نظيف
npx eslint <الملفات المعدّلة>     → نظيف
node --test tests/               → 696/696 ✅ · 0 فشل
npm run build                    → ✓ Compiled successfully
```

- **دورة المشروع الكاملة** (`project_lifecycle_e2e`, 30): رئيسي + فرعيّان · منع الحلقات ·
  الفريق والمهن · بوّابة `ready` · تعارض موارد + تجاوز بسبب + عدم تكرار عند Retry ·
  طلب تغيير + تطبيق idempotent · المخرجات والإصدارات والاعتماد وقفل النسخة المعتمدة ·
  التسليم النهائي · بوّابات الإقفال · إعادة الفتح · تعليق/استئناف/إلغاء · التجميع الموزون.
- **الأمن** (`authz_null_collapse` 10 + `authz_hardening2` 17).
- **الإغلاق** (`v1_closure_fixes` 13).
- **RLS/العميل:** تحقّق حيّ — مسح 372 دالّة: لا دالّة تُعيد بيانات لـanon.
- **الأداء والهاتف:** ❌ لم يُختبرا.

---

## ٨) نظام الإشعارات وGoogle Apps Script

النظام المعتمد والعقد الكامل في **[`docs/NOTIFICATION_EVENT_CONTRACT.md`](NOTIFICATION_EVENT_CONTRACT.md)**:
أسماء الأحداث · مخطّط الحمولة v1 · التحقّق · idempotency (`event:entity:user`) · إعادة المحاولة
والتراجع الأسّي والفشل النهائيّ · حالات الطابور · الروابط العميقة · التفضيلات مقابل الصلاحية ·
فصل العميل عن الداخليّ.

**حالة Apps Script:** مشروع أُنشئ ونُسخ فيه كود أوّليّ فقط — **لم يُنشر ولا Triggers ولا في الإنتاج.**
المنصّة **لا تعتمد عليه**: البنية الداخلية مكتملة، والبريد وحده ينتظره.
المطلوب: `kianHandlePortalNotify_` + `kianJson_` من
[`apps_script_portal_notify_HANDLER.gs`](apps_script_portal_notify_HANDLER.gs) · ٣ أسطر في `doPost`
· **Deploy → New version** · **لا Triggers** · Script Properties: `KIAN_PORTAL_FALLBACK_TO`
(و`KIAN_PORTAL_SHARED_TOKEN` اختياريّ) · متغيّرات Vercel: `PORTAL_NOTIFY_ENDPOINT`،
`PROJECT_EMAIL_ALERTS_ENABLED` (يجب ألّا تساوي `"false"`)، `SUPABASE_SERVICE_ROLE_KEY`، `CRON_SECRET`.

**عدم الازدواجية:** مُرسِل واحد (`sendProjectEmail`) · طابور واحد (`email_deliveries`) ·
مُحلِّل واحد · cron واحد. والردّ الموسوم `handler:"portal_notify"` **إلزاميّ** — أي ردّ مبهم
يُسجَّل `relay_handler_missing` ولا يُعتبر تسليمًا، فلا يتكرّر عطل «النجاح الكاذب».

---

## ٩) Production

| البند | الحالة |
|---|---|
| Commit الحالي على `origin/main` | `8ffefa0` (قبل دفعة الإغلاق) |
| Commits محليّة غير مدفوعة | 5 (`da0fa70` … `cf70d4f`) |
| قاعدة البيانات | 76/76 جدولًا موجودة · الدوالّ موجودة |
| الإصلاح الأمنيّ الأول | ✅ مُطبَّق ومتحقَّق حيًّا |
| Cron | 3 وظائف يوميّة · `notify-email` عند 03:10 |
| طابور الإشعارات | يعمل؛ الإرسال يتوقّف على معالج Apps Script |
| متغيّرات البيئة (أسماء فقط) | `NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_ANON_KEY` · `SUPABASE_SERVICE_ROLE_KEY` · `PORTAL_NOTIFY_ENDPOINT` · `PROJECT_EMAIL_ALERTS_ENABLED` · `CRON_SECRET` · `PORTAL_PUBLIC_URL` |
| Smoke Test | حرّاس الصلاحية = `false` ✅ · تقارير الشركة = `42501` ✅ · مسح 372 دالّة: لا تسريب ✅ |
| هل الإنتاج يستخدم آخر Commit؟ | **لا** — 5 commits غير مدفوعة |

---

## ١٠) الإجراءات المطلوبة منك (قائمة واحدة مرتّبة)

1. **`git push`** (5 commits) ثم انتظر نشر Vercel.
2. شغّل على Supabase **بالترتيب**:
   1. `docs/project_platform_authz_hardening2_RUNME.sql` ← **أمنيّ، أولًا**
   2. `docs/project_closure_integrity_RUNME.sql`
   3. `docs/project_lifecycle_hold_cancel_RUNME.sql`
   4. `docs/event_bound_email_dispatch_batch9g_RUNME.sql` (إعادة تشغيل)
   5. `docs/project_review_decouple_batch10_RUNME.sql` (إعادة تشغيل)
3. **Apps Script:** الصق `docs/apps_script_portal_notify_HANDLER.gs` + ٣ أسطر في `doPost` +
   **Deploy → New version**. (أو انشره كسكربت جديد وضع رابطه في `PORTAL_NOTIFY_ENDPOINT`.)
4. **Vercel:** تأكّد أنّ `PROJECT_EMAIL_ALERTS_ENABLED` محذوف أو ≠ `"false"`
   (⚠️ `.env.example:128` يشحنه `false`)، وأنّ `CRON_SECRET` و`SUPABASE_SERVICE_ROLE_KEY` مضبوطان.
5. **اختياريّ لكن مُوصى به:** زِد تواتر `notify-email` (كل ساعة) — يتطلّب خطة Vercel تسمح بذلك،
   أو استدعِ `GET /api/cron/notify-email?secret=$CRON_SECRET` من n8n كل 15 دقيقة.
6. **تحقّق:** لوحة مراقبة الإشعارات ← «اختبار إشعار لحسابي» ⇒ يجب أن تقول «بوابة ✓ · بريد ✓».
7. **تحقّق أمنيّ:** `curl` على `rpc/is_client_owner` ⇒ يجب أن يصبح `42501`.

---

## ١١) قائمة فحص نهائية

**المالك:** إنشاء مشروع · رئيسي + فرعيّ · تعيين فريق · تغيير الحالة · **تعليق ثم استئناف**
(يجب أن يعود للمرحلة السابقة) · مراجعة المالية · المخاطر · طلب تغيير · تجاوز تعارض بسبب ·
إغلاق **بسبب إلزاميّ**.

**الموظف:** رؤية المشروع المسموح · المهام · الجلسات · رفع مخرَج · تحديث حالته ·
**عدم** رؤية المالية · **عدم** القدرة على ترقية نفسه مديرًا.

**العميل:** رؤية مشروعه فقط · المخرَج · تعليق تايم كود · طلب تعديل · اعتماد ·
تحميل النسخة النهائية عند السماح · **عدم** رؤية أي بيانات داخلية.

**الإشعارات:** إشعار داخل المنصّة · بريد · Deep Link · مقروء/غير مقروء · إعادة محاولة ·
**عدم تكرار** عند الضغط مرّتين · فشل البريد **لا** يُفشل الإجراء.

---

## ١٢) الحكم النهائي

# ✅ `Project Platform V1: READY FOR FINAL ACCEPTANCE`

**الأساس:** كل البوابات الآليّة خضراء (Build · TypeScript · ESLint · 733/733) · قاعدة
الإنتاج مُطبَّقة ومتحقَّق منها · التسريب الأمنيّ مُغلق ومُثبَت بمسح 372 دالّة · **واختبار
القبول اليدويّ نُفِّذ على Production من قِبل المالك ونجح بالكامل**.

**تحفّظات مسجَّلة للأمانة — لا تمنع القبول:**
- اختبار القبول شهادة المالك، لا مشاهدة منّي (لا أملك بيانات دخول).
- لم أستطع إثبات أي commit نشره Vercel من الخارج (حزم مساحة العمل محميّة بتسجيل الدخول)؛
  تحقّق المالك منه في لوحة Vercel. وقد أمسكتُ **إيجابيّتين كاذبتين** أثناء المحاولة
  واستبعدتُهما بدل تقديمهما كدليل.
- **وصول البريد الفعليّ** ما زال يتوقّف على معالج Apps Script المؤجَّل بقرار المالك.
  البنية الداخليّة مكتملة: الرسائل تُدرَج في الطابور سليمةً بلا تكرار، ولا تُستهلك
  محاولاتها، وتُرسَل تلقائيًّا خلال ~30 دقيقة من تركيب المعالج.
- الأداء والاستخدام على الجوال لم يُختبرا رسميًّا.

---

<details>
<summary>الحكم السابق (محفوظ للسجلّ) — NOT READY</summary>

# `Project Platform V1: NOT READY`

**الفجوات التشغيلية الخمس أُغلقت كلّها في الكود، والاختبارات تمرّ (719/719)، والبناء ينجح.**
لكن الحكم يظلّ `NOT READY` لسببين حقيقيّين فقط — كلاهما خارج ما أستطيع تنفيذه:

### ✅ ما اكتمل وتحقّقتُ منه
- النشر: كل الكود على `origin/main` = `b21ea07`، والموقع يخدم.
- الملفات الخمسة على الإنتاج: hardening ×2، دورة الحياة، نزاهة الإقفال، حماية ما بعد الإغلاق.
- صفر تسريب: مسح 372 دالّة.
- 719/719 اختبارًا · tsc/eslint · البناء.

### ⏳ العائق الوحيد المتبقّي: شهادة لم تصلني
**نتائج اختبار المالك والموظف والعميل لم تصلني بعد** (ذكرتَ أنّك سترسلها). وهي **الشرط
الأخير** — لا أستطيع أن أشهد بنجاح دورة لم أرَ نتيجتها، ولا أستطيع تنفيذها بنفسي: لا أملك
كلمات مرور هذه الحسابات ولا يجوز لي إدخال بيانات اعتماد.

**وسجلّات Vercel وSupabase خارج وصولي تمامًا** — فحصها بيدك.

### ما تحقّقتُ منه فعلًا وما لم أستطع
| البند | الحالة |
|---|---|
| الإصلاحات الأمنيّة الأربعة | ✅ مُطبَّقة ومتحقَّق منها حيًّا |
| بنية الإشعارات على الإنتاج | ✅ الجداول والأعمدة والدوالّ كلّها موجودة |
| عدم تكرار الرسائل | ✅ `idempotency_key` + `correlation_id` موجودان |
| **محتوى دوالّ الإشعارات بعد إعادة التشغيل** | ⚠️ **لم أستطع التحقّق** — لا يمكن قراءة جسم الدالّة بمفتاح `anon`. أستطيع إثبات وجودها لا صحّة جسمها. |
| **وصول بريد فعليّ** | ⚠️ يتوقّف على معالج Apps Script — **مؤجَّل بطلبك** |
| الأداء والهاتف | ❌ لم يُختبرا |

### الإجراءات اليدوية المتبقّية — القائمة الكاملة والوحيدة

**لا يوجد سواها. كل ما عداها مُنجَز ومُتحقَّق منه.**

| # | الإجراء | لماذا لا أستطيع تنفيذه |
|---|---|---|
| 1 | تأكيد أنّ آخر Deployment في **Vercel** حالته **Ready** ومربوط بـ`9b94168` | لا وصول للوحة Vercel، ولا يكشف الموقع الـcommit المنشور |
| 2 | تنفيذ **`docs/ACCEPTANCE_TEST_V1.md`** بثلاثة حسابات (مالك/موظف/عميل) وإرسال الجدول + مخرجات SQL | لا أملك بيانات الدخول، ولا يجوز لي إدخال بيانات اعتماد |
| 3 | فحص **Vercel Logs** و**Supabase Logs** (البند ٨ في ملف الاختبار) | لا وصول لأيٍّ منهما من بيئتي |

> **Google Apps Script مؤجَّل بطلبك** ولا يُحتسب عائقًا: البنية الداخلية مكتملة وموثّقة،
> والرسائل تُدرَج في الطابور سليمةً وتُرسَل تلقائيًّا خلال ~30 دقيقة من تركيب المعالج.

### تفصيل اختبار القبول (الملف الكامل: `docs/ACCEPTANCE_TEST_V1.md`)
أهمّ أربع نقاط تُحسم بها الجاهزيّة:
   - تفعيل «إغلاق الفروع أولًا» ثمّ محاولة إغلاق الرئيسي وفرعٌ مفتوح ⇒ يجب أن يُمنع؛
   - طلب تغيير يمشي حتى `implemented`، والضغط على «تطبيق» مرّتين ⇒ يجب أن تظهر
     `already_applied` لا تطبيق مزدوج؛
   - قبول العميل من **حسابه هو** ⇒ ثمّ تحقّق أنّ `accepted_by` = مُعرّف العميل:
     ```sql
     select accepted_by, accepted_at from public.project_final_acceptances
      where acceptance_type='client_final' order by accepted_at desc limit 5;
     ```
   - محاولة تعديل مشروع مُغلق بحساب موظّف ⇒ يجب أن تظهر `project_closed`.

**وللبند ٤ (إشعارات المعاينة والتسليم)** — بعد إرسال معاينة وتعليم تسليم نهائيّ من مساحة
العمل الرئيسية، شغّل على Supabase:
```sql
-- يجب أن تظهر صفوف جديدة (status='pending' مقبول: التسليم ينتظر معالج Apps Script)
select status, left(coalesce(idempotency_key,''),42) as idem, subject, created_at
  from public.email_deliveries
 where created_at > now() - interval '1 hour'
 order by created_at desc limit 20;

-- عدم التكرار: كل مفتاح مرّة واحدة فقط
select idempotency_key, count(*) from public.email_deliveries
 where idempotency_key is not null group by 1 having count(*) > 1;
-- المتوقّع: صفر صفوف
```
ظهور صفوف بمفاتيح `rev:…` و`preview:…` = المُنتِج يعمل. بقاؤها `pending` مع
`last_error='relay_handler_missing'` هو **السلوك الصحيح المصمَّم** ما دام معالج Apps Script
مؤجَّلًا — الرسائل محفوظة ولا تُستهلك محاولاتها، وتُرسَل تلقائيًّا خلال 30 دقيقة من تركيب المعالج.

**عدم نشر Google Apps Script ليس عائقًا** لهذا الحكم (وقد طلبتَ تأجيله): البنية الداخلية
مكتملة وموثّقة. العائق الحقيقي هو (أ) و(ب) أعلاه فقط.

> لم أُصدر `READY` لأنّني لا أستطيع أن أشهد بنجاح شيء لم أختبره. بعد إتمامك للخطوات
> الثلاث أعلاه — وخصوصًا الدورة بثلاثة حسابات — يصبح الحكم قابلًا للترقية.

</details>
