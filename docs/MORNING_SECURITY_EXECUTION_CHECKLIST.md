# قائمة تنفيذ الصباح — الأمن والصلاحيات

> **ملفّ واحد في كل مرة.** شغّله · تحقّق · ثم انتقل. لا تُشغّل دفعة.
> بعد كل خطوة: إن فشل شرط النجاح **توقّف** ولا تنتقل.
>
> 🔴 **زرّ الطوارئ — يعمل في أيّ لحظة من أيّ مرحلة:**
> ```sql
> update public.mfa_settings set enforcement_mode = 'off' where id = 1;
> ```
> يُبطل بوّابة MFA فورًا على الطلب التالي. **لا يُلغي** إصلاحات A/B/C — تلك تُتراجَع بملفّاتها.

---

## Phase 0 — قراءة فقط · بلا اعتماديّة

| | |
|---|---|
| **الملفّ** | `docs/PREFLIGHT_P1_role_census.sql` — **الاستعلام (3) فقط** |
| **النوع** | 🟢 قراءة فقط |
| **الاعتماديّة** | لا شيء |
| **المتوقَّع** | عدد الأدوار خارج قائمة الـ12 |
| **✅ نجاح** | `0` |
| **❌ فشل** | `> 0` ⇒ توجد أدوار غير معروفة في البيانات |
| **إن فشل** | **لا يمنع Phase 1.** لكن **يمنع Phase 5** (org_admin) حتى نقرّر مصيرها |
| **تراجع** | لا يوجد — لم يتغيّر شيء |
| **التالي؟** | ✅ نعم |

---

## Phase 1 — Fix A · منع إنشاء Super Admin بلا حدّ

| | |
|---|---|
| **الملفّ** | `docs/authz_fixA_super_admin_grant_RUNME.sql` |
| **النوع** | 🔴 كتابة (تعريف دالّة واحدة) |
| **الاعتماديّة** | **لا شيء** — مستقلّ تمامًا |
| **المتوقَّع** | `✓ إصلاح A: منح super_admin صار مقصورًا على المالك · كل الشروط الأصلية سليمة` |
| **✅ نجاح** | ظهور الإشعار أعلاه وبلا أيّ `فشل:` |
| **❌ فشل** | أيّ `raise exception 'فشل: …'` ⇒ المعاملة تراجعت ولم يتغيّر شيء |
| **تراجع** | `docs/authz_fixA_super_admin_grant_ROLLBACK.sql` ⚠️ يُعيد فتح الثغرة |

**التحقّق بعده:** §1 من `docs/SECURITY_POST_APPLY_VERIFICATION.sql`

**الاختبار اليدوي:** من حساب المالك، جرّب إسناد `super_admin` لموظف — **يجب أن ينجح**
(أنت المالك الحقيقي). ثمّ جرّب أيّ دور تشغيلي آخر — يجب أن ينجح كالمعتاد.

> 💡 بما أن `super_admin_count = 0`، **لا أحد اليوم يستطيع استغلال الثغرة**. هذا الإصلاح
> يسبق أوّل منح — لأن أوّل super_admin يُنشأ يصبح قادرًا على صناعة غيره بلا حدّ.

| **التالي؟** | ✅ فقط إن ظهر إشعار النجاح |

---

## Phase 2 — Fix C · البوّابات الستّ الفاشلة-مفتوحة

| | |
|---|---|
| **الملفّ** | `docs/authz_fixC_null_failopen_gates_RUNME.sql` |
| **النوع** | 🔴 كتابة (6 مُسنِدات) |
| **الاعتماديّة** | لا شيء |
| **المتوقَّع** | الستّ تُعيد `boolean` حقيقيًّا دائمًا، ولا تُعيد `NULL` |
| **✅ نجاح** | إشعار النجاح بلا `فشل:` |
| **تراجع** | `docs/authz_fixC_null_failopen_gates_ROLLBACK.sql` ⚠️ يُعيد الفشل-المفتوح |

**التحقّق بعده:** §2 من ملفّ التحقّق.

**⚠️ أثر تشغيليّ متوقَّع — اقرأه قبل أن تظنّه عطلًا:**
مستخدم يمرّ **اليوم** من أحد الحرّاس بسبب `NULL` سيبدأ **يُرفض** بعد الإصلاح.
هذا **هو الإصلاح يعمل**، لا كسرًا. إن اشتكى موظف من فقدان وصول بعد هذه الخطوة،
فالسبب أنه كان يصل بلا صلاحية فعليّة — امنحه الصلاحية صراحةً بدل التراجع.

**الاختبار اليدوي — الأهمّ في هذه المرحلة:**
1. افتح `/quote-request` **بمتصفّح خفيّ (غير مُسجَّل دخول)** وأرسل طلبًا → **يجب أن ينجح**.
2. افتح `/opportunities` وأرسل → **يجب أن ينجح**.
3. سجّل الدخول كموظف عادي → تأكّد أن صفحاته المعتادة تعمل.

> الملفّ **لا يسحب** `EXECUTE` من `anon` عمدًا — ثلاث دوالّ مكشوفة له، والسحب الأعمى
> كان قد يكسر النماذج العامّة. أُغلق الفشل-المفتوح بإرجاع `false` صريحة بدل ذلك.

| **التالي؟** | ✅ فقط إن نجح النموذجان العامّان |

---

## Phase 3 — s4pre ثم Fix B · فصل الهوية عن المشاريع

### 3-أ

| | |
|---|---|
| **الملفّ** | `docs/authz_identity_hardening_s4pre_RUNME.sql` |
| **النوع** | 🔴 كتابة (مُسنِدان جديدان، غير مربوطَين) |
| **الاعتماديّة** | لا شيء |
| **✅ نجاح** | `✓ S4-pre: صلاحية إدارة الهويّات مفصولة · حارس منح الأدوار جاهز` |
| **تراجع** | لا حاجة — يُنشئ دالّتين لا يستدعيهما أحد بعد |

### 3-ب

| | |
|---|---|
| **الملفّ** | `docs/authz_fixB_identity_permissions_RUNME.sql` |
| **النوع** | 🔴 كتابة (7 دوالّ) |
| **الاعتماديّة** | ⛔ **3-أ إلزاميّة قبله** — يستدعي `can_manage_identity()` |
| **✅ نجاح** | إشعار النجاح بلا `فشل:` |
| **تراجع** | `docs/authz_fixB_identity_permissions_ROLLBACK.sql` ⚠️ يعيد للمدير كتابة الصلاحيات |

**التحقّق بعده:** §3 و §4 من ملفّ التحقّق.

**الاختبار اليدوي:**
1. **بحساب المالك:** افتح شاشة صلاحيات الموظفين وعدّل صلاحية → **يجب أن ينجح**.
2. **بحساب مدير (manager):** جرّب نفس الشيء → **يجب أن يُرفض** بـ`authorization_denied`.
3. تأكّد أن المدير ما زال يدير **المشاريع** كالمعتاد — الفصل يخصّ الهويّة لا المشاريع.

| **التالي؟** | ✅ فقط إن نجح المالك ورُفض المدير |

---

## Phase 4 — اختبارات دخان شاملة لـA/B/C

**لا ملفّ يُشغَّل.** تحقّق يدويًّا:

- [ ] دخول المالك يعمل (وشاشة TOTP تظهر كالمعتاد)
- [ ] دخول موظف يعمل ولم يتغيّر
- [ ] دخول عميل يعمل ولم يتغيّر
- [ ] `/quote-request` العامّ يعمل
- [ ] `/opportunities` العامّ يعمل
- [ ] صفحات المشاريع تعمل للمدير
- [ ] العهدة / HR / التأجير تعمل لأصحابها
- [ ] لا شاشة فارغة في أيّ مكان

**❌ أيّ فشل هنا ⇒ توقّف، ولا تبدأ Phase 6.** راجع خطة التراجع.

---

## Phase 5 — org_admin

> # ⛔ DO NOT RUN WITHOUT SEPARATE APPROVAL
>
> `docs/org_admin_migration_RUNME.sql` **لا يُشغَّل صباحًا** إلا بقرار منفصل منك.
>
> - لا يُنشئ أحدًا ولا يمنح شيئًا — يوسّع القيد ويضيف مُسنِدات خاملة فقط.
> - **لا يجعل الدور قابلًا للإسناد**: `admin_set_staff_role` تحمل قائمتها النصّية
>   ولا تقرأ القيد، فستردّ `invalid staff role: org_admin` حتى تُوسَّع القائمة لاحقًا.
> - الراية `NEXT_PUBLIC_ORG_ADMIN_ROLE_ENABLED` تبقى **OFF**.
> - يعتمد على نتيجة Phase 0 إن كانت > 0.

---

## ⛔⛔ Phase 6 — موقوفة · حاجز مؤكَّد بمراجعة عدائية

> # لا تُشغّل `mfa_write_gate_s4b_RUNME.sql` صباحًا.
>
> **السبب (مؤكَّد لا مُفترَض):** `components/portal/useSensitiveWrite.tsx` — وهو المسار
> الوحيد الذي يلتقط `mfa_required` ويفتح نافذة التحقّق — **لا يستورده أيّ ملفّ**.
> `grep` عبر `app/` و`components/` و`lib/` يُعيد تعريفه فقط.
>
> **الأثر لو طُبِّق S4b الآن:** حسابك — وهو الوحيد الذي يحمل عامل TOTP — سيُمنع من كل
> عملية إدارية حسّاسة، وسيرى النصّ الخام `mfa_required` بلا أيّ نافذة تحقّق.
> `AdminStaff.tsx:57` و`AdminProfessions.tsx:74` يعرضان `r.error` مباشرة، و
> `client.ts:33` يُرجع حقل `message` حرفيًّا. **المخرج الوحيد وقتها زرّ الطوارئ SQL.**
>
> **ما يرفع الحاجز:** ربط `useSensitiveWrite` في `AdminStaff.tsx` و`AdminAccounts.tsx`
> و`AdminProfessions.tsx` و`ProfessionPicker.tsx`، ثم اختبار الدورة. عمل واجهة لا SQL.
>
> **Phases 0–4 غير متأثّرة إطلاقًا** — إصلاحات A و B و C مستقلّة عن MFA ولا تحتاج هذا الربط.
> نفّذها صباحًا كالمخطّط.

---

## Phase 6 — S4a ثم S4b (بعد رفع الحاجز أعلاه)

### 6-أ · فحص مسبق

| | |
|---|---|
| **الملفّ** | `docs/mfa_write_gate_s4b_PREFLIGHT.sql` |
| **النوع** | 🟢 قراءة فقط |
| **✅ نجاح** | يؤكّد أن A و B و s4pre مُطبَّقة، ويعرض الوضع وعدد الحسابات ذات العامل |
| **❌ فشل** | أيّ «غير مُطبَّق» ⇒ **ارجع للمرحلة الناقصة** ولا تكمل |

### 6-ب

| | |
|---|---|
| **الملفّ** | `docs/mfa_write_gate_s4a_RUNME.sql` |
| **النوع** | 🔴 كتابة (مُسنِدان، **غير مربوطَين بأيّ دالّة**) |
| **✅ نجاح** | `✓ S4a: المُسنِد جاهز · لا دالّة مبوَّبة بعد · لا سلوك تغيّر` |
| **ملاحظة** | بعده **لا شيء يتغيّر** — البوّابة موجودة ولا أحد يستدعيها |

### 6-ج

| | |
|---|---|
| **الملفّ** | `docs/mfa_write_gate_s4b_RUNME.sql` |
| **النوع** | 🔴 كتابة (7 دوالّ) — **هنا يبدأ الإلزام فعليًّا** |
| **الاعتماديّة** | ⛔ **A + B + s4pre + S4a كلّها إلزاميّة** |
| **✅ نجاح** | إشعار النجاح، وتأكيد بقاء فحوص Fix A و Fix B |
| **تراجع** | `docs/mfa_write_gate_s4b_ROLLBACK.sql` — يُزيل البوّابة **ويُبقي A و B** |

### 6-د

| | |
|---|---|
| **الملفّ** | `docs/mfa_write_gate_s4b_POSTCHECK.sql` |
| **النوع** | 🟢 قراءة فقط |
| **✅ نجاح** | السبع تحمل البوّابة · لا حارس أصليّ سقط · `mfa_admin_set_mode` **بلا بوّابة** |

| **التالي؟** | ✅ فقط بعد نجاح 6-د بالكامل |

---

## Phase 7 — قبول المالك اليدوي

المرجع الكامل: `docs/MFA_S4B_MANUAL_ACCEPTANCE.md`

1. [ ] **تسجيل خروج**
2. [ ] **دخول بكلمة المرور** → تظهر شاشة TOTP
3. [ ] **أدخل الرمز** → تدخل البوابة
4. [ ] **تأكيد `aal2`** — الشارة في `/client-portal/profile` تقول «مُتحقَّق منها بخطوتين»
5. [ ] **نفّذ عملية حسّاسة** (مثلًا تغيير دور موظف) → **تنجح**
6. [ ] **تحديث الصفحة** → الجلسة باقية، ولا يُعاد التحدّي
7. [ ] **اختبار رفض aal1:** سجّل خروجًا ودخولًا، وقبل إدخال الرمز حاول تنفيذ عملية حسّاسة
       (من تبويب آخر إن لزم) → **يجب أن تُرفض بـ`mfa_required` وتظهر نافذة التحقّق**
8. [ ] **اختبار الطوارئ:** `update public.mfa_settings set enforcement_mode='off' where id=1;`
       → أعد التحميل → العملية الحسّاسة تنجح **بلا** مطالبة MFA
9. [ ] **أعِد التفعيل:** `update public.mfa_settings set enforcement_mode='enrollment' where id=1;`
       → أعد التحميل → عادت المطالبة

**كل بند يجب أن يمرّ.** أيّ إخفاق ⇒ الطوارئ ثم أبلغني.

---

## ما لا يُشغَّل صباحًا

| الملفّ | السبب |
|---|---|
| `org_admin_migration_RUNME.sql` | يحتاج موافقتك المنفصلة |
| أيّ `_ROLLBACK.sql` | فقط عند فشل مُثبَت |
| `enforcement_mode = 'enforced'` | **ليست قيمة مشروعة** في القيد أصلًا |
