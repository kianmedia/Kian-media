# SQL_MANUAL_AUDIT_PROGRESS — تقدّم المراجعة اليدوية

> 🔴 **لا يُدَّعى هنا مراجعة ملفّ لم يُقرأ فعلًا.**
> ⛔ ولم يُشغَّل أيّ ملفّ SQL. آخر تحديث: ٤ أغسطس ٢٠٢٦.

---

## ١. الأرقام

| البند | العدد |
|---|---|
| **إجمالي ملفّات SQL في `docs/`** | **٣٤١** |
| ملفّات v2.1 (`wave*`) | ٤٢ |
| ملفّات سابقة لهذا البرنامج | ٢٩٩ |
| **مراجَعة يدويًّا بقراءة المحتوى** | **٥٦** |
| — منها: الـ٩ `RUNME REQUIRED` (§٧ كاملة) | ٩ |
| — منها: مرافقاتها (PREFLIGHT/POSTCHECK/ROLLBACK) | ٢٧ |
| — منها: حزمة Wave 8 الرباعية | ٤ |
| — منها: ملفّات قديمة قُرئت لتحقيق تعارض | ٦ |
| — منها: بذور وتدقيقات v2.1 | ١٠ |
| **مفحوصة آليًّا بتحليل محتوى** (لا بالاسم) | **٢٩٩** |
| **متبقٍّ للقراءة الكاملة** | **٢٤٣** |

⚠️ **«مفحوصة آليًّا» ≠ «مراجَعة»:** الفحص الآليّ قرأ **محتوى** كل ملفّ (بعد تجريد
التعليقات والسلاسل) بحثًا عن أنماط محدّدة، ⛔ ولا يُغني عن القراءة البشرية.

---

## ٢. 🔴 لماذا المراجعة اليدوية ضرورية — دليل من هذه الجلسة

الفحص الآليّ رصد **٣٥ ملفًّا «مدمّرًا»** خارج ملفّات التراجع. **قُرئ منها اثنان،
وكلاهما إيجابيّة كاذبة:**

| الملفّ | ما رصده الفحص | الحقيقة بعد القراءة |
|---|---|---|
| `final_platform_acceptance_RUNME.sql` | `drop table` | `drop table if exists **pg_temp**.…` — جدول مؤقّت داخل تقرير **قرائيّ** |
| `asset_intelligence_RUNME.sql` | `truncate` | مُشغِّلات **تمنع** TRUNCATE + تعليق يشرحها — ⛔ لا اقتطاع |

⇒ **معدّل الإيجابيات الكاذبة في العيّنة المقروءة: ٢ من ٢.**
🔴 **ولذلك لم يُصنَّف أيّ ملفّ `DO NOT RUN` بناءً على الفحص الآليّ.**

---

## ٣. نتائج الفحص الآليّ عبر الـ٢٩٩ — مع تحفّظاتها

| الفحص | النتيجة | التفسير |
|---|---|---|
| `GRANT … TO PUBLIC` | **٠** | 🟢 نظافة صلبة عبر المستودع كلّه |
| `SECURITY DEFINER` بلا `search_path` مثبَّت | **٠** | 🟢 لا دالّة قابلة للاختطاف |
| ملفّات بلا أيّ كتابة (قرائيّة بالكامل) | **٩٣** | تُصنَّف READ-ONLY AUDIT / PREFLIGHT / POSTCHECK بثقة |
| بذور بيانات | **٠** خارج v2.1 | البذرتان الوحيدتان داخل v2.1 |
| عبارات مدمّرة خارج ROLLBACK | **٣٥** | ⚠️ **مرشَّحات للقراءة لا أحكام** (§٢) |

---

## ٤. 🔴 تعارضات واعتماديات مكتشَفة

**١. `wave3_production_ops_RUNME.sql` ليس مستقلًّا.**
لا يُنشئ جدولًا واحدًا — هو `ALTER` على `ops_call_sheets` و`ops_job_weather`.
**يشترط `operations_center_RUNME.sql` مطبَّقًا**، ويفرضه بحارس صلب يذكره بالاسم.

**٢. `wave6_case_study_generator_RUNME.sql` ليس مستقلًّا.**
`ALTER` على `cs_case_studies`. **يشترط `case_studies_platform_RUNME.sql`**.

**٣. ملفّا تراجع يهدمان أساس حزم v2.1:**

| الملفّ | يهدم | الأثر لو شُغِّل بعد v2.1 |
|---|---|---|
| `operations_center_ROLLBACK.sql` | `ops_call_sheets` · `ops_job_weather` · `ops_job_permits` | 🔴 يُسقط أساس `wave3_production_ops` و`wave3_permits_media` |
| `case_studies_platform_ROLLBACK.sql` | `cs_case_studies` | 🔴 يُسقط أساس `wave6_case_study_generator` |

⚠️ **ويبقيان مصنَّفَين `ROLLBACK`** — فهما يؤدّيان وظيفتهما المشروعة. لكن
**ترتيب التراجع معكوس**: يُتراجَع عن v2.1 **أوّلًا** ثمّ عن الأساس. ⛔ والعكس
يترك دوالّ تشير إلى جداول غير موجودة.

**٤. سطح `anon` اثنان لا واحد** (تصحيح للمصفوفة):
`prodops_calendar_feed` (تُعيد **محتوى**) و`crm_testimonial_invite_check`
(تُعيد `{ok:false}` أو `{ok:true}` فقط، ⛔ بلا `project_id`).

---

## ٥. التصنيفات — العدّ الحاليّ

| التصنيف | العدد | ملاحظة |
|---|---|---|
| **RUNME REQUIRED** | **١١** | + `custody_enterprise_incidents_RUNME.sql` (prerequisite لحزمة الامتثال) |
| **RUNME OPTIONAL** | ٢ | `wave8_push_tokens` · `kian_testimonials_v1` |
| **PREFLIGHT** | ٣٤ | |
| **POSTCHECK** | ٣٥ | |
| **ROLLBACK** | ٣٨ | منها ٢ بترتيب تراجع حرج (§٤-٣) |
| **READ-ONLY AUDIT** | ١٥ | |
| **DEVELOPMENT SEED** | ٢ | ⛔ لا تُشغَّل على الإنتاج |
| **STATIC TEST** | ٠ | الاختبارات الساكنة في `tests/` لا في `docs/` |
| **LEGACY / SUPERSEDED** | ٠ **مُثبَتة** | ⛔ لم أثبت خَلَفًا لملفّ بعينه بعد |
| **DO NOT RUN** | **٠** | 🔴 **ولا واحد بلا دليل** — انظر §٢ |
| **NEEDS MANUAL REVIEW** | **٢٤٣** | الباقي |

---

## ٦. الملفّ التالي بالضبط

**آخر ملفّ قُرئ:** `docs/asset_intelligence_RUNME.sql` (إيجابيّة كاذبة، §٢).

**الملفّ التالي:** `docs/authz_fixB_identity_permissions_RUNME.sql`
— ثلاث عبارات `delete from` ويمسّ الصلاحيات، فهو أعلى الـ٣٣ المتبقّية خطرًا.

**ثمّ بالترتيب:**
1. `docs/case_studies_platform_RUNME.sql` (٥ × `delete`، وهو **شرط مسبق** لحزمة v2.1)
2. `docs/mfa_write_gate_s4b_RUNME.sql` (٣ × `delete`، بوّابة كتابة)
3. `docs/kian_ai_assistant_RUNME.sql` (٤ × `delete`)
4. `docs/live_operations_dashboard_RUNME.sql` (٣ × `delete`)
5. باقي الـ٣٥ المرشَّحة، ثمّ ملفّات RLS والمُشغِّلات، ثمّ الأقدم.

---

## ٧. ما لا يدّعيه هذا الملفّ

- ⛔ **لم تُراجَع ٣٤١ ملفًّا.** قُرئ **٥٦** منها فعلًا، والباقي **٢٤٣** لم يُقرأ.
- ⛔ **ولم يُشغَّل ملفّ واحد.**
- ⛔ ولا `DO NOT RUN` بلا دليل — و«غير مراجَع» ≠ «آمن» و≠ «خطر».
- ⚠️ الأعداد في §٥ **مقيسة** من نظام الملفّات، لا مُقدَّرة.


---

## ٨. حسم تعارض `kian_testimonials` (٦ أغسطس ٢٠٢٦)

**التعارض:** المصفوفة تصنّف `kian_testimonials_v1_RUNME.sql` **RUNME OPTIONAL**،
بينما كان PREFLIGHT لـWave 4 يعامل جدولها كاعتماد **مطلوب**، وRUNME يربطه بمفتاح
أجنبيّ **غير مشروط** داخل الـDDL.

**القرار: OPTIONAL** — والتصنيف في المصفوفة كان صحيحًا، والخطأ في Wave 4.

**الدليل (من الاستخدام لا من الاسم):**
- الميزة خلف علم `NEXT_PUBLIC_SHOW_TESTIMONIALS` **مطفأ**.
- الاستخدام الوحيد كان **قيد مفتاح أجنبيّ**؛ ⛔ ولا دالّة تقرأ الجدول أو تكتب فيه.

**العلاج:** المفتاح الأجنبيّ صار **شرطيًّا وidempotent** داخل RUNME.
⇒ جدولٌ اختياريّ لم يعد يُفشل إنشاء جدول CRM ويحجب الحزمة كلّها.

## ٩. `crm_sales_FOUNDATION` — من NEEDS MANUAL REVIEW إلى RUNME REQUIRED

بوّابات `crm_can_manage()` و`crm_can_read_opportunity(uuid)` و
`crm_can_edit_opportunity(uuid)` تُستدعى داخل Wave 4 **بلا حارس وجود**.
⇒ prerequisite حقيقيّ، أُضيف رسميًّا إلى الترتيب بموضع **٤**، ورفاقه الثلاثة
(PREFLIGHT/POSTCHECK/ROLLBACK) موجودة.

⛔ **ولم تُنسخ البوّابات إلى Wave 4** — ذلك يصنع تعريفين متنافسين لنفس البوّابة.


## ١٠. `custody_enterprise_03_incidents_alerts_PATCH.sql` ⇒ **LEGACY / SUPERSEDED**

أوّل ملفّ يُصنَّف كذلك **بدليل**: حلّت محلّه حزمة
`custody_enterprise_incidents_{PREFLIGHT,RUNME,POSTCHECK,ROLLBACK}.sql`.

**السبب:** ثلاث معاملات مستقلّة (`begin`…`commit` ×٣) ⇒ فشلُ جزء لاحق يترك
الجداول مطبَّقة **بلا RLS ولا صلاحيات**؛ وقسم التحقّق `select` فقط فينتهي
بحالة خروج 0. وفيه عيبان أُصلحا: مُشغِّل الحجز على الإدراج وحده، وإمكان ربط
حادثة بأصل لا يخصّ الموظّف.
