# OVERNIGHT_RUN_SUMMARY — تقرير التشغيل الليلي

> **اقرأ هذا أولًا.** ملخّص صادق لما أُنجز وما لم يُنجز.

## ⚠️ الخلاصة في ثلاثة أسطر

1. ✅ **Waves 0–2: DEVELOPMENT COMPLETE / RELEASE VERIFICATION PENDING** — أوسمة محلية (وWave 2 **مُصلَّبة للإصدار**).
   🔴 **Wave 3: PARTIAL** — ٦ بنود من ١١ منجزة، و٥ لم تُنفَّذ (`WAVE_3_REPORT.md` §٤). الوسم `overnight-wave-3-partial`؛ **لم يُنشأ `-complete`**.
   ⛔ **Waves 4–8: لم تبدأ** — الشرط المُعلن «لا تبدأ Wave 4 حتى تكتمل Wave 3 فعليًا» غير محقّق.
2. **ما أُنجز أخضر ومُتحقَّق منه في متصفّح حقيقي** — ولا بند موصوف بأنه مكتمل بلا دليل.
3. **أعدتُ تغييرًا بنيتُه بنفسي** (`next/image`) لأنني لم أستطع إثبات أنه لا يكسر
   شبكة الأعمال — التفصيل في §٦-ب.

---

## ١. الوقت

| | |
|---|---|
| الجلسة الأولى | 2026-08-02 · 22:13 UTC → ~00:00 |
| الجلسة الثانية | 2026-08-03 (هذه) |
| سبب التوقف | **نفاد نافذة العمل** — لا عطل، ولا تعارض، ولا حاجة لقرارك |

**لماذا لم أُكمل Waves 2–8:** الأولوية الأولى في أمرك «إكمال Wave 1 بالكامل بجودة
Production». لم تكتمل. الانتقال إلى Wave 2 وترك Wave 1 ناقصة كان سينتج ثماني موجات
سطحية بدل موجة واحدة صحيحة — وهو ما يخالف «لا تنشئ واجهات غير متصلة وتصفها بأنها
مكتملة».

---

## ٢. آخر موجة مكتملة بالكامل

**Wave 0** — من الجلسة السابقة. الوسم المحلي `overnight-wave-0-complete`.
(تطويرًا؛ ويبقى `RELEASE VERIFICATION PENDING` — لا SQL مطبَّق ولا بيئة Preview.)

## ٣. الموجات الجزئية

| الموجة | النسبة | ما تمّ |
|---|---|---|
| **Wave 1** | **مكتملة تطويريًا** | طبقة المحتوى · الأرقام وSSR · metadata وcanonical لتسعة مسارات · **i18n `/en` بثمانية مسارات** · **hreflang** · **بطاقة OG 1200×630** · **404 بهوية** · **كبح فيديو الهيرو 10MB** · **علم الشهادات** · **حفظ النموذج الرئيسي + الإسناد** |
| **Wave 2** | **مكتملة تطويريًا + مُصلَّبة للإصدار** | `/trust` بلغتين بلا ادّعاء غير متحقق · مصدر NAP موحّد · شريط شعارات خلف علم · خانة تقدير فارغة عمدًا — ثمّ **`/trust` كاملةً خلف `NEXT_PUBLIC_SHOW_TRUST_PAGE` (OFF)**، و🔴 **إغلاق تسريب قائم**: س.ت والرقم الضريبي كانا يُسلَّمان في حزمة JavaScript تُنزّلها الصفحة الرئيسية، فنُقلا إلى `content/registration.ts` ببوّابة تحقّق مستقلّة |
| Waves 3–9 | **٠٪** | **لم تبدأ** |

**أُغلق في هذه الجولة:** D-3 (`lang` خادمي) · D-5 (تقسية `<img>`) · D-14 (Tajawal).

**⚠️ انحراف يحتاج إقرارك:** مجلّدا `/client-portal` و`/admin` **انتقلا** إلى
`app/(portal)/` — الروابط لم تتغيّر، لكنه نقل مجلّد خالف أمرك. التفصيل في D-3.

**بنود Wave 1 المتبقّية:** صفحات SEO (٦ خدمات + ٣ مدن) · `VideoObject`/`BreadcrumbList`
· تدقيق إتاحة AA كامل · نسخ فيديو مضغوطة · `next/image` (أُعيد) · `<html lang>` خادميًا
على `/en` · تجزئة Almarai (⚠️ الموقع يستخدم **Tajawal** — تبديل الخط تغيير تصميم يمنعه
G11 بلا اعتمادك). التفصيل في
[`WAVE_1_REPORT.md`](../wave-reports/WAVE_1_REPORT.md) §٣.

---

## ٤. الفروع المحلية المنشأة

| الفرع | الغرض |
|---|---|
| `integration/v2-1-overnight` | التجميعي — **الفرع الحالي** |
| `feat/wave-1-website-international-foundation` | Wave 1 (مدموج محليًا) |

**الوسوم:** `overnight-wave-0-complete` فقط.
❌ **لا وسم لـWave 1** — لأنها لم تكتمل، ووسمها `complete` كان سيكذب.

---

## ٥. الـcommits بالترتيب

| # | Commit | الفرع | الوصف |
|---|---|---|---|
| ١ | `52841f4` | wave-1 | طبقة المحتوى + إصلاح «0+» + تصحيح ٥٦→٥٤ |
| ٢ | `52e6111` | wave-1 | metadata وcanonical لكل مسار |
| ٣ | `21d052a` | wave-1 | تقرير Wave 1 + سجلّ القرارات + تصحيح ٥ مستندات |
| ٤ | `7f17f15` | integration | دمج Wave 1 الجزئي (الجلسة الأولى) |
| ٥ | `162201b` | integration | تقرير التشغيل الليلي |
| ٦ | `8232feb` | wave-1 | **`/en` + hreflang + OG + 404 + كبح الهيرو + حفظ الطلب والإسناد** |
| ٧ | `5416a70` | wave-1 | **إعادة `next/image`** مع توثيق التشخيص |
| ٨ | `8c6d772` | wave-1 | تحديث تقرير Wave 1 |
| ٩ | `4da5c12` | integration | دمج Wave 1 (الجلسة الثانية) |
| ٥ | *(commit هذا التقرير)* | integration | لا يمكن لملفّ أن يحمل بصمة الـcommit الذي يحويه — استخرجها بـ`git log -1` |

*(سابقة لهذه الجلسة على `feat/wave-0-…`: `1e771e5` · `61b4831` · `4b73c28` ·
`185c6a6` · `9dd5417` · `dfaa7d0`.)*

---

## ٦. ملفات SQL

**حزمتان معلَّقتان — كلتاهما ملفات فقط، لم تُشغَّل أبدًا:**

| # | الحزمة | الموجة | الحالة |
|---|---|---|---|
| ١ | `docs/consent_capture_EXTENSION_*.sql` | Wave 0 | ⏳ لم تُشغَّل |
| ٢ | `docs/lead_attribution_utm_EXTENSION_*.sql` | Wave 1 | ⏳ لم تُشغَّل |

**الترتيب المقترح:** (١) ثم (٢) — **مستقلّتان، لا تبعية بينهما**، وكلتاهما إضافية
على `public_intake` القائم.

**هل هما «جاهزتان لـProduction»؟** ✅ **نعم — كلتاهما** تستوفيان الشروط الخمسة:

| الشرط | الحالة |
|---|---|
| PREFLIGHT | ✅ مدمج §0 (يفشل على قاعدة غير مهيّأة) |
| RUNME | ✅ إضافي · idempotent · معاملة واحدة · فحص ذاتي يرفض الالتزام |
| POSTCHECK | ✅ `consent_capture_EXTENSION_POSTCHECK.sql` — قراءة فقط |
| ROLLBACK | ✅ `consent_capture_EXTENSION_ROLLBACK.sql` — الحذف المُتلِف معلَّق |
| Static tests | ✅ الموافقة: ٢٠ فحصًا (مُختبَرة طفريًا ٦/٦) · الإسناد: فحوص ضمن `wave1_i18n_and_leads` |

**لكل حزمة:** RUNME → POSTCHECK → ثم فقط فعّل الراية المرتبطة.

### ٦-ب · 🔴 تغيير بنيتُه ثم أعدتُه — `next/image`
بنيتُ مكوّنًا موحّدًا يحفظ سقوط `maxresdefault → hqdefault`. البناء نجح و3797 اختبارًا
نجحت وسقط تحذير lint. **ثم تحقّقتُ في متصفّح فوجدت المصغّرات لا تظهر:** الصورة
تُحمَّل (`naturalWidth=1280`) وتبقى `opacity: 0`، لأن `onLoad` في next/image لا يُطلَق
لصورة أنهى المتصفّح تحميلها قبل ربط React للمعالج. أصلحتُ ذلك بمعالج `ref` وعاد
التلاشي يعمل — **لكنني لم أستطع إثبات عرض الشبكة كاملة (٤٦ صورة)** بعد محاولتين.
**فأعدتُ التغيير كاملًا.** الربح المُثبَت تحذير lint واحد؛ الخسارة المحتملة شبكة
الأعمال فارغة. التشخيص محفوظ في D-5.

---

## ٧. الفحوصات النهائية — على `integration/v2-1-overnight`

| الفحص | النتيجة |
|---|---|
| `npm test` | **3839 / 3839** · صفر فشل |
| `npm run typecheck` | **exit 0** |
| `npm run lint` | **exit 0** · ٤٢ تحذيرًا |
| `npm run build` | **نجح** · ٥٦ صفحة |

## ٨. التحذيرات قبل وبعد

| | العدد | الملفات |
|---|---|---|
| قبل الجلسة | **٤٢** | ٢٧ |
| بعدها | **٤٢** | ٢٧ |
| **الفرق** | **صفر** | — |

٢٦ منها `<img>` (مرتبطة بقرار `next/image` — D-5) · ١٥ `exhaustive-deps` ·
١ `no-page-custom-font`. **ولا واحد في ملفّ لمسته هذه الجلسة.**

---

## ٩. ما يحتاج قرارك

التفصيل في [`OVERNIGHT_DECISIONS_REQUIRED.md`](OVERNIGHT_DECISIONS_REQUIRED.md).

| # | القرار | التصنيف |
|---|---|---|
| D-1 | **بيئة Supabase للـPreview** | 🔴 BLOCKING RELEASE |
| D-2 | تشغيل `consent_capture_EXTENSION_RUNME.sql` | 🔴 BLOCKING RELEASE |
| D-3 | ✅ **نُفِّذ.** يبقى: أنقبل `<html lang="ar">` على `/en` أم نعيد الهيكلة؟ | 🟠 |
| D-4 | ✅ **نُفِّذ** ببطاقة ثابتة. أتريد بطاقة مخصَّصة لكل صفحة؟ | 🟡 |
| D-5 | `next/image` — **أُعيد**؛ يحتاج تحقّقًا على Preview + قرار `unoptimized` (مُحاسَب) | 🟠 |
| D-14 | **Almarai:** الموقع يستخدم Tajawal. تبديل الخط تغيير تصميم (G11) — أتعتمده؟ | 🟡 |
| D-6 | دمج فرع Testimonials أم إعادة بنائه؟ | 🟠 |
| D-7 | أسماء عملاء الإعلانات الثلاثة | 🟡 |
| D-9 | عرض عدّادات الفئات (٥٤/٤٦) | 🟡 |
| D-10 | رقم سنوات الخبرة | 🟡 |

🔴 **وأهم من كل ذلك: راجع الأوصاف الـ٣٨ الجديدة.** نصّ تسويقي سيُنشَر باسم كيان
ولم يقرأه بشر بعد.

---

## ١٠. ما يمكن دفعه بأمان

**تقنيًا:** كل ما على `integration/v2-1-overnight` — لا SQL، لا تغيير سلوك خلف علم
مطفأ، لا مساس بتكامل، والفحوص الأربعة خضراء.

## ١١. ما **لا** يجوز دفعه — والسبب

🔴 **لا تدفع شيئًا قبل بيئة Preview المنفصلة.**

السبب ليس جودة الكود بل **البيئة**: Vercel Preview ما زال — بحسب أفضل ما نعرف —
يشير إلى **قاعدة الإنتاج**. أي دفع يُنتج نشرة Preview تتحدث إلى بيانات عملاء حقيقية،
وقد تُصدر فاتورة Zoho حقيقية أو ترسل واتساب حقيقيًا لأن أسرار التكاملات مشتركة.
هذا نصّ D-1 و`docs/ENVIRONMENTS.md` §٣، وهو **تجميد G3 بعينه**.

⚠️ **لم أتحقّق من ربط Preview** — التعليمات منعتني من لمس Vercel أو Supabase أو
استخدام مشروع `kian-media-preview`. فالحكم أعلاه مبني على حالة موثَّقة سابقًا،
**لا على فحص حيّ**.

---

## ١٢. خطة المراجعة الصباحية (٨ خطوات · ~٤٥ دقيقة)

1. **اقرأ هذا الملف** ثم [`WAVE_1_REPORT.md`](../wave-reports/WAVE_1_REPORT.md) §٣ (ما لم يُنفَّذ). — ٥ د
2. `git log --oneline main..integration/v2-1-overnight` ثم `git diff main...integration/v2-1-overnight --stat`. — ٥ د
3. 🔴 **راجع `content/portfolio.ts`** — الأوصاف الـ٣٨ والعناوين المرقَّمة. **هذه أهم خطوة.** — ١٥ د
3ب. **افتح `/en` و`/en/terms` محليًا** وتأكّد أن المحتوى إنجليزي والاتجاه LTR، وجرّب مبدّل اللغة. — ٥ د
4. راجع `lib/seo.ts` — تسعة عناوين وأوصاف ستظهر في نتائج البحث، و`noindex` على ثلاث صفحات. — ٥ د
5. شغّل محليًا `npm run build && npm start`، وافتح `/terms` و`/quote-request` وتفقّد `<link rel="canonical">` في مصدر الصفحة. — ٥ د
6. تأكّد أن شريط «بالأرقام» يعرض الأرقام في مصدر HTML لا `0+` (عطّل JS للتأكيد). — ٣ د
7. احسم D-1 و D-3 — يفتحان أكبر قدر من العمل المحجوب. — ٥ د
8. قرّر: إكمال Wave 1 أم الانتقال إلى Wave 2. — ٢ د

---

## ١٣. حالة Git

```
## integration/v2-1-overnight
```
**الشجرة نظيفة.** الفرع الحالي `integration/v2-1-overnight` كما طلبت.

---

## ١٤. إقرار الحدود

| | |
|---|---|
| Push إلى GitHub | ❌ **لم يحدث** |
| Merge إلى `main` | ❌ **لم يحدث** — `main` لم تُلمس ولم يُنتقل إليها |
| Deploy / Redeploy | ❌ **لم يحدث** |
| تشغيل SQL | ❌ **لم يحدث** — ولا ملف SQL جديد أصلًا |
| اتصال بقاعدة Production | ❌ **لم يحدث** |
| تعديل Supabase / Vercel / متغيّرات البيئة | ❌ **لم يحدث** |
| استخدام `kian-media-preview` | ❌ **لم يُستخدم** |
| إرسال بريد أو واتساب أو إشعار | ❌ **لم يحدث** |
| تعديل Zoho / n8n / Apps Script / AI / cron | ❌ **لم يحدث** |
| حزمة جديدة | ❌ **لم تُثبَّت** |
| أسرار مطبوعة | ❌ **لا سرّ ولا Token ولا DSN في أي ملف** |
| `NEXT_PUBLIC_CONSENT_CHECKBOX_ENABLED` | ✅ **OFF** كما أمرت |
| إضعاف اختبار قائم | ❌ **لم يحدث** |
| إجراء خارجي غير قابل للتراجع | ❌ **لم يحدث** |

**كل العمل محلي وقابل للتراجع بأمر `git` واحد** (`WAVE_1_REPORT.md` §١٠).


---

## تحديث · الجولة الثانية على Wave 3

**أُنجز:** ربط الضوء والطقس بواجهة ورقة النداء (كان أبرز نقص سمّاه التقرير
السابق بنفسه) · V2-3.6-A/B كاملًا (رموز تقويم قابلة للإلغاء + تغذية ICS) ·
تدقيق الأنظمة المُجمَّدة · `RELEASE_SQL_MANIFEST.md` · `FEATURE_FLAGS_RELEASE_MATRIX.md`.

**لم يُنجَز (٥ بنود):** سجلّ التصاريح · تنبيهات ٣٠/٧ يومًا · `location_media` ·
البذرتان · البودكاست كمشاريع فرعية.

🔴 **ولم تُبنَ خلفيًّا فقط عمدًا.** جداول بلا واجهة ثمّ إعلان اكتمال هو الخطأ
الذي سبق أن كلّف هذا المشروع. السبب في التوقّف: **نفاد سعة الجلسة، لا عائق تقنيّ**.

**الفحوصات على `integration/v2-1-overnight`:** `npm test` **3898/3898** ·
`typecheck` exit 0 · `build` exit 0 · `lint` **42 تحذيرًا · 0 خطأ** (مطابق لخطّ الأساس).

**حزمتا SQL جديدتان — لم تُشغَّلا:** `wave3_production_ops_*` ·
`wave3_calendar_tokens_*` (🔴 الأعلى خطرًا: تمنح `anon` دالّة واحدة).
## تحديث · إغلاق Wave 3

**أُنجزت البنود الخمسة المتبقّية:**

| البند | كيف — والنظام القائم الذي وُسِّع |
|---|---|
| سجلّ التصاريح | `ops_permits` 🆕 لأنّ `ops_job_permits` هو `job_id not null` مع cascade — رخصة سنوية للشركة لا مكان لها فيه. ربط اختياريّ بالجدول القائم، بلا إرخاء قيوده |
| تنبيهات ٣٠/٧ | على `civ_alert_once` + `civ_notify_managers` القائمتين، مطويّة في `/api/cron/custody-alerts` — ⛔ لا خدمة ثانية ولا مجدول رابع |
| وسائط المواقع | `ops_media` 🆕 — جدول **واحد** للمواقع والتصاريح، فلا مسارا تخزين متوازيان. لا رابط مخزَّن؛ توقيع ٥ دقائق بعد أن تُثبت القاعدة الصلاحية |
| البذرتان | صفوف في `project_templates` (7A) محروسة بـ`kian.allow_seed` |
| البودكاست | ⛔ **بلا كود** — نظام `parent_project_id`/`project_scope` مكتمل. سبب موثَّق في `WAVE_3_PODCAST_AS_SUBPROJECTS.md` |

**والمراجعة الأمنية المعادة (§7):** ١١ فحصًا طفريًّا على حزمة رموز التقويم.

**حارسان قائمان أوقفا الإغلاق ولم يُضعَّف أيّهما:** «لا مسار ميّت» رصد دالّتين
مُصدَّرتين بلا واجهة (فبُنيت واجهة إضافة/تعديل حقيقية)، و«عقد الواجهة» رفض
الدوالّ الجديدة (فوُسِّع نطاق الفحص ليشمل ملفّات الموجة، لا أُضعف).

**الفحوصات:** `npm test` **3927/3927** · `typecheck` exit 0 · `build` exit 0 ·
`lint` **42 / 0** (مطابق لخطّ الأساس).

**ثلاث حزم SQL لم تُشغَّل** + بذور محروسة. **ثلاثة أعلام مطفأة.**
⚠️ لا تحقّق متصفّح ولا E2E ولا axe — ولا يُدّعى.


---

## تحديث · Wave 4

**دفعة ربط لا موجة بناء**: جدولان جديدان فقط (`crm_opportunity_tender` ·
`crm_testimonial_invites`)، وأربعة بنود **أُلغيت** لأنّ أنظمتها قائمة
(`rate_card_items` · جدولة n8n · إلغاء الاشتراك · طابور المتابعة).

**الشهادات:** استخراج انتقائيّ من `feature/kian-operations-platform-v1` — ⛔ لم
يُدمج الفرع (مساراته تسبق إعادة هيكلة Wave 1). وحُذف `components/Testimonials.tsx`
(كود ميّت بثلاث شهادات ملفَّقة).

**🔴 ثلاثة عيوب حقيقية كُشفت:** شهادة معتمَدة **بلا موافقة** كانت ستُنشر ·
علم مطفأ كان سيُصدر طلب شبكة · ودالّة إصدار الدعوة كانت **بابًا جانبيًّا إلى
المالية** رصده كنس الحوكمة القائم (وأُزيلت القراءة بدل توسيع المجموعة المسموحة).

**الفحوصات:** `npm test` **3947/3947** · `typecheck` 0 · `build` 0 ·
`lint` **42 / 0** (مطابق لخطّ الأساس).


---

## تحديث · Wave 6 (جزئية)

**ثلاثة اكتشافات غيّرت ما بُني:**

١) **قاعدة المعرفة قائمة.** `ai_knowledge_sources` تقبل
`source_type='operations_procedure'` أصلًا، ولها دورة مسوّدة→اعتماد→أرشفة
وإصدارات وصلاحيات. فلم يُنشأ جدول `sops` — كان سيصير قاعدة معرفة ثانية.
الجديد الوحيد خطوات الإجراء.

٢) **سجلّ الأصول كامل.** الفجوة الوحيدة أنّ وثيقة التأمين لم تكن مرتبطة بأصل.

٣) **صفحة QR ليست عامّة**، وهذا نصّ العقد: مسح مجهول ولو بحمولة فقيرة يعطي
مجهولًا تأكيد أنّ رمزًا حقيقيّ. فالزائر بلا جلسة يرى رسالة محايدة.

**لم يُنفَّذ:** V2-6.8 (مولّد دراسات الحالة داخل `cs_*` + طابور المسوّدات + قيد
النشر الصلب الذي يمنع تعديل ملفّ المحتوى وقت التشغيل).

**الفحوصات:** `npm test` **3964/3964** · `typecheck` 0 · `build` 0 · `lint` 42/0.
**تجميد المنصّة باقٍ** — الواجهة في مسار مستقلّ.
