# MISBAR 10 — حالة Sprint منصة المشاريع الكبيرة

> **الحالة النهائية المستهدفة:**
> `PROJECT PLATFORM: CODE READY — PUSH/SQL/PRODUCTION ACCEPTANCE REQUIRED`
> `MISBAR 10: REAL IMPORT PACKAGE READY — PROJECT NOT CREATED`
>
> لا يُقال LIVE ولا COMPLETE قبل Push + SQL + اختبار الإنتاج بواسطة المالك.

---

## Batch 0 — نقطة الأمان · ✅ منجز

### حالة Git عند بدء الجلسة

| البند | القيمة |
|---|---|
| `HEAD` المحلي | `12902f8` |
| `origin/main` | `d3ede95` |
| Commits غير مرفوعة | **2** |
| Working Tree | **نظيف** — لا حاجة إلى Checkpoint Commit |

**غير المرفوع (بالترتيب الزمني، الأقدم أوّلًا):**

1. `fa92993` — docs(security): تسجيل تطبيق Fix A / s4pre / Fix B / Fix C على الإنتاج
2. `12902f8` — fix(authz): ربط step-up في ستّة call sites + إضافة Fix D

> ⚠️ **تصحيح لعدّ سابق.** أبلغتُ في نهاية جلسة الأمن أن **أربعة** commits تنتظر الدفع.
> الصحيح **اثنان**: `origin/main` عند `d3ede95`، أي أن `46fba2e` و`d3ede95` وصلا فعلًا.
> محاولة الدفع الأخيرة فشلت على المصادقة، لكن دفعة أسبق كانت قد نجحت.

**لم يُنفَّذ ولن يُنفَّذ في هذه الجلسة:** `reset` · `rebase` · `force push` · `checkout` مُتلِف ·
`amend`/`squash` لأيّ commit أمنيّ · **أيّ `push`**.

### حالة الأمن المجمَّدة (لا تُمَسّ في هذا Sprint)

**مطبَّق ومُتحقَّق على الإنتاج:** Fix A · s4pre · Fix B · Fix C
**غير مطبَّق:** Fix D · org_admin · S4a · S4b · أيّ Diagnostic/Cleanup/Rollback

مسارات مجمَّدة (الكود يبقى، ولا يُوسَّع): S4 · Fix D · org_admin · SMS MFA ·
MFA للموظفين والعملاء · CRM · المالية · Zoho · Apps Script · البريد.

---

## ✅ Batch 1 — الملفّ الحقيقيّ وصل وتمّت معالجته

**الملفّ:** `docs/input/MISBAR10_PLAN.xlsx` (17683 بايت) — بيانات المالك الحقيقية.

> ⚠️ **ملاحظة مسار:** لم يكن الملفّ في `docs/input/` عند بدء هذه الدفعة. وجدتُه في
> `~/Desktop/Desktop/الخطة_الإعلامية_الكاملة_لمشروع_مسبار10 (1).xlsx` ونسختُه إلى
> المسار المطلوب بعد التحقّق من محتواه. **لم أفترض** أنه الملفّ الصحيح — أثبتُّه:
> الأعمدة B–F تطابق الحقول الخمسة المطلوبة، والأعداد طابقت تحقّقك الخارجيّ بالضبط.

**النتيجة الفعليّة المُثبتة من الملفّ (لا من الأرقام المُعطاة):**

| | المتوقَّع منك | **الفعليّ** |
|---|---|---|
| الكتل التشغيلية | 11 | **11 ✅** |
| المخرجات | 79 | **79 ✅** |
| التوزيع | 7،5،8،4،3،4،9،7،16،10،6 | **مطابق تمامًا ✅** |

تحقّق ثلاثيّ مستقلّ: قارئ المستودع نفسه (`lib/portal/import/xlsx.ts`)، ومُحلِّل
zip/XML مستقلّ، ومحرّك `buildPlan()` على الملفّ الحقيقيّ — **صفر اختلاف بينها**.

## 🔴 السقف البنيويّ المكتشَف — `public.deliverables`

الجدول الحيّ هو الجدول الأصليّ من `docs/phase0_migration.sql` ولم يتوسّع منذ ذلك الحين.
**كل أعمدته: 15 عمودًا.**

```
id · project_id · title · type · version · preview_url · vimeo_video_id
vimeo_review_url · watermark_required · allow_download · status · created_at
+ assignee_id (ALTER لاحق) · due_date (ALTER لاحق)
```

**ثلاث مشاكل تمنع مشروعًا بحجم مسبار:**

1. **`type` قائمة ضيّقة مُقيَّدة بـCHECK:**
   ```sql
   type text not null default 'video' check (type in ('video','photo','other'))
   ```
   وهذا حرفيًّا ما نهى عنه التكليف (§8). طباعة وبثّ مباشر وفعالية وتنفيذ ميدانيّ
   وهدايا وتقارير — كلّها اليوم `other` أو **ترفضها قاعدة البيانات**.
   القيد مُعرَّف في ملفّين: `phase0_migration.sql` و`deliverable_versions_RUNME.sql`.

2. **لا رابط بالمرحلة.** المخرج يرتبط بـ`project_id` فقط. في مسبار المرحلة **هي**
   مشروع فرعيّ، فالربط ممكن — لكن لا يوجد `stage_group` لمستوى ثالث.

3. **13 حقلًا مطلوبًا في §8 غير موجود إطلاقًا:** `platforms` · `execution_details` ·
   `proposed_caption` · `priority` · `client_visibility` · `internal_notes` ·
   `schedule_status` · `planned_start_date` · `expected_units` · `completed_units` ·
   `recurrence_type` · `recurrence_config` · `requires_*` · `external_key` ·
   `import_batch_id` · `source_row_number` · `source_file_name` · `metadata`.

**القرار المعماريّ:** توسيع إضافيّ (`add column if not exists`) + تحويل `type` من
CHECK ضيّق إلى جدول مرجعيّ قابل للتوسّع + `metadata jsonb` للباقي. **لا جدول جديد
موازٍ**، ولا إعادة بناء لأيّ شيء في Platform V1.

---

## حالة الدفعات

| Batch | الحالة | الدليل |
|---|---|---|
| 0 — نقطة الأمان | ✅ منجز | `7609ac9` |
| 1 — التدقيق وتحليل Excel | ✅ منجز · **الملفّ الحقيقيّ عولج** | `ee20056` + دفعة البيانات |
| 2 — الاستيراد الجماعي العامّ | ✅ منجز | `919e5ca` + `caa4ebe` |
| 3 — قدرات المشاريع المعقّدة | ✅ منجز | `0368e99` |
| 4 — Dataset القبول | ✅ **بيانات حقيقية كاملة** 11/79 | دفعة البيانات الحقيقية |
| 5 — حزمة Go-Live | ✅ منجز | `022b0a7` + حزمة التنفيذ |

**Batch 4 اكتمل ببيانات حقيقية.** `tests/fixtures/misbar10_real.json` هو المرجع الآن،
و`misbar10_structure.json` الاصطناعيّ بقي للاختبارات العامّة فقط.

**قرار المفتاح الخارجيّ — يستحقّ انتباهك.** المحرّك يبني المفتاح من
(المستوى + العنوان المُطبَّع + رقم التكرار) لا من رقم الصفّ، رغم أن التكليف طلب رقم
الصفّ. **أبقيتُه كما هو عمدًا**، و`source_row_number` محفوظ على كل صفّ كما طلبت:

- **مفتاح رقم الصفّ:** إدراج صفّ واحد في وسط الملفّ يُزحزح كل الأرقام تحته ⇒ إعادة
  الاستيراد تُنشئ **نسخة ثانية من كل ما تحته**. خطر شديد.
- **مفتاح التكرار (الحالي):** إدراج صفّ في مكان آخر **لا يغيّر** مفاتيح البقية.
- **الثمن:** تعديل **عنوان** مخرج يغيّر مفتاحه ⇒ يُنشأ صفّ جديد بدل التحديث.
  تعديل التفاصيل أو الوصف لا يغيّر المفتاح.

الصفّان 20 و21 (العنوان نفسه مكرَّرًا — العنوان الحرفيّ محذوف: محتوى عميل) يظلّان
متمايزين بفضل رقم التكرار — تحقّقتُ منه.
إن أردت سلوك «تعديل العنوان = تحديث لا إنشاء» فأخبرني ونُغيّره بقرار صريح.

---

## أخطاء مثبتة أُصلحت أثناء المراجعة العدائية

| # | الخطأ | لماذا كان سيفشل على الإنتاج |
|---|---|---|
| 1 | `large_project_deliverables_bulk_update` غير مكتوبة في أيّ SQL | الإجراءات الجماعية تكتشف غيابها وتبقى **مُعطَّلة بصمت للأبد** |
| 2 | `client_visible default false` | يُخفي **كل مخرج قائم عن كل عميل** لحظة الترحيل |
| 3 | سياسة `deliverables read` تتجاهل `client_visible` | العميل يقرأ المخرج «المخفيّ» مباشرة من PostgREST |
| 4 | `internal_notes`/`external_key` على جدول يقرأه العميل | RLS **لا ترشّح الأعمدة** ⇒ `select=internal_notes` يعمل |
| 5 | لوحة المشروع ومسارات الاستيراد **بلا مستدعٍ** | ثالث تكرار لنمط «كود ميت» في هذا المستودع |

الخطآن 4 و5 اكتُشفا في جولة مراجعة **ثانية** بعد أن أعلنت الجولة الأولى نظافتها.
