# KIAN PLATFORM — MASTER EXECUTION BRIEF v2.1 (APPROVED — REALITY-RECONCILED)

> ## ✅ حالة هذا المستند
> **معتمَد.** هذا هو **المرجع التنفيذي الوحيد** لمنصة كيان اعتبارًا من 2026-08-02.
> اعتمده خالد صراحةً عند GATE A، ويحلّ محل v2.0 بالكامل.
>
> **v2.0 مؤرشَف بلا تعديل** في
> [`docs/archive/MASTER_BRIEF_v2.0.md`](docs/archive/MASTER_BRIEF_v2.0.md)
> (`sha256 0cb5bbdc…9c2e7` — مطابق بايت ببايت للملف الذي سُلِّم).
> يُرجَع إليه للمرجعية التاريخية فقط. ❌ **لا يُنفَّذ منه شيء.**
>
> **الأساس:** v2.0 (170 سطرًا) + `MASTER_ORDER_FINAL.md` (Rev.2) +
> تدقيق واقع المستودع على `HEAD 7b92391` (2026-08-02).
> **المستندات المرافقة:** [`EXISTING_CAPABILITIES.md`](docs/EXISTING_CAPABILITIES.md) ·
> [`DATABASE_APPLICATION_STATUS.md`](docs/DATABASE_APPLICATION_STATUS.md) ·
> [`PROTECTED_ARCHITECTURE.md`](docs/PROTECTED_ARCHITECTURE.md) ·
> [`OVERLAP_DEDUP_MATRIX.md`](docs/OVERLAP_DEDUP_MATRIX.md) ·
> [`V2_1_CHANGELOG.md`](docs/V2_1_CHANGELOG.md)
>
> **هذه المسودة مكتفية بذاتها:** كل قواعد `MASTER_ORDER_FINAL.md` مدمجة نصًا داخل
> أقسامها، فلا يحتاج القارئ إلى الرجوع إلى أمر التدقيق.

> **بروتوكول اللغة:** التقارير والأسئلة لخالد → عربي. الكود والتعليقات والـcommits →
> إنجليزي. واجهة المستخدم → عربي أولًا RTL (+ إنجليزي حيث تضيفه Wave 1).

---

## 0. لماذا وُجدت v2.1 — الفرق الجوهري عن v2.0

v2.0 كُتب بافتراض أن الغالبية ستُبنى. **تدقيق الواقع أثبت العكس:**

| المقياس | الرقم |
|---|---|
| متطلبات v2.0 بعد التفكيك الذرّي | **174** |
| مبنية كليًا (`REMOVE — DUPLICATE`) | **38** |
| تُدمج في نظام قائم (`MERGE INTO EXISTING`) | **35** |
| موجودة جزئيًا (`VERIFY & EXTEND`) | **36** |
| **بناء جديد حقيقي (`KEEP — NEW`)** | **50 (28.7%)** |
| تحتاج قرار خالد | **14** |
| مؤجَّلة | **1** |

**الواقع المُقاس:** 421 جدولًا · 1,662 دالة · 258 تفعيل RLS · 271 جدولًا بسياسات ·
239 ملف اختبار · 292 ملف SQL · CI فعّال · تطبيق Expo قائم.

**ثلاث حقائق تُعيد ترتيب كل شيء:**
1. **Waves 3/4/5/7 مبنية بنسبة 70–93%** — تتحول من «تنفيذ» إلى «تحقق ودمج وحسم ازدواج».
2. **Wave 0 هي الموجة الوحيدة التي تكاد تكون كلها بناءً جديدًا (71%)** — وهي بالضبط
   موجة السلامة: **لا نسخ احتياطي، لا مراقبة، لا بيئة Preview منفصلة، لا SPF/DKIM/DMARC.**
3. **الخطر الأكبر لم يعد النقص — بل الازدواج.** المستودع يحمل **10 ازدواجات موروثة**
   (D-1…D-10) قبل v2. تنفيذ v2.0 حرفيًا كان سيرفعها إلى ≥20.

---

## 1. الحالة المُتحقَّقة (فحص خارجي مؤرَّخ 2026-08-02 + تحقّق بالكود)

### ✅ حيّ على Production
- أوصاف أعمال فريدة في قسم الشركات — **8 أعمال تحمل `dAr`** (لا 10 كما قدّر v2.0).
  🔒 **تُحفظ ولا تُعاد كتابتها.**
- رقم جوال ثانٍ (+966543553038).
- جملة موافقة **ضمنية** في النموذج الرئيسي (`components/Contact.tsx:234`).
- نموذج `/quote-request` غني ويتضمن «كيف تعرفت علينا».
- ترويسات أمان حيّة (`next.config.js:14-60`) وRate limiting مطبَّق (`42501` مؤرَّخ 2026-07-27).
- `sitemap.xml` · `robots.txt` · `manifest` · PWA · MFA (دورة المالك بخطوتين مؤكَّدة).

### ❌ غير حيّ (مُتحقَّق بالكود)
| الملاحظة | الدليل في الكود |
|---|---|
| العدّادات `0+` في HTML الأولي | `components/Counter.tsx:5` — `useState(0)` |
| قسم تقييمات فارغ ظاهر | `components/Reviews.tsx` مركّب في `app/page.tsx:47` |
| title/canonical موحّد لكل المسارات | `app/layout.tsx:44` — الاستثناء `/case-studies` فقط |
| لا consent checkbox في أي نموذج | grep على النماذج الأربعة |
| مجموع فلاتر الفئات مقابل المعروض | **56 مقابل 46** (لا 54) — والفجوة **بالتصميم**: العمل ينتمي لعدة فئات عمدًا |
| أوصاف مكرّرة | **38** عملًا بلا `dAr` (لا ~36) |
| 3 أعمال باسم «إعلان قصير» | `components/Portfolio.tsx:109,111,113` |
| النموذج الرئيسي → واتساب فقط بلا حفظ | `components/Contact.tsx:30` |
| OG image شعار مربّع 800×800 | `app/layout.tsx` + `public/logo.png` |
| الفوتر `info@` فقط | `components/Footer.tsx:46` مقابل `Contact.tsx:154-155` (`info@` **و**`sales@`) |
| **لا `not-found.tsx`** (404 غير مخصصة) | `find app -name 'not-found*'` → لا شيء |

### 🔒 قاعدة الواقع (تحكم عند أي تعارض)
> **Reality wins.** المستودع والـmigrations والأنواع وRPCs وسياسات RLS والخدمات والأعلام
> وملاحظات الإنتاج المُتحقَّقة هي مصدر الحقيقة. **لا تُعِد بناء ما هو موجود — أكمِله أو
> أصلحه أو انشره.** عند تعارض جوهري لا يُحسم من الكود: **توقّف واسأل خالد.**

### ⛔ «موجود» ≠ «مطبَّق»
**5 ملفات فقط من 292 لها دليل تطبيق مؤرَّخ على Production.** لا `supabase/migrations`
ولا سجل تطبيق آلي. الحالة الافتراضية لكل ملف: `APPLICATION STATUS REQUIRES KHALED
CONFIRMATION`. راجع [`DATABASE_APPLICATION_STATUS.md`](docs/DATABASE_APPLICATION_STATUS.md).

---

## 2. الضوابط — G1…G14 (معاد صياغتها على الواقع)

> ⚠️ **أربعة ضوابط في v2.0 تتعارض مع الواقع تعارضًا صلبًا (G2/G6/G7/G8).** إبقاؤها
> حرفيًا يعطّل أنظمة إنتاجية. صياغتها هنا **تحفظ نيّتها وتُسقط استحالتها** — وكلها
> تحتاج تأكيد خالد في GATE A.

- **G1 — الفروع:** فروع ميزات فقط `feat/wave-<n>-<slug>`. ❌ لا مساس بـ`main` مباشرة.
  الفروع القائمة (34) لا تُعاد تسميتها.

- **G2 — قاعدة البيانات (معاد صياغته):** ❌ **لا تُنفَّذ SQL على أي قاعدة إطلاقًا.**
  تغييرات المخطط = ملف قابل للتشغيل + ملف تراجع مقابل، **بعُرف المستودع القائم
  `docs/*_RUNME.sql` + `docs/*_ROLLBACK.sql`** — ❌ **لا يُنشأ نظام migrations ثانٍ في
  `supabase/migrations/`** بينما 292 ملفًا تعيش في `docs/`. كل حزمة: `PREFLIGHT` (تحقق
  اعتماديات) → `RUNME` (transactional، idempotent، بلا `CONCURRENTLY`، بفحص ذاتي يرفض
  الالتزام عند اختلال الشرط) → `POSTCHECK` (قراءة فقط، مجموعة نتائج واحدة) →
  `ROLLBACK`. **توقّف للتطبيق اليدوي من خالد، ثم استأنف بعد تأكيده.**
  ❓ *تأكيد GATE A مطلوب.*

- **G3 — تجميد الدفع:** commits محلية مسموحة. **لا Push** حتى يؤكد خالد أن متغيرات
  بيئة Vercel Preview تشير إلى مشروع Supabase **منفصل غير إنتاجي**. بعد التأكيد:
  الدفع لفروع الميزات مسموح؛ الدمج والنشر يحتاجان «أعتمد» صريحة دائمًا.
  ⚠️ *الواقع: `main` متطابق مع `origin/main` ⇒ الدفع يحدث. حالة التجميد تحتاج حسمًا.*

- **G4 — RLS:** كل جدول جديد يُشحن بـRLS مفعّلة وسياسات **deny-by-default** في نفس
  الملف. اختبار كل دور **إيجابيًا وسلبيًا**.

- **G5 — الأسرار (سياسة ثلاثية صلبة):** ❌ لا service-role key في العميل · ❌ لا أسرار
  في جانب المتصفح · ❌ **لا عرض ولا تسجيل لأي Secret أو Token أو DSN أو كلمة مرور في
  أي ملف أو تقرير.** يُذكر **اسم المتغير وحالته فقط**: `Configured` / `Missing` /
  `Unknown`. التسجيل يمرّ عبر `pgRedact()` الذي يحذف JWT والمفاتيح والبريد والـUUID
  و**الـURL كاملًا** (يحمل قيم الفلاتر أي بيانات حقيقية).

- **G6 — الأعلام (معاد صياغته):** ❌ **لا يُنشأ `lib/flags.ts`** كمصدر ثالث. الأعلام
  تعيش اليوم في **18 جدول `*_settings`** و**~20 متغير بيئة**. المطلوب **سجل أعلام
  موحّد واحد** يوثّق الاثنين. لكل علم: **Owner · Default state · Activation steps ·
  Rollback steps · Removal date.** الميزات الكبيرة وتغييرات الـworkflow خلف أعلام؛
  التغييرات الخطرة تُفعَّل تدريجيًا؛ **إصلاحات الأمان الحرجة لا تبقى معطلة بعد
  اعتمادها**؛ Bug fixes المُعيدة للسلوك الصحيح لا تحتاج دائمًا علمًا؛ **أعلام على
  مستوى الموجة** بدل عشرات الأعلام الصغيرة. **المبدأ الثابت: مع إطفاء أعلام الميزات
  الكبيرة، تجربة الموقع والبوابة الحالية لا تتغير.**

- **G7 — التكاملات القائمة (معاد صياغته — يلغي صياغة v2.0):**
  > **“Do not add, replace, disable or alter the existing Zoho integration. Preserve
  > existing code paths and flags. Any Zoho expansion requires a separate approved brief.”**
  > **ونفس القاعدة حرفيًا تسري على AI Assistant وWhatsApp.**

  **السبب:** v2.0 يمنع «AI features · WhatsApp sending/automation · Zoho API calls» —
  **والثلاثة مبنية ومشحونة:** `ai_*` (15 جدولًا + 2,214 سطر SQL + `/assistant`) ·
  `whatsapp_*` (14 جدولًا + `lib/whatsapp/*` + مسار إرسال) · Zoho (7 مسارات API +
  6 وحدات خدمة + cron يومي). **المنع مستحيل؛ التجميد ممكن.**
  ❌ **يبقى ممنوعًا فعلًا:** مدفوعات إلكترونية · نفاذ · مشغّل مراجعة دقيق الإطار
  (محجوب على قرار Stream vs Mux) · **أتمتة WhatsApp جديدة** · نظام AI ثانٍ.
  ❓ *تأكيد GATE A مطلوب.*

- **G8 — الجدولة (معاد صياغته):** ❌ **لا cron إضافي.** ثلاث مهام Vercel قائمة وتعمل:
  `/api/cron/custody-alerts` 03:00 · `/api/cron/notify-email` 03:10 ·
  `/api/cron/zoho-sync` 03:20. أي جدولة جديدة **تُطوى داخل هذه الثلاث** أو تمرّ عبر
  n8n webhook (`/api/hooks/*` بترويسة سر مشترك) أو GitHub Actions `workflow_dispatch`.
  السبب: سقف خطة Vercel Hobby. ❓ *تأكيد GATE A مطلوب.*

- **G9 — الواقع يفوز:** عند اختلاف المستودع عن الوصف: تكيّف بأقل تغيير وسجّل الانحراف.
  التعارض الجوهري → **توقّف واسأل**. *(هذا الضابط هو الذي يحسم G2/G6/G7/G8.)*

- **G10 — الواجهة:** عربي أولًا RTL؛ حالات فارغة مصمَّمة على كل قائمة.

- **G11 — انضباط التصميم:** **تلميع لا إعادة تصميم.** لغة العلامة السينمائية: حركة
  بطيئة واثقة (300–500ms ease)، لوحة ألوان مضبوطة، Almarai. ❌ لا إعادة كتابة تخطيط
  لأي قسم قائم بلا اعتماد صريح — التغييرات تُعبَّر عنها كـtokens أو صقل.

- **G12 — ميزانية الاعتماديات:** كل حزمة جديدة مبرَّرة في تقرير موجتها (الحجم،
  الصيانة، البديل المدروس). *الحزم المقترحة وغير المثبَّتة: `next-intl` · `@vercel/og` ·
  `suncalc` · `@sentry/nextjs` · `playwright`.*

- **G13 — النظام القائم أولًا (جديد — القاعدة الحاكمة للازدواج):** قبل اقتراح أي
  جدول/RPC/خدمة/دور جديد:
  1. ابحث عن النظام القائم الذي يغطي المجال. 2. وثّق جداوله وخدماته وواجهاته.
  3. إن غطّى **≥60%** من المتطلبات **وجب توسيعه**. 4. الحقول شديدة التخصص →
  **Extension Table** مرتبط بالسجل القائم. 5. ❌ **يُمنع مصدر بيانات موازٍ لنفس
  الحقيقة.** 6. ❌ **يُمنع محرك ثانٍ** لحساب حالة المشروع أو التقدم أو المالية أو
  الصلاحيات. 7. **كل جدول جديد يتضمن فقرة «لماذا لا يمكن توسيع الموجود».**
  8. ❌ لا حذف/إعادة تسمية/ترحيل لبيانات قائمة دون خطة توافق وتراجع واعتماد خالد.

- **G14 — الخطوة الصفرية Git (جديد):** أول أمر في أي مهمة تنفيذية:
  `git status --short --branch`. إن كانت الشجرة غير نظيفة أو الفرع المستهدف بحالة
  متعارضة: **توقّف فورًا وأبلغ خالد ولا تتقدم خطوة قبل رده.**
  ❌ **ممنوع منعًا باتًا:** `stash` · `reset` · `checkout` يتجاهل تغييرات · حذف أو نقل
  أي تغييرات قائمة — مهما كان السبب.

---

## 3. المسار والبوابات

### التفكيك والـIDs الثابتة
كل بند مركّب مُفكَّك إلى متطلبات ذرّية بـID ثابت `V2-<wave>.<item>-<letter>`
(مثال: `V2-1.9-A` فيديو الهيرو · `V2-1.9-B` صور الأعمال · `V2-1.9-C` الخطوط).
**نفس الـID يُستخدم في كل مصفوفة وتقرير ومستند** — لا إعادة ترقيم.

### تعريف الإنجاز (Definition of Done)
ملف SQL (إن وُجد) + RLS + الأنواع + العلم + حالات فارغة عربية + `npm run build` ينجح +
`npm test` أخضر + خطوات اختبار يدوي. **موجات الموقع تضيف:** Lighthouse موبايل
≥90 أداء / 95 SEO / 95 a11y / 95 BP على الصفحات المتغيرة، `axe` نظيف، وقياسات قبل/بعد.

### الاختبارات — Preview/Local حصرًا
❌ **أبدًا على Production.** كل موجة تضيف اختبارات ميزاتها **قبل** اعتمادها.

### كل موجة
تنفيذ → `docs/wave-reports/WAVE_<n>_REPORT.md` → commit محلي → **⛔ توقّف** انتظارًا لـ«أعتمد».

### قالب تقرير الموجة
1. ملخص ما تم (٣–٥ أسطر) · 2. **ملفات SQL بانتظار التطبيق اليدوي** (الأسماء + الترتيب
+ خطة التراجع) · 3. الأعلام المضافة وحالتها الخمس (Owner/Default/Activation/Rollback/
Removal) · 4. خطوات الاختبار اليدوي · 5. قياسات الأداء قبل/بعد (لموجات الموقع) ·
6. انحرافات G9 وأسبابها · 7. **ازدواجات مُنعت بموجب G13** · 8. أسئلة تحتاج قرار خالد ·
9. المقترح التالي.

---

## 4. الموجات

> **مفتاح القرارات:** 🆕 جديد · 🔧 تحقّق ووسّع · 🔗 ادمج في القائم · ❌ محذوف كمكرر ·
> ❓ قرار خالد · ⏸️ مؤجَّل · 🔒 محجوب على إثبات Production

---

### WAVE −1 · DELTA-AUDIT ✅ **مُنجَزة**

تمّت في هذه المهمة. المخرجات الستة: `EXISTING_CAPABILITIES.md` ·
`DATABASE_APPLICATION_STATUS.md` · `PROTECTED_ARCHITECTURE.md` ·
`OVERLAP_DEDUP_MATRIX.md` · هذه المسودة · `V2_1_CHANGELOG.md` ·
`V2_1_EXECUTIVE_SUMMARY_AR.md`.

**بديل مستندات v2.0 §3:** `docs/REPO_AUDIT.md` → `EXISTING_CAPABILITIES.md` ·
`docs/SCHEMA_SNAPSHOT.md` → §0 و§2 منه · `docs/STATUS_MATRIX.md` →
`OVERLAP_DEDUP_MATRIX.md` · `docs/EXECUTION_PLAN.md` → §4 من هذه المسودة.
❌ لا تُنشأ الأربعة الأصلية — ستكون مستندات موازية.

---

### WAVE 0 · السلامة والتحصين والبيئة — **الأولوية القصوى (71% بناء جديد)**

> ⛔ **لا شيء قبلها.** وهي الموجة الوحيدة التي يقابلها فراغ حقيقي في المستودع.

**0.A — الاختبارات الأساسية (منقولة من Wave 7 إلى هنا) — 🔧**
الحد الأدنى المطلوب **موجود بالفعل** ضمن 239 ملف اختبار: Login · رؤية المشروع بالدور ·
عرض العميل · عرض إصدار المخرَج · تعليق العميل · اعتماد/رفض · **رفض الحجز المزدوج
(`23P01`)** · قيود الرؤية المالية · اتساق اللقطة التشغيلية · حفظ طلب عرض السعر ·
**رفض تتابع البريد المجهول** · صلاحيات العهدة.
**المطلوب: تشغيلها والتحقق من خضرتها على Preview/Local — لا كتابتها.**
+ اختبارات Regression على RLS.
🔒 **اختباران محميان لا يجوز إسقاطهما:** رفض تتابع البريد المجهول
(`comms_anon_zero_access` · `email_backbone_phase1` · `relay_handler_batch11`) وعدم
عودة فكّ الـJWT بلا تحقق توقيع في `/api/public/intake`.

| ID | المتطلب | القرار |
|---|---|---|
| V2-0.1-A…D | checkbox موافقة إلزامي على: النموذج الرئيسي · `/quote-request` · `/book-meeting` · `/upload-files`. النص: «أوافق على سياسة الخصوصية وعلى تواصل كيان معي بخصوص طلبي» + رابط `/privacy-policy` | 🆕 |
| V2-0.1-E | ~~checkbox على `/quick-access`~~ — **مشطوب: الصفحة لا تحوي نموذجًا**، هي روابط إلى الثلاثة الأخرى | ❌ |
| V2-0.1-F | حفظ الموافقة + الطابع الزمني — **عمودان امتداد على `public_intake`** لا جدول | 🔗 |
| V2-0.2-A/B | `docs/SECRETS_AUDIT.md` — يوثّق الفحص القائم + يضيف فحص سجل Git + **يرصد `SHEETS_ENDPOINT` المكتوب حرفيًا في `lib/submitForm.ts`**. المواقع والمخاطر فقط، **بلا أي قيمة** | 🔧 / 🆕 |
| V2-0.3-A…D | `docs/ENVIRONMENTS.md` + تقسيم `.env.example` (**+13 متغيرًا يقرأها الكود وغائب عنه**) + `scripts/seed-preview.ts` + **مشروع Supabase ثانٍ للـPreview** — 🔴 يحجب رفع تجميد G3 | 🆕 |
| V2-0.4-A/B | `.github/workflows/db-backup.yml` (pg_dump ليلي، `workflow_dispatch` حتى ضبط الأسرار) + `docs/RESTORE_RUNBOOK.md` — **هدف RTO ≤ 60 دقيقة** | 🆕 |
| V2-0.5-A…C | `@sentry/nextjs` يعمل **فقط** مع `SENTRY_DSN` + `docs/OBSERVABILITY.md` + مراقب uptime مجاني | 🆕 |
| V2-0.6-A | ~~Rate limiting~~ — **مبني ومطبَّق بدليل مؤرَّخ.** يُستبدل بـ**تدقيق تغطية**: أي مسار POST عام بلا حدّ؟ | ❌ → 🔧 |
| V2-0.6-B | ~~ترويسات أمان~~ — **مبنية وحيّة** (CSP بنصفين: منفَّذ + Report-Only). يبقى بندان: **`HSTS` غائب** + مسار nonce لإكمال CSP | ❌ → 🔧 |
| V2-0.7-A | `docs/EMAIL_DNS.md` — SPF/DKIM/DMARC لـ`kianmedia.com` | 🆕 |
| **V2-0.8-A** | **🆕 جديد في v2.1 — سجل الأعلام الموحّد** (G6): جدول واحد يوثّق كل علم في 18 جدول إعدادات و~20 متغير بيئة، بحقول Owner/Default/Activation/Rollback/Removal | 🆕 |
| **V2-0.9-A** | **🆕 جديد في v2.1 — إثبات حالة Production بالقراءة فقط:** تشغيل `*_POSTCHECK.sql` للحزم الـ15 بالترتيب + استعلام جداول النسخ الاحتياطي. ❌ **لا `RUNME`.** بدونه كل تخطيط لاحق على أرض مجهولة | 🆕 🔒 |

⛔ **GATE.**

---

### WAVE 1 · الموقع: أساس عالمي (53% جديد)

| ID | المتطلب | القرار |
|---|---|---|
| V2-1.1-A/B/C/E | `next-intl` + توجيه `/en` + `hreflang` + metadata حسب اللغة + صحّة RTL/LTR | 🆕 / 🔧 |
| V2-1.1-D | ~~كتابة نسخة إنجليزية كاملة~~ — **النصوص EN موجودة أصلًا** عبر `t({ ar, en })` في كل مكوّن. المطلوب: **حصر ما ينقصه `en`** ثم مراجعة خالد | 🔗 |
| V2-1.2-A | نقل مصفوفة `STATS` من `components/Stats.tsx` إلى `content/stats.ts` — مصدر واحد | 🔗 |
| V2-1.2-B | الأرقام النهائية تُرسَم من الخادم؛ الحركة تبدأ من القيمة المرسومة | 🆕 |
| V2-1.2-C | 4000+ إنتاج · 2000+ عميل · 13 منطقة · **سنوات الخبرة = ❓** | ❓ |
| V2-1.3-A | **تعميم نمط `/case-studies` القائم** (`generateMetadata` + canonical صحيح) على 8 مسارات: `/` · `/quote-request` · `/book-meeting` · `/upload-files` · `/opportunities` · `/privacy-policy` · `/terms` · `/live-status` | 🔧 |
| V2-1.3-B | OG ديناميكي بهوية **1200×630** عبر `@vercel/og` — يتقاعد الشعار المربّع | 🆕 |
| V2-1.4-A | نقل 46 عملًا من `ITEMS` في `components/Portfolio.tsx` إلى `content/portfolio.ts`. ⚠️ **`content/portfolio.ts` لم يكن موجودًا** — تصحيح لاسم ورد في v2.0 | 🔗 |
| V2-1.4-B | كتابة **38** وصفًا عربيًا فريدًا (لا ~36) — 🔒 **مع الحفاظ على الـ8 الحيّة** | 🔧 |
| V2-1.4-C | إعادة تسمية الثلاثة «إعلان قصير» (`:109,111,113`) — أسماء العملاء ❓ | ❓ |
| V2-1.4-D | اشتقاق عدّادات الفئات من البيانات. ⚠️ **الاشتقاق وحده لا يزيل الفجوة**: 56 مقابل 46 لأن العمل ينتمي لعدة فئات **بالتصميم**. القرار: عرض كما هو / توضيح / منع العضوية المتعددة ❓ | 🔧 + ❓ |
| V2-1.5-A/B | قسم الشهادات خلف علم. **النظام كامل على فرع `feature/kian-operations-platform-v1`** (`kian_testimonials_v1_RUNME.sql` · `lib/portal/testimonials.ts` · `/share-experience` · `AdminTestimonials.tsx`). ❌ **لا ملف ثابت مؤقت ولا جدول جديد** — يُدمج الفرع. 🔴 **وحذف `components/Testimonials.tsx` إلزامي** (كود ميت بـ3 شهادات ملفَّقة) | 🔗 |
| V2-1.6-A | النموذج الرئيسي يحفظ. ⚠️ **تصحيح ترتيب v2.0:** **Supabase أولًا** (`captureIntake` → `/api/public/intake` → `public_intake`) **ثم** الـwebhook الاختياري **ثم** فتح واتساب. **Apps Script ليس قاعدة بيانات الـLeads.** فشل Apps Script أو واتساب **لا يُفقد الطلب** | 🔗 |
| V2-1.6-B | ~~غير حاجب عند الفشل~~ — **مبني** (`captureIntake` لا يرمي أبدًا). يبقى ربط Sentry | ❌ |
| V2-1.6-C | توحيد Source/UTM/Attribution — **أعمدة امتداد على `public_intake`**. `/quote-request` هو النموذج المرجعي | 🔗 |
| V2-1.7-A | ~~`LocalBusiness`~~ — `ProfessionalService` + `Organization` قائمان. مراجعة حقول | 🔧 |
| V2-1.7-B | `VideoObject` لكل عمل | 🆕 |
| V2-1.7-C | `BreadcrumbList` — **النمط قائم** في `case-studies/[slug]`، يُعمَّم | 🔧 |
| V2-1.8-A/B | ~~`sitemap.xml` + `robots`~~ — **مبنيان** (`app/sitemap.ts` · `app/robots.ts` من `lib/site.ts`). يبقى إضافة مسارات `/en` | ❌ → 🔧 |
| V2-1.8-C | **`not-found.tsx` غائبة فعلًا** → 404 بهوية. `error.tsx` قائم → تحسين | 🔧 |
| V2-1.9-A | `hero.mp4` + `hero-poster.jpg` قائمان → ضغط + نسخ متعددة + كسل تحت الطية | 🔧 |
| V2-1.9-B | `next/image` لكل المصغّرات مع سقوط `hqdefault` (روابط `maxres` تُرجع 404) | 🆕 |
| V2-1.9-C | تجزئة Almarai + `font-display: swap` | 🆕 |
| V2-1.9-D | أهداف: LCP < 2.5s موبايل · CLS < 0.1 · INP < 200ms | 🆕 |
| V2-1.10-A/B | إتاحة AA: تباين · حالات تركيز · بدائل نصية من العناوين · تنقل لوحة مفاتيح · `prefers-reduced-motion` | 🆕 |
| V2-1.11-A…C | نظام صفحات SEO **فوق i18n**: قالب + 6 خدمات (درون، بث مباشر، عقاري، أفلام مؤسسية، وثائقي، بودكاست) + 3 مدن (الرياض، جدة، الدمام)، AR+EN، خلف `SHOW_SEO_PAGES`، **خارج sitemap حتى التفعيل** | 🆕 |

⛔ **GATE.**

---

### WAVE 2 · مصداقية الموقع (40% جديد)

| ID | المتطلب | القرار |
|---|---|---|
| V2-2.1-A | ~~`content/case-studies/` + صفحة قالب~~ — 🔴 **منصة دراسات الحالة مبنية بالكامل:** 13 جدول `cs_*` · 3,054 سطر SQL · `/case-studies` + `[slug]` بـmetadata وJSON-LD وBreadcrumb · دورة تحرير واعتماد وسرّية · 7 ملفات اختبار. ❌ **نظام ملفات موازٍ ممنوع** — المحتوى يُدخَل عبر المنصة | 🔗 |
| V2-2.1-B | بذر 6 دراسات رائدة (معادن ٢٠٢٥ · وثائقيات القطيف/الدروازة · مهرجان أفلام السعودية · بث مباشر كبير · عرس فاخر · **السادسة ❓**) — مع احترام قاعدة «لا تفاصيل تنفيذ قبل الاتفاق» | ❓ |
| V2-2.1-C | ربط بطاقات الأعمال بـ«اقرأ القصة» (`ITEMS` ↔ `cs_case_studies`) | 🔧 |
| V2-2.2-A | شريط الشعارات (رمادي→ملوّن عند المرور). ⚠️ **62 شعارًا في `public/clients` لا 22**، و`components/Clients.tsx` قائم | 🔧 |
| V2-2.2-B | تأكيد حق استخدام الشعارات + **Publication Consent** للشعار والتقييم ودراسة الحالة | ❓ |
| V2-2.3-A | صفحة `/trust`: RLS · تشفير عند التخزين · نسخ احتياطي · سجل تدقيق · التزام PDPL · س.ت 1009179096 · الرقم الضريبي 311047382900003 · بيان HSE. 🔴 **مشروطة بإنجاز Wave 0** — «backups» غير موجودة اليوم و«audit log» مشتَّت على 15 مصدرًا. ❌ **لا ادّعاء غير متحقق في صفحة مشتريات** | 🆕 |
| V2-2.3-B | AR/EN (تابع V2-1.1-A) | 🔗 |
| V2-2.4-A | خانات صحافة/جوائز/تغطية مهرجانات على «لماذا كيان» | 🔧 |
| V2-2.5-A/B | حسم `info@` / `sales@` / `contact@` ❓ ثم توحيد NAP في كل الموقع | ❓ / 🆕 |

⛔ **GATE.**

---

### WAVE 3 · توسيع تشغيل البوابة القائم (30% جديد فقط)

> 🔴 **تحوّل جذري عن v2.0: 16 من 23 متطلبًا مبنية.** هذه موجة **تحقّق وحسم ازدواج**،
> لا موجة بناء.

**⛔ شرط دخول إلزامي — حسم ثلاثة ازدواجات موروثة قبل أي كود:**
- **D-1 Call Sheet:** `ops_call_sheets` مقابل `project_call_sheets` — أيّهما المصدر؟
- **D-2 دوال Call Sheet:** `prodops_call_sheet*` مقابل `project_core_call_sheet_*`
- **D-3 المواقع:** `ops_locations` · `project_locations` · `custody_inventory_locations`

| ID | المتطلب | القرار |
|---|---|---|
| V2-3.1-A | ~~جدول `call_sheets`~~ — **جدولان قائمان.** ❌ لا ثالث. توسعة المصدر المعتمد بعد حسم D-1 | 🔗 |
| V2-3.1-B/C | ~~`call_sheet_crew` / `call_sheet_equipment`~~ — `ops_job_crew` و`ops_job_equipment` قائمان (والأخير محروس بـ`23P01`) | ❌ |
| V2-3.1-D | الساعة الذهبية عبر `suncalc` — **امتداد** على الجدول المعتمد | 🆕 |
| V2-3.1-E | الطقس عبر Open-Meteo (بلا مفتاح) خادميًا ≤48 ساعة — **`ops_job_weather` قائم**، يُعبَّأ لا يُنشأ | 🔧 |
| V2-3.1-F | لافتة تحذير رياح لأيام الدرون | 🆕 |
| V2-3.1-G | عرض عربي قابل للطباعة — دوال النشر قائمة | 🔧 |
| V2-3.1-H | `backup_date` (تاريخ بديل) — عمود امتداد | 🆕 |
| V2-3.2-A | التصاريح — `ops_job_permits` قائم **كابن لوظيفة**. سجل تصاريح عام = **Extension Table** مرتبط | 🔗 |
| V2-3.2-B | ~~`crew_documents`~~ — **`tvn_documents` + `tvn_document_types` قائمان**، ومنصة `vcc_*` (16 جدولًا) للامتثال ومنح الوصول | ❌ |
| V2-3.2-C | تنبيهات انتهاء 30/7 يومًا — **حدث في الـOutbox القائم**، مطويّ داخل `/api/cron/custody-alerts`. ❌ لا مجدول رابع (G8) | 🔧 |
| V2-3.2-D | ~~Bucket خاص + روابط موقَّعة + RLS~~ — النمط قائم (`secure-document` · rental evidence · `deliverable_assets`) | ❌ |
| V2-3.3-A | ~~`crew_members (roles[], …)`~~ — 🔴 **`tvn_profiles` + 13 جدولًا = نظام المستقلين والموردين كاملًا** (`tvn_profile_rates` · `tvn_profile_restricted` · `tvn_profile_bank` · `tvn_availability` · `tvn_reviews`، مع 10 ملفات اختبار). ❌ **و`roles[]` كمصفوفة ممنوع أصلًا** — جدول ربط قائم | ❌ |
| V2-3.3-B | ~~`crew_assignments`~~ — **`tvn_assignments` + `tvn_assignment_candidates` قائمان**، والأجر المتفق في `tvn_profile_rates`/التكليف (يختلف بين المشاريع) | ❌ |
| V2-3.3-C | ~~«تحذير UI حاجب» للتعارض~~ — 🔴 **تراجع أمني.** الحماية **في القاعدة**: `23P01` بنطاقات `person:`/`equipment:`/`location:`، حارس **على الجدول** لا داخل RPC، وترجمة عربية في `lib/portal/pgerror.ts`. المسموح: **توسعة النطاق بنفس العقد ونفس شكل الـhint** | ❌ |
| V2-3.4-A | ~~جدول `locations`~~ — **ثلاثة قائمة.** ❌ لا رابع. بعد حسم D-3 | 🔗 |
| V2-3.4-B | `location_media` — امتداد على الجدول المعتمد | 🆕 |
| V2-3.4-C | ~~الاختيار من Call Sheet~~ — الربط قائم | ❌ |
| V2-3.5-A | ~~`project_templates` + `template_tasks`~~ — **`project_templates` + `project_template_versions` + `apply_template_v2` + حزمة 7A كاملة** | ❌ |
| V2-3.5-B | بذرتا «فيلم مؤسسي» و«عرس» — بذور لا نظام | 🔧 |
| V2-3.5-C | **بودكاست ٢٥ حلقة** — 🔴 ❌ **لا جدول حلقات مستقل.** الحلقة = مشروع فرعي عبر `parent_project_id` + `project_scope`، أو وحدة ضمن برنامج (8A). جاهز لـ«سواليف أسرية» | 🔗 |
| V2-3.6-A/B | `calendar_tokens` + `/api/calendar/[token].ics` (أيام التصوير، رموز طويلة قابلة للإلغاء). نمط الرمز الملغى قائم في `liveops_client_links` — يُحتذى | 🆕 |

⛔ **GATE.** **التقرير يجب أن يوصي ببدء الطيار الحقيقي (مسبار ١٠) على Waves 0–3 قبل
اعتماد Wave 4.**

---

### WAVE 4 · توسيع CRM والأعمال القائم (7% جديد فقط)

> 🔴 **13 من 14 متطلبًا مبنية.** هذه ليست موجة — هي **دفعة ربط**.

| ID | المتطلب | القرار |
|---|---|---|
| V2-4.1-A | ~~جدول `tenders`~~ — **المناقصة = نوع فرصة أو مرحلة أو Extension Table** مرتبطة بـ`crm_opportunities`. ❌ لا تكرار بيانات الجهة. تُربط بالعميل والفرصة وعرض السعر وبالمشروع عند الفوز | 🔗 |
| V2-4.1-B | ~~`rate_card_items`~~ — **`sq_price_books` + `sq_price_book_versions` + `sq_price_book_entries` + `sq_cost_rates` + `sq_service_catalog` + `sq_pricing_rules`** (2,964 سطرًا) | ❌ |
| V2-4.1-C | لوحة نسبة الفوز والقيمة ومتوسط الهامش — **تقرير في محرك CRM القائم** مع حماية الهامش | 🔗 |
| V2-4.2-A/C/D | جدول الشهادات + واجهة الاعتماد + التغذية التلقائية لقسم الموقع — **كلها مكتوبة على `feature/kian-operations-platform-v1`**. يُدمج الفرع. ✅ مبدأ «Pipeline واحدة» صحيح | 🔗 |
| V2-4.2-B | المُشغِّل: إغلاق المشروع **و**سداد الدفعة النهائية → نموذج مُرمَّز بالبريد. يُركَّب على `project_closure_requests`/`final_close` + `fin_payment_milestones`/`fin_collections` | 🔧 |
| V2-4.3-A | الملخص الأسبوعي — **حدث في الـOutbox القائم** يقرأ `activity_log` | 🔗 |
| V2-4.3-B | ~~جدولة n8n~~ — **يُطوى داخل `/api/cron/notify-email` القائم** (G8، سقف Hobby) | 🔧 |
| V2-4.3-C | ~~إلغاء اشتراك لكل عميل~~ — `notification_preferences` + `comms_preferences` قائمان | ❌ |
| V2-4.4-A | `client_health` — **عرض مشتق (view)** من CRM والمشاريع. ❌ لا جدول | 🔗 |
| V2-4.4-B | ~~طابور `follow_ups`~~ — `crm_activities` + نظام المهام القائم | 🔗 |
| V2-4.4-C | قاعدة «>180 يومًا صامت → متابعة مقترحة». 🔒 **لا شيء يُرسَل تلقائيًا** | 🆕 |
| V2-4.5-A | لوحة موسمية (أيام التصوير × القطاع × السنوات) — **تقرير يستهلك `project_shoot_sessions`** عبر المحرك القائم | 🔗 |

⛔ **GATE.**

---

### WAVE 5 · التسليم والحقوق والمال (11% جديد)

**⛔ شرطا دخول:** (1) حسم **D-4** (جدولا الإصدارات) · (2) إثبات حالة **Phase A/B**
المالية — بدونه لا يُلمس أي بند مالي.

| ID | المتطلب | القرار |
|---|---|---|
| V2-5.1-A/B | إصدارات المخرجات — ✅ v2.0 نفسه يقول «adapt to existing per G9» وهي **الصياغة الصحيحة**. جدولان قائمان + مُشغِّل V1 تلقائي + `deliverable_version_summary`. 🔒 **الحفاظ على كل الإصدارات والتعليقات والاعتمادات السابقة** | 🔧 |
| V2-5.2-A | `showreel_allowed` / `confidential` — **عمودان امتداد** على `deliverables` يُضبطان عند الاعتماد. ❌ لا نظام حقوق جديد | 🔗 |
| V2-5.2-B | عرض تصفية تسويقي فوق العمودين | 🆕 |
| V2-5.3-A…C | روابط التسليم: `project_delivery_release` + `deliverable_downloads` + `deliverable_final_opens` + `pc_release_window_ok` قائمة. **إكمال ما ينقص من الرباعية** (رمز/انتهاء/عدّاد/إلغاء) + **صفحة عامة بهوية كيان** تُدمج مع نظام المعاينة القائم | 🔧 |
| V2-5.3-D | ملاحظة سياسة الأرشفة («الاسترجاع بعد فترة الأرشفة خدمة مدفوعة») | 🆕 |
| V2-5.4-A…C | **الموافقة الحكمية (Deemed Approval)** — 🔴 **`NEEDS KHALED CONFIRMATION` والراية OFF.** ❌ **لا يُفترض ثبوت الأساس التعاقدي** قبل تحديد **نسخة العقد النهائية الموقّعة ونص البند الحرفي لكل مشروع على حدة**. **عقد بناء لا يُطبَّق تلقائيًا على «مسبار ١٠».** التفعيل لكل مشروع منفردًا. القيد التدقيقي غير القابل للتغيير يُكتب في السجل القائم لا في سجل جديد | ❓ |
| V2-5.5-A | ~~`project_costs`~~ — **قائم** (وكذلك `fin_costs` و`project_expenses` — ازدواج D-5) | ❌ |
| V2-5.5-C | ~~`payment_milestones` «إن غابت»~~ — ✅ الشرط صحيح، **والجواب: `fin_payment_milestones` ليست غائبة** | ❌ |
| V2-5.5-B/D/E | بطاقة الهامش + تقويم التدفق النقدي + عدّاد التأخر — **تستهلك `pc_project_financials()` و`fin_receivables`/`fin_collections`/`fin_revenue`**. ❌ لا إعادة حساب. 🔒 حماية استنتاج الأرباح محفوظة. **الفصل الإلزامي بين ستة مفاهيم:** ميزانية تشغيلية · تكاليف فعلية · دفعات · فواتير رسمية · حالة سداد Zoho · تنبؤ تدفق نقدي | 🔗 |
| V2-5.5-F | مسوّدة إشعار عربي رسمي بنقرة (بند التعليق المتبادل) — **قالب في `comms_templates`**. 🔒 **الإرسال قرار بشري دائمًا** | 🔗 |
| V2-5.6-A | `client_viewer` / `client_approver` — **قدرات ضمن `project_members`/`project_member_roles`** ضمن نموذج الـ124 صلاحية القائم. ❌ لا نموذج أدوار موازٍ. 🔒 **صلاحية الاعتماد تُفرض في RLS/RPC لا في UI فقط** | 🔗 |

⛔ **GATE.**

---

### WAVE 6 · الأصول والأرشيف والامتثال والمعرفة (47% جديد)

| ID | المتطلب | القرار |
|---|---|---|
| V2-6.1-A | QR قابل للطباعة → صفحة حالة `/e/[id]`. QR داخلي قائم (`custody_qr_events` · `lib/qr/*` · حمولة فقيرة معدَّلة مُدقَّقة). 🔴 **الصفحة العامة تخضع لـ`QR_SECURITY_CONTRACT.md` وتراعي صلاحية المشاهد** — ❌ لا عرض بيانات عهدة أو أسعار أو موظفين للعامة | 🔧 |
| V2-6.1-B | ~~`equipment_usage_log`~~ — **دفتر استخدام قائم ملحق بثلاثة مُشغِّلات**، مُشتَق تلقائيًا | ❌ |
| V2-6.1-C | ~~`maintenance_schedule` + تنبيهات~~ — **`custody_inventory_maintenance` + `_maintenance_plans`** + cron التنبيهات اليومي | ❌ |
| V2-6.1-D | حقول سجل الأصول (شراء/قيمة/تسلسل/تأمين) — `custody_inventory_assets` + `depreciation_enabled` + `asset_insurance_policies`. فحص الناقص فقط | 🔧 |
| V2-6.2-A/B | `archive_media` + `archive_project_links` — **جديد مبرَّر:** `project_archives` القائم أرشيف إغلاق مشروع لا سجل وسائط فيزيائية (HDD/SSD/NAS/LTO، سعة، صحة، موقع فيزيائي) | 🆕 |
| V2-6.3-A/B | `music_licenses` + ملخص حقوق قابل للطباعة لكل مشروع | 🆕 |
| V2-6.4-A | سجل HSE — **امتداد يجمع `ops_job_hse` و`ops_incidents` و`custody_incidents` القائمة**. ❌ لا سجل حوادث رابع | 🔗 |
| V2-6.5-A | `model_releases` — PDPL بالحد الأدنى، bucket خاص، RLS صارمة، يعيد استخدام نمط `SECURE_DOCUMENT_GRANT_CONTRACT.md` | 🆕 |
| V2-6.6-A/B | `sops` + `sop_items` + بذور (ما قبل إقلاع الدرون · ما قبل البث/إنترنت احتياطي/encoder · تجهيز استوديو البودكاست) | 🆕 |
| V2-6.6-C | إرفاقها بـCall Sheet كقوائم إلزامية — **إعادة استخدام `project_task_checklists`** | 🔗 |
| V2-6.7-A | ~~`project_postmortems`~~ — **`project_post_reviews` + `project_lessons_learned` قائمان** ضمن دورة الإغلاق 5C | ❌ |
| V2-6.8-A | مولّد دراسات الحالة (بقالب، **بلا AI**) — يُبنى **داخل منصة `cs_*`**. ✅ مبدأ «مولّد واحد يغذّي مخرجين» صحيح | 🔗 |
| V2-6.8-B | طابور `portfolio_drafts` — يُفضَّل كحالة داخل `cs_*` لا جدولًا منفصلًا | 🆕 |
| V2-6.8-C | 🔴 **قيد صلب:** ❌ **ممنوع تعديل ملف محتوى الأعمال تلقائيًا وقت التشغيل على Vercel** — نظام الملفات للقراءة، والكتابة تُفقد عند أول نشر. **المسار المعتمد:** Draft في القاعدة أو Export Queue → معاينة → موافقة → **Script أو Pull Request** يُحدِّث ملف المحتوى → نشر **بعد اعتماد خالد**. (المخزن الفعلي `components/Portfolio.tsx` → `content/portfolio.ts` بعد V2-1.4-A) | 🔧 |

⛔ **GATE.**

---

### WAVE 7 · تجربة المستخدم والبحث والتلميع المؤسسي (10% جديد فقط)

> 🔴 **9 من 10 مبنية.** الموجة تتقلّص إلى بندين حقيقيين + تدقيقات.

| ID | المتطلب | القرار |
|---|---|---|
| V2-7.1-A | **بحث شامل Cmd+K عبر Postgres FTS** (مشاريع/عملاء/مخرجات/معدات). ✅ لا `tsvector` في أي RUNME ⇒ جديد حقيقي. ✅ «بلا خدمة خارجية» صحيح | 🆕 |
| V2-7.2-A | ~~مركز الإشعارات~~ — **`/client-portal/notifications` قائم**. تدقيق تجربة لا بناء | ❌ |
| V2-7.3-A | عارض سجل التدقيق — ⚠️ `activity_log` **+ 14 جدول `*_audit`**. ❓ **قرار مطلوب:** عارض **قراءة فقط** يقرأ الخمسة عشر، أم `activity_log` وحده؟ ❌ **لا سجل سادس عشر** | ❓ |
| V2-7.4-A | ~~اللوحة التنفيذية~~ — **مبنية** (`/client-portal/executive` + 1,674 سطر SQL + `executive_kpi_catalog`/`_snapshots`/`_alert_rules`). ✅ «تستهلك ولا تضيف مصادر» مبدأ قائم بالفعل. إضافة مؤشرات ناقصة فقط | ❌ |
| V2-7.5-A | ~~تصدير CSV~~ — **`lib/portal/csv.ts` أداة موحّدة قائمة** بترميز عربي وتوقيت الرياض وإخفاء أعمدة بالدور. تدقيق تغطية | ❌ |
| V2-7.6-A | ~~MFA (TOTP)~~ — **مبني ومُختبَر حيًا على Production** (دورة المالك بخطوتين مؤكَّدة). يبقى **إجراء لوحة تحكم واحد**: تفعيل TOTP في Supabase | ❌ |
| V2-7.7-A | ~~مستأجر تجريبي + `DEMO_MODE`~~ — 🔴 **يُشطب بوصفه خطرًا محذوفًا من v2.0.** ❌ **لا `DEMO_MODE` داخل Production ببيانات وهمية.** البديل: **بيئة Demo/Preview منفصلة** (V2-0.3-D) بنفس الكود وEnvironment مستقل، **بلا أي بيانات أو مفاتيح إنتاج**. ✅ الخطر غير قائم اليوم | ❌ |
| V2-7.8-A | حزمة Playwright E2E — **طبقة فوق 239 ملف اختبار قائم**، ❌ لا استبدال | 🔧 |
| V2-7.8-B | ~~CI على الـPRs~~ — **`.github/workflows/ci.yml` قائم**: lint → typecheck → test → build على `push` و`pull_request`، + وظيفة `mobile`. تُضاف خطوة Playwright فقط | ❌ |
| V2-7.9-A | تدقيق الحالات (فارغة/تحميل/خطأ) لكل مسار — 7 ملفات `error.tsx` قائمة | 🔧 |

⛔ **بوابة الاستقرار.**

---

### STABILIZATION · أسبوعان بلا ميزات

إصلاحات فقط · **طيار حقيقي (مسبار ١٠ مرشح)** · تقرير استخدام · ملخص ملاحظات الطيار ·
**قرار Mobile Go / No-Go.**

---

### WAVE 8 · جاهزية الجوال (50% جديد)

| ID | المتطلب | القرار |
|---|---|---|
| V2-8.1-A | ~~PWA~~ — **مبنية**: `app/manifest.ts` · `public/sw.js` · `lib/pwa/*` · `PwaProvider` · `/offline` · وسوم `apple-*` · 4 اختبارات + عقد. يبقى **اختبار حيّ على جهاز حقيقي** | ❌ |
| V2-8.2-A | `docs/MOBILE_API.md` — يوثّق كل جدول وRPC يحتاجه التطبيق. 🔒 ❌ **لا Mobile API يعيد بناء منطق الأعمال** — المطلوب **facade آمن (RPC/API) فوق المنطق القائم** | 🆕 |
| V2-8.2-B | إضافة RPCs الناقصة خلف أعلام — بعد حصر V2-8.2-A من بين 1,662 دالة قائمة | 🔧 |
| V2-8.3-A | `push_tokens` — Push اليوم **أساس فقط بلا مستمع** (موثَّق في `PWA_PUSH_CONTRACT.md`) | 🆕 |
| V2-8.3-B | مسار خادمي عبر Expo Push. ✅ **قناة جديدة في الخدمة القائمة لا نظام منفصل** (القنوات: داخل التطبيق · بريد · Push — **ولا WhatsApp ولا SMS**) | 🔗 |
| V2-8.4-A | روابط عميقة `kian://` + **Universal Links (iOS) وApp Links (Android)** | 🆕 |
| **V2-8.5-A** | **🆕 جديد في v2.1 — أساسيات أمان الجوال:** تخزين آمن للرموز · فتح بيومتري اختياري · إبطال الجلسة · تقارير الأعطال · ❌ منع تخزين ملفات حساسة غير مشفَّرة | 🆕 |
| **V2-8.6-A** | **🆕 جديد في v2.1 — العمل دون اتصال:** Call Sheet دون اتصال + بيانات وصف التصاريح/المستندات + **طابور مزامنة عند عودة الإنترنت**. ⚠️ الوضع الحالي **قراءة فقط عمدًا** (`FINAL_PRODUCTION_READINESS_MATRIX.md` §4) — التغيير قرار معماري لا إصلاح | 🆕 |

⛔ **GATE.**

---

### WAVE 9 · التطبيق الأصلي (33% جديد)

**⛔ سؤال دخول:** v2.0 يقول «مستودع جديد `kian-app`» — لكن **`apps/mobile` قائم داخل
هذا المستودع** (Expo SDK 51 · `expo-camera` · `expo-location` · `expo-secure-store` ·
3 شاشات فيها **`ScanScreen`** · **وCI يفحصه**). ❓ **قرار خالد: مستودع جديد أم توسيع
القائم؟** (التوصية: توسيع القائم — التخلي عنه يهدر عملًا جاهزًا.)

| ID | المتطلب | القرار |
|---|---|---|
| V2-9.1-A | الهيكلة — **`apps/mobile` قائم**؛ يُوسَّع | 🔧 + ❓ |
| V2-9.1-B | `expo-router` + i18n ar/en + RTL — الحالي React Navigation؛ الترحيل يُبرَّر أو يُترك | 🔧 |
| V2-9.1-C | ثيم العلامة (Almarai، داكن سينمائي) من اليوم الأول | 🆕 |
| V2-9.2-A | **تجربة العميل:** المشاريع والتقدم · المخرجات + الاعتماد (مدرك للموافقة الحكمية) · روابط التسليم · عرض عروض الأسعار والفواتير · الإشعارات | 🆕 |
| V2-9.2-B | **تجربة الطاقم:** Call Sheet اليوم · تقويم التصوير · **ماسح QR للعهدة** (`ScanScreen` + `expo-camera` قائمان) · تنبيهات انتهاء التصاريح | 🔧 |
| V2-9.3-A | Push موصول ببنية Wave 8 + روابط عميقة إلى الشاشات | 🆕 |
| V2-9.4-A | ملفّات بناء EAS (dev/preview/prod) + أيقونات/شاشة بدء + بيئة لكل ملف | 🔧 |
| V2-9.5-A | `docs/STORE_SUBMISSION.md` — Apple Developer Program (تسجيل مؤسسي، **D-U-N-S**) · تسميات الخصوصية · ملاحظات مراجعة App Review · Play Console · خطة لقطات AR/EN | 🆕 |
| V2-9.6-A | **غير أهداف v1 التطبيق:** ❌ مدفوعات · ❌ محادثة · ❌ AI | 🆕 |

**🔒 صياغة Apple Guideline 4.2 (نص ملزم):** التطبيق ثنائي الدور (عميل + طاقم، شامل
مسح QR للعهد) يُقدَّم **بوصفه تعزيزًا للقيمة الأصلية للتطبيق في سياق Apple Guideline
4.2 — وليس ضمانًا لقبول Apple.** ❌ لا يُكتب في أي مستند أنه «يضمن» القبول.

**🔒 إعادة الاستخدام الإلزامية:** Supabase Auth القائم · RLS القائم ·
`project_operational_snapshot` · خدمة الإشعارات القائمة · RPCs القائمة · workflow
الاعتماد القائم · workflow العهدة القائم.

⛔ **البوابة النهائية:** مراجعة قائمة تقديم المتاجر مع خالد → يبدأ عصر الاستقرار.

---

## 5. الأنظمة المحمية — ملخص تنفيذي

التفصيل الكامل في [`PROTECTED_ARCHITECTURE.md`](docs/PROTECTED_ARCHITECTURE.md)
(20 نظامًا). **ممنوع تجاوزها:**

`project_operational_snapshot` (المصدر الوحيد للتقدم) · دورة حياة المشروع وبوابات
الانتقال · Deliverable Workflow · خدمة الإشعارات · منع الحجز المزدوج في القاعدة
(`23P01`) · مصنّف أخطاء PostgreSQL (`lib/portal/pgerror.ts`) · CRM مصدر الحقيقة
التجاري · المالية ومنع استنتاج الأرباح · حدّ فواتير Zoho الرسمية · نموذج الموظف/
المهنة/التكليف/الصلاحية بأربع طبقات · نموذج العهدة والأصول · سجل التدقيق ·
المشاريع الأب–الابن والبرامج · مركز ما قبل الإنتاج · المسار العام وحفظ الـLeads ·
محرك التقارير واللوحة التنفيذية · Pipeline الشهادات/دراسات الحالة/الأعمال · RLS
وSoft Delete · قيود المفاتيح والأعلام والجدولة.

**🔴 الازدواجات الموروثة العشرة (D-1…D-10)** — عبء موجود قبل v2. v2.1 مسؤولة عن
**عدم زيادتها**، وWaves 3 و5 تحملان بندَي حسم صريحين (D-1/D-2/D-3 وD-4).

---

## 6. ملفات SQL المحتملة (وصفًا فقط — ❌ لا تُنشأ في هذه المهمة)

| الموجة | الملف المحتمل | الغرض | التبعية |
|---|---|---|---|
| 0 | `consent_capture_EXTENSION_RUNME.sql` | عمودا `consent_given`/`consent_at` على `public_intake` | — |
| 0 | `feature_flag_registry_RUNME.sql` | سجل الأعلام الموحّد (G6) | — |
| 1 | `lead_attribution_utm_EXTENSION_RUNME.sql` | أعمدة UTM على `public_intake` | 0 |
| 1 | — | دمج `kian_testimonials_v1_RUNME.sql` من الفرع القائم (لا ملف جديد) | — |
| 3 | `call_sheet_consolidation_RUNME.sql` | 🔴 **حسم D-1/D-2 بخطة توافق وتراجع** | ❓ قرار خالد |
| 3 | `location_consolidation_RUNME.sql` | 🔴 **حسم D-3** | ❓ |
| 3 | `call_sheet_golden_hour_weather_EXTENSION_RUNME.sql` | أعمدة الساعة الذهبية والتاريخ البديل | حسم D-1 |
| 3 | `permits_registry_EXTENSION_RUNME.sql` | سجل تصاريح عام مرتبط بـ`ops_job_permits` | — |
| 3 | `calendar_tokens_RUNME.sql` | رموز ICS قابلة للإلغاء | — |
| 5 | `deliverable_versions_consolidation_RUNME.sql` | 🔴 **حسم D-4** | ❓ |
| 5 | `deliverable_rights_EXTENSION_RUNME.sql` | `showreel_allowed` / `confidential` | حسم D-4 |
| 5 | `delivery_links_completion_RUNME.sql` | إكمال الرمز/الانتهاء/العدّاد/الإلغاء | — |
| 5 | `client_approver_capability_RUNME.sql` | قدرة الاعتماد ضمن العضوية | — |
| 6 | `archive_media_RUNME.sql` | سجل الوسائط الفيزيائية | — |
| 6 | `music_licenses_RUNME.sql` · `model_releases_RUNME.sql` · `sops_RUNME.sql` | جداول جديدة مبرَّرة | — |
| 6 | `hse_registry_EXTENSION_RUNME.sql` | امتداد يجمع سجلات الحوادث القائمة | — |
| 7 | `global_search_fts_RUNME.sql` | فهارس `tsvector` | — |
| 8 | `push_tokens_RUNME.sql` | قناة Push في الخدمة القائمة | — |

**كل ملف يلتزم بـG2:** `PREFLIGHT` → `RUNME` (transactional، idempotent، فحص ذاتي) →
`POSTCHECK` (قراءة فقط) → `ROLLBACK`. **وكل جدول جديد يحمل فقرة «لماذا لا يمكن توسيع
الموجود» (G13-7).**

---

## 7. متطلبات التراجع (Rollback)

1. **لكل ملف `RUNME` ملف `ROLLBACK` مقابل** — لا استثناء.
2. **لكل علم خطوات تراجع موثَّقة** بأمر واحد (النمط القائم:
   `update … set <flag> = false where id = 1`).
3. **لا حذف صلب لبيانات قائمة** — Soft Delete بسجل أسباب (`is_deleted` ·
   `deleted_at` · `delete_reason`).
4. **حسم الازدواجات (D-1…D-4) يحتاج خطة توافق ثلاثية:** فترة كتابة مزدوجة → تحويل
   القراءة → إيقاف المصدر القديم. ❌ **لا حذف مباشر لجدول قائم.**
5. **إصلاحات الأمان لا تُتراجع بلا اعتماد مستقل** — تحديدًا: إغلاق `nt_enqueue_email`
   وإزالة فكّ الـJWT بلا تحقق في `/api/public/intake`.
6. **بطاقة كسر الزجاج لـMFA** تبقى خارج البوابة (مسار Supabase SQL مستقل عن البوابة
   وVercel).

---

## 8. عصر الاستقرار (سياسة ما بعد Wave 9)

بعد البوابة النهائية: **تجميد الميزات افتراضيًا.** إصلاحات علل فقط، وتحديثات محتوى
عبر `content/*`، وترقيعات أمنية. أي ميزة جديدة تحتاج Brief موقّعًا جديدًا من خالد.
**هكذا يبقى «الموقع النهائي والتطبيق النهائي» نهائيًا.**

---

## 9. مدخلات مطلوبة من خالد

### 🔴 GATE A — تحجب البدء
1. **حسم G2:** اعتماد عُرف `docs/*_RUNME.sql` القائم رسميًا، أم ترحيل 292 ملفًا إلى `supabase/migrations/`؟
2. **حسم G7:** اعتماد صياغة «تجميد التكاملات» بدل «منعها» (AI · WhatsApp · Zoho مبنية وتعمل)؟
3. **حسم G8:** اعتماد «لا cron إضافي» بدل «لا Vercel cron» (ثلاث مهام حيّة)؟
4. **حسم G3:** هل أُنشئت بيئة Supabase المنفصلة للـPreview؟ (شرط رفع تجميد الدفع)
5. **هل «الملفات الأربعة» المطبَّقة 2026-07-26 هي `authz_fixA..D`؟** (يحسم إن كانت حادثة تسريب بيانات الشركة مغلقة)
6. **هل Phase B المالية مطبَّقة على Production؟**
7. **نتيجة `M-012`** — اختبار المسار العام بعد Fix C (`MANUAL TEST PENDING`).
8. **الفروع الستة القديمة للمخرجات/المعاينات** (2026-07-01→03، و`main` تقدّم شهرًا بعدها): دمج أم إسقاط؟
9. **حسم Cloudflare Stream مقابل Mux** (يفكّ حجب مشغّل المراجعة دقيق الإطار).
10. **بدء تسجيل Apple Developer Program باسم المؤسسة الآن؟** (D-U-N-S يستغرق أسابيع — يوازي Waves 0–7)
11. **ترقية Vercel Pro: نعم/لا؟** (تحليلات · WAF · تحسين صور · سقف cron)
12. **حالة `SENTRY_DSN` وأسرار وجهة النسخ الاحتياطي** — `Configured` / `Missing` / `Unknown` **فقط**.

### 🔴 قرارات معمارية تحجب موجاتها
13. **D-1/D-2 Call Sheet:** `ops_call_sheets` أم `project_call_sheets`؟ *(يحجب Wave 3)*
14. **D-3 المواقع:** أي من الثلاثة هو المصدر؟ *(يحجب Wave 3)*
15. **D-4 إصدارات المخرجات:** `deliverable_versions` أم `project_deliverable_versions`؟ *(يحجب Wave 5)*
16. **سجل التدقيق:** عارض قراءة فقط يقرأ الخمسة عشر، أم `activity_log` وحده؟ *(يحجب V2-7.3-A)*
17. **التطبيق:** مستودع جديد `kian-app` أم توسيع `apps/mobile` القائم؟ *(يحجب Wave 9)*

### 🟡 محتوى وأعمال
18. **رقم سنوات الخبرة** (الكود يقول `20+` ثابتًا) — *Wave 1*
19. **أسماء عملاء «إعلان قصير» الثلاثة** (`Portfolio.tsx:109,111,113`) — *Wave 1*
20. **عدّادات الفئات:** 56 مقابل 46 بالتصميم (عضوية متعددة). عرض كما هو / توضيح / منع؟ — *Wave 1*
21. **`LEADS_WEBHOOK_URL`** — *Wave 1*
22. **دراسة الحالة السادسة** — *Wave 2*
23. **تأكيدات حق استخدام الشعارات** (62 شعارًا) + Publication Consent — *Wave 2*
24. **توحيد البريد:** `info@` / `sales@` / `contact@` — *Wave 2*

### 🔴 قانوني
25. **الموافقة الحكمية:** ما **نسخة العقد النهائية الموقّعة لمشروع «مسبار ١٠»**، وهل
    تتضمن بند الموافقة الحكمية، وإن وُجد **فما نصّه الحرفي**؟ — *Wave 5، والراية تبقى OFF حتى الجواب.*

---

*عند الشك: diff أصغر، تغيير إضافي لا استبدالي، واسأل خالد. **نبني على ما نملك — لا
نوازيه.*** 🎬
