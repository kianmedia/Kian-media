"use client";
// ════════════════════════════════════════════════════════════════════════
// /client-portal/explore — "Project Tools" for leads + clients (Phase 4).
// A non-empty visitor workspace: value header, requests center, brief builder,
// production needs calculator, free resources, account completion, community
// teaser. Staff/admin don't use this tab (their SETS don't include it).
// ════════════════════════════════════════════════════════════════════════
import VisitorDashboard from "@/components/portal/VisitorDashboard";

export default function ExplorePage() {
  return <VisitorDashboard />;
}
